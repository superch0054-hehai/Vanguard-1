# Neware 测试机通讯调通 —— 准备清单

> 核心结论：**这件事的瓶颈不是写代码，而是拿到"钥匙"和现场信息。**
> 通讯代码本身只有几百行（已写好，见 `README.md`），真正会卡住你的是
> 注册码、协议文档、通道四元组、以及"客户端和边缘盒子是不是同一台机器"。
> 这些事情都需要**别人给你**，所以必须最先启动。

---

## 一、立刻启动（有流程周期，越早越好）

这几项要走对方内部流程，可能几天到两周。**今天是 9/22，里程碑 10/15，没有余量。**

| # | 向谁要 | 要什么 | 没有它会怎样 |
|---|---|---|---|
| 1 | **Neware（新威尔）** | **API 模式注册码 / 激活码** | API 功能根本不工作。客户端【帮助 → 模式设置】填入并**重启客户端**后才生效 |
| 2 | **Neware** | **API 调试小程序（TestAPI）及其源码** | 源码是理解"联机"握手与报文细节的最快路径（见下方第 5 节的重要发现） |
| 3 | **Neware** | **与现场客户端版本对应的协议文档**（不要只拿 1.19） | 命令兼容性无法确认；现场客户端可能比 1.19 新或旧 |
| 4 | **Neware** | **`LogCode.csv`** | `log_code` / `protect` 等异常事件无法解读，SOH 报告里的异常归因写不出来 |
| 5 | **Neware** | **《辅助通道参数类型.docx》** | `V1/T1/Thick1/N14` 这些列名没有语义定义，无法确定它们是温度、电压还是厚度 |
| 6 | **Neware** | 客户端安装包 `BTS_Client_8.0.0_XXXX` + 服务器端 `BTS_Server_8.0.0_XXXX` | 官网 https://www.neware.com.cn/ 可下载 |
| 7 | **设备负责人** | 客户端软件版本号（【帮助 → 关于】） | **TCP 方式要求版本 ≥ 2023/3/17**；不确认就无法决定用管道还是 TCP |
| 8 | **设备负责人** | 管理员账号密码 | 管道方式要求客户端以**管理员权限**运行；截图显示现场登录用户是 `admin` |

> ⚠️ 第 1 项和第 2 项是**一票否决项**。如果这两样拿不到，整条 API 路线走不通，
> 只能退回到"导出 NDA/CSV 文件 + 离线解析"（见分析报告第 3 节）。

---

## 二、现场信息采集表（打印或复制填写）

这些信息决定架构，**不是可选项**。建议直接填完再动手写代码。

### A. 网络与拓扑

| 项 | 填写 |
|---|---|
| 运行 BTS Client 的电脑 IP | |
| BTS Client 与 TY1200 边缘盒子是否**同一台机器** | □ 是　□ 否 |
| 若不同机：两台机器能否互通（ping / 502 端口 telnet） | □ 能　□ 不能 |
| 502 端口是否被其他程序占用 | □ 未被占用　□ 被占用（进程：______） |
| 防火墙是否放行 502 | □ 已放行　□ 未放行 |

> **这张表决定用管道还是 TCP**：
> - 同机 → 管道 `\\.\pipe\NewareBtsAPI` 可用（但需管理员权限），也可用 `tcp 127.0.0.1`
> - 异机 → **只能 TCP**，因为协议明确"管道通讯方式不支持操作远程电脑上的客户端"

### B. 设备与通道

| 项 | 填写 |
|---|---|
| 设备型号（BTS80/81/82/83/84/85） | |
| 对应 devtype（22/23/24/25/26/27） | |
| 设备台数 | |
| 每台设备的 devid | |
| 每台设备的 subdevid 范围 | |
| 每个 subdevid 下的 chlid 范围 | |
| 通道总数 | |
| 是否有辅助通道（温度/电压/厚度/力） | □ 有（列出）：______　□ 无 |
| 是否有 DBC/CAN 工步 | □ 有　□ 无 |
| 通道 ↔ 电芯条码（barcode）的对应关系 | 见附表 |

> **通道四元组** `devtype-devid-subdevid-chlid` 是所有命令的寻址单位。
> 不要手工猜，直接跑 `python neware_client.py --transport tcp --host <IP> info`
> 会自动列出全部通道（见 `README.md`）。
>
> **通道 ↔ 条码映射是整个项目"按同一测试对象关联"的落点**，
> 也是 BatteryLab 里 Cell / Experiment 实体的入口，务必从现场拿到。

### C. 数据现状

| 项 | 填写 |
|---|---|
| 当前是否有通道正在跑测试 | □ 有　□ 无 |
| 有几个"已跑完"的测试可以拿来验证 | |
| 是否有历史 NDA/NDAX 文件可归档 | □ 有（约____个）　□ 无 |
| BTSDA 分析软件版本（用于做基准比对） | |
| 数据目录磁盘剩余空间 | |

---

## 三、环境准备步骤

1. **装客户端**：在目标机器上安装 `BTS_Client_8.0.0_XXXX`（需要的话一并装服务器端）。
2. **以管理员权限启动**客户端（管道方式必须；TCP 方式也建议，避免权限坑）。
3. **登录**（现场用 `admin`）。
4. **激活 API**：【菜单栏 → 帮助 → 模式设置】→ 填入注册码 → 点击【激活】→ **重启客户端**。
5. **验证激活**：在模式设置窗口把鼠标悬停在【已激活】上，应显示包含 **API** 的注册模式。
6. **腾出内存**：保持内存占用 **< 90%**。厂商明确说明超过 90% 会导致**取数不完整**，
   这是"数据缺行"最常见的非协议原因。
7. **关闭占用 502 的程序**，确认防火墙放行。
8. **准备 Python 环境**（若在边缘盒子上跑）：Python 3.9+；`pandas` + `pyarrow` 用于 Parquet 落盘
   （缺失会回落为 CSV，不阻塞验证）。

---

## 四、验证路径：8 步，每步都有明确通过标准

**顺序非常重要**：先用**厂商自己的 TestAPI 工具**证明"设备 + 客户端 + 协议"是通的，
再换成自己的代码。否则一旦失败，你分不清是设备问题、客户端问题还是自己代码问题。

### 第一阶段：用官方 TestAPI 打通（不要自己写代码）

| 步骤 | 操作 | 通过标准 |
|---|---|---|
| 0-1 | 管理员启动客户端 → 启动 TestAPI → 点【设备信息】→【发送】 | **输出框返回正确的设备信息** = 通讯正常 |
| 0-2 | 点【通道状态】→【发送】 | 返回的通道状态与客户端界面显示一致 |
| 0-3 | 点【查询RT数据】→【发送】 | 返回的电压/电流与客户端界面数字一致 |
| 0-4 | 点【下载数据】→ 改成目标通道 →【发送】 | 返回 `<data ...>` 行；字段含 volt/curr/cap/eng/atime |

> 第 0-1 步是最关键的分水岭。**它通了，说明钥匙和现场都没问题，剩下的都是软件活儿。**
> 若它不通：客户端是否运行 → 是否已激活 API → 是否重启过 →（管道）是否管理员启动
> →（TCP）502 是否被占用。

### 第二阶段：用自己的工具复现 + 体检

```bash
cd neware_comms

# 一键体检：8 个环节 + 单位校验 + SOH 数据可行性预览
python probe.py --transport tcp --host <客户端IP>
# 或同机走管道（需管理员权限的终端）
python probe.py --transport pipe
```

`probe.py` 会逐步给出 PASS / FAIL / WARN 与证据，并输出 `probe_report.json`。重点看：

| 检查项 | 期望结果 | 不通过说明什么 |
|---|---|---|
| `testtime` 单位判定 | **ms**（比值 ≈1000） | 若判为 s，说明现场行为与协议不符，所有时间换算要改 |
| `seqid` / `stepid` / `cycleid` 单调性 | 递增 | 非单调说明数据有乱序，SOH 取数逻辑要加排序 |
| 辅助通道列名 | 自动列出现场实际列名 | 列名与预期不同 → 解析不能硬编码列名 |
| `downloadStepLayer` 的 `dcir` | **有值** | 为空则 SOH 的内阻指标拿不到，需在工步里开 DCIR 计算 |
| 工步类型 | 都在协议 17 种之内 | 出现未定义类型需现场核对 |
| SOH 预览 | 能算出容量保持率/库仑效率/DCIR | 算不出说明口径与现场不一致，**必须在做报告前解决** |

> **这一步的产出直接决定 FR-03 能不能做。** SOH 预览能出来，
> 就意味着"数据读取 → 指标计算"这个链路已经验证完了。

---

## 五、重要发现：协议里提到但未文档化的"联机消息"

协议 2.1 写明：

> "通讯过程：**先要使用"联机消息"连接过来，然后才能进行其它命令进行通讯。**"

但协议的 22 条命令中**没有任何一条是"联机"命令**，API 使用文档的 TestAPI
工具截图里也没有"联机"按钮。

**这意味着三种可能**，必须现场确认：
1. 打开管道 / 建立 TCP 连接这个动作**本身就是**"联机"，无需额外命令；
2. 存在一条**未写入协议文档**的联机命令（例如 `<cmd>login</cmd>` 之类）；
3. 该要求只针对老版本或 BTSAPIServer 模式。

**应对**：
- 已提供 `raw` 子命令用于试探未文档化的命令：
  ```bash
  python neware_client.py --transport tcp --host <IP> raw "<cmd>login</cmd>"
  ```
- **这正是必须拿到 TestAPI 源码的原因** —— 源码里能看到它连接后第一条发的到底是什么。

---

## 六、常见故障速查

| 现象 | 排查顺序 |
|---|---|
| **管道方式：打开管道被拒绝** | ① 是否以管理员权限运行？② BTS Client 是否也在运行？③ 是否误连了远程客户端（管道不支持远程） |
| **管道方式：管道不存在** | ① BTS Client 未启动；② 对接的是 BTSAPIServer，应改用 `\\.\pipe\NewareBtsAPIServer`（且仅支持 127.0.0.1） |
| **TCP 连接被拒绝** | ① 客户端未启动；② 未激活 API 模式；③ 502 被占用；④ 防火墙；⑤ 客户端版本早于 2023/3/17 |
| **发了命令没反应** | ① 检查命令是否符合协议（大小写必须一致）；② **看客户端日志**（日志路径见 API 文档截图） |
| **取通道数据不完整** | ① **电脑内存是否超过 90%**（厂商明确指出的原因）；② 检查中位机数据上传情况 |
| **某条命令超过 30 秒无回应** | 协议规定可确认该命令失效并重发。本工具的 `--timeout` 默认 30 秒 |
| **`download` 只拿到 1000 条** | 正常。`count` 上限 1000，必须用 `startpos` 分页（`download_all` 已自动处理） |
| **`download` 报错或返回空** | `download` 只支持下载 **127.0.0.1 服务器**下的测试数据；确认 `testid` 是否正确（0=当前测试） |

---

## 七、时间倒排（距 10/15 还有约 3 周）

| 周次 | 目标 | 交付 |
|---|---|---|
| **W1** 9/22–9/28 | 拿到注册码 + 协议文档 + TestAPI 源码；激活 API；**TestAPI 打通 getdevinfo**；确认管道 or TCP；拿到通道四元组与条码映射 | 通讯层打通，`probe.py` 全 PASS |
| **W2** 9/29–10/5 | `probe.py` 跑出 SOH 预览；定死 SOH 指标口径；**同时向需求方催 SOH 报告模板** | 数据链路验证完毕，报告字段清单确定 |
| **W3** 10/6–10/12 | 按模板生成 SOH 报告；封装受控 API；Agent 编排 | 端到端 Demo 可跑 |
| **10/13–10/15** | 联调、演示准备、兜底演练 | 可演示 Demo |

> **W2 的 SOH 报告模板是关键路径**。模板不定，报告字段清单就定不下来，
> W3 会立刻堵住。建议 W1 就并行催。

---

## 八、开工第一天就能做的事（不依赖任何人）

1. 在自己电脑上跑离线自测，确认工具链本身没问题：
   ```bash
   cd neware_comms
   python selftest.py        # 70 项断言，用协议文档样例验证命令拼装与解析
   ```
2. 把第二节的**现场信息采集表**发给设备负责人，让他们填。
3. 把第一节的**索取清单**发给 Neware，正式提工单。
4. 催 SOH 报告模板。

第 1 项现在就能做完；第 2–4 项是"等别人"，所以要**今天发出去**。
