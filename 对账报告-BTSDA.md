# BTSDA 对账报告

对账日期：2026-09-24
对账对象：通道 `27-188-10-2`（BTS85，设备号 188，单元号 10，通道 2）
基准文件：`C:\Users\gzz25\Desktop\CCCC.xlsx`（BTSDA 官方导出，8 个工作表）
我方数据：通过 BTSAPI 协议从客户端读取的原始值

---

## 一、结论

**算法正确，但单位标注错了 1000 倍。**

协议文档（v1.19）说 `cap` 单位是 Ah、`curr` 是 A —— **文档是错的**。
实测客户端与 BTSDA 的真实单位是：

| 物理量 | 协议文档说的 | **实际（BTSDA 声明 + 数据验证）** |
|---|---|---|
| 电流 | A | **mA** |
| 电压 | V | V ✅ |
| 容量 | Ah | **mAh** |
| 能量 | Wh | **mWh** |
| 功率 | — | W |
| 内阻 | mΩ | mΩ ✅ |
| 温度 | — | ℃ |

**单位来源**：BTSDA 导出的 `unit` 工作表里有一行"单位方案清单"，
逐项列明了时间/电流/电压/容量/能量/功率/内阻/温度/气压的单位。
这是**厂商自己的声明**，比协议文档可靠。

---

## 二、证据：逐项对照

### 2.1 工步层（最直接）

| 工步 | 起始电压 我方 / BTSDA | 容量 我方原始值 | ×1000 | BTSDA 显示 |
|---|---|---|---|---|
| 搁置 | 0.2771 / **0.2771** ✅ | 0 | 0 | 0 |
| 恒流充电 | 0.2944 / **0.2944** ✅ | 2.11492e-05 | **0.0211492** | **0.0211 mAh** |
| 搁置 | 3.2378 / **3.2378** ✅ | 0 | 0 | 0 |
| 恒流放电 | 2.0550 / **2.0550** ✅ | 2.87777e-06 | **0.00287777** | **0.0029 mAh** |

**电压逐位一致**，说明读的是同一份数据、同一个时标。
**容量恰好差 1000 倍** —— 唯一解释就是单位是 mAh 而文档写了 Ah。
（BTSDA 显示值只保留 4 位小数，所以 0.0211492 显示成 0.0211，属正常四舍五入。）

### 2.2 循环层

| 项目 | 我方算出的 | BTSDA | 结论 |
|---|---|---|---|
| **首圈库仑效率** | **13.61 %** | **13.61 %** | ✅ **完全一致** |
| 首圈充电容量 | 2.11492e-05 | 0.0211 mAh | 数值同，单位应为 mAh |
| 首圈放电容量 | 2.87777e-06 | 0.0029 mAh | 同上 |
| 首圈充电能量 | 4.0496e-05 | 0.0405 mWh | 同上 |
| 首圈放电能量 | 2.5971e-06 | 0.0026 mWh | 同上 |

**关键**：库仑效率是"放电容量 ÷ 充电容量"，**比值不受单位影响**，
所以我们算出的 13.61% 和 BTSDA 一模一样。

**这说明我们"按圈分组、同圈放电容量除以充电容量"的口径是正确的。**

### 2.3 测试身份

| 项 | 我方读到 | BTSDA 导出 |
|---|---|---|
| 条码 | `D672035TAA112` | `D672035TAA112` ✅ |
| 设备号 | 188 | 188 ✅ |
| 启动时间 | — | 2026-09-23 08:26:27 |
| 结束时间 | — | 2026-09-24 19:29:07 |
| 工步文件 | — | HK-测试工步.xml |
| 创建者 | — | HK |

**确认对的是同一个测试。**

### 2.4 规模（我方是中途快照，BTSDA 是最终值）

| 项 | 我方读取时 | BTSDA 最终 | 说明 |
|---|---|---|---|
| 循环数 | 55 | **185** | 当时测试还在跑 |
| 工步数 | 225 | **741** | 同上 |
| 明细行数 | 12092 | **37048** | 同上 |

**这印证了一件事**：测试运行中读取的数据是**不完整的**，
`inquiredf` 报"完整=否"是准确的警告。

---

## 三、影响范围

### 3.1 受影响的（单位标错，必须改）

| 项 | 原标注 | 应改为 | 影响 |
|---|---|---|---|
| 容量 `cap` | Ah | **mAh** | 绝对容量值差 1000 倍 |
| 能量 `eng` | Wh | **mWh** | 同上 |
| 电流 `curr` | A | **mA** | 同上 |
| 内阻 `dcir` | mΩ ✅ | mΩ | 单位本来就对 |

### 3.2 不受影响的（比值类，单位约掉了）

| 指标 | 是否受影响 | 原因 |
|---|---|---|
| 库仑效率 | ❌ 不受影响 | 放电容量 ÷ 充电容量 |
| 能量效率 | ❌ 不受影响 | 放电能量 ÷ 充电能量 |
| 容量保持率 | ❌ 不受影响 | 末圈 ÷ 首圈 |
| DCIR 增长 | ❌ 不受影响 | 末圈 ÷ 首圈 |

**所以我们之前算出的那些"反常"数字，比率部分从来没错。**
13.61% 的首圈库仑效率、17.24% 的容量保持率，**BTSDA 的结论一致**。

---

## 四、附带确认的四件事

### 4.1 电流符号：充电为正、放电为负 ✅

按工步类型分组统计明细数据里的电流：

```
恒流充电    +0.014181 ~ +0.142093 mA     ← 正
恒流放电    -0.142105 ~ -0.014187 mA     ← 负
搁置         0
```

**充电为正、放电为负。** 这条协议文档没写，现在确定了。

### 4.2 辅助通道 `CPU` 是温度 ✅

BTSDA 的 `unit` 表把温度标为 ℃，`record` 表的列名是 `CPU(℃)`，
还有一列 `辅助通道温差(℃)`。所以 `CPU` 确实是**测试设备的 CPU 温度**。

⚠️ **仍然没有电池温度通道。**

### 4.3 测试已结束 ✅

结束时间 2026-09-24 19:29:07。所以现在可以读到**完整数据**了，
不再有"中途快照"的问题。

### 4.4 BTSDA 的明细表没有循环号

`record` 表的列是：数据序号 / 工步类型 / 时间 / 总时间 / 电流 / 电压 / 容量 /
能量 / 绝对时间 / 功率 / CPU / 辅助通道温差 —— **没有 `cycleid` 和 `stepid`**。

而 API 的 `download` **有** `cycleid` 和 `stepid`。
**说明 API 给的信息比 BTSDA 导出的还多**，这是个好消息。

---

## 五、★ 对账带来的一个重要发现

`test` 表里有两行信息，是这次对账最有价值的收获：

```
活性物质     1.184 mg
标称比容量   120 mAh/g
```

**理论容量 = 1.184 mg × 120 mAh/g = 0.142 mAh**

拿它对照实测：

| 项 | 实测 | 占理论容量 |
|---|---|---|
| 首圈充电容量 | 0.0211 mAh | **14.9 %** |
| 首圈放电容量 | 0.0029 mAh | **2.0 %** |
| 末圈放电容量 | 0.0005 mAh | **0.35 %** |

**并且**：工步方案里循环工步的电流是 0.1421 mA（1C），
前面 3 圈是 0.0142 mA（0.1C）—— 与 1.184mg × 120mAh/g 完全自洽：

```
0.1C × 0.142 mAh = 0.0142 mA   ✅ 与工步方案一致
1C   × 0.142 mAh = 0.1421 mA   ✅ 与工步方案一致
```

**这说明：容量量级完全正常，数据是可信的。**
但**活性物质利用率极低**（首圈放电只有理论的 2%），
这属于材料/工艺/测试条件的问题，**是需要专业人士判断的核心结论**。

---

## 六、DCIR 也验证通过了

DCIR 是最后一项没对上的。BTSDA 导出的 `cycle` 表和 `step` 表**都没有内阻列**，
所以没法直接抄数字对照。改用**反推验证**：从 BTSDA 导出的明细数据里，
自己算一遍 DCIR，再和 API 给的值比。

**推算方法**：DCIR = 工步切换点的 |ΔV| / |ΔI|

| 循环/工步 | ΔV (V) | ΔI (mA) | 反推 R (mΩ) | 我们读到 (mΩ) | 误差 |
|---|---|---|---|---|---|
| 循环1 恒流充电 | 0.017100 | 0.014181 | 1205843 | 1208500 | **0.22 %** |
| 循环1 恒流放电 | −0.034100 | −0.014190 | 2403063 | 2400277 | **0.12 %** |
| 循环2 恒流充电 | 0.018600 | 0.014189 | 1310883 | 1306783 | **0.31 %** |
| 循环2 恒流放电 | −0.036900 | −0.014188 | 2600774 | 2600996 | **0.01 %** |

**最大误差 0.31%。** 误差来源是 BTSDA 导出的电压只保留 4 位小数，
而 ΔV 本身只有几十毫伏 —— 0.0001 V 的舍入就带来约 0.6% 误差。
**所以这是导出精度限制，不是我们的问题。**

**结论：DCIR 就是工步切换点的 |ΔV|/|ΔI|，单位 mΩ，我们的值正确。**

---

## 七、完整字段表（已全部验证）

| 字段 | 含义 | 单位 | 验证方式 |
|---|---|---|---|
| `seqid` | 数据序号 | — | 从 1 连续递增，无缺号 |
| `cycleid` | 循环号 | — | 工步层与 BTSDA 循环表逐圈一致。⚠️ **明细里的值比工步层小 1**，见第十一节 |
| `stepid` | 工步号（**循环内归零**） | — | 与工步表一致 |
| `steptype` | 工步类型 | — | 与 BTSDA 工步类型一一对应 |
| `testtime` | 本工步已运行时间（**每工步归零**） | **毫秒** | 用真实数据反推确认 |
| `atime` | 绝对时间 | 本地时间 | 与 BTSDA「绝对时间」逐位一致 |
| `volt` | 电压 | **V** | 与 BTSDA 逐位一致 |
| `curr` | 电流（**充电为正、放电为负**） | **mA** | 对账确认 + 分组统计验证 |
| `cap` | 容量（**该工步内累计**） | **mAh** | 原始值 ×1000 = BTSDA 显示值 |
| `eng` | 能量（**该工步内累计**） | **mWh** | 同上 |
| `CPU`（辅助通道） | 设备 CPU 温度 | **℃** | 与 BTSDA `CPU(℃)` 列一致 |
| `stepindex` | 工步序号（**全局累加，不归零**） | — | 与 BTSDA 工步表一致 |
| `steptime` | 工步运行时长 | ⚠️ 推测毫秒 | **未直接验证**，见第八节 |
| `startvolt` / `endvolt` | 起止电压 | **V** | 与 BTSDA 一致 |
| `startcurr` / `endcurr` | 起止电流 | **mA** | 与 BTSDA 一致 |
| `dcir` | 直流内阻 | **mΩ** | 反推验证，误差 <0.31% |
| `startseqid` / `endseqid` | 起止数据序号 | — | 与明细序号能对上 |

**协议文档在这三处是错的**（文档 v1.19 写的是 Ah / Wh / A）：
`cap` 实际 mAh、`eng` 实际 mWh、`curr` 实际 mA。

---

## 八、还没验证的项（只剩一项）

| 项 | 状态 | 怎么确认 |
|---|---|---|
| `steptime` 的单位 | ⚠️ **未直接验证**，推测是毫秒 | 在 BTSDA 里看某个工步的「工步时间」（格式 `hh:mm:ss`），换算成毫秒，和我们 `step` 命令输出的 `steptime` 比。例如 BTSDA 显示 `01:29:21` = 5361 秒 = 5361000 毫秒，我们的 `steptime` 应是 5361000 而不是 5361 |

`steptime` 与已确认的 `testtime` 在协议文档里都标注为毫秒，
且都表示"工步内的时间"，所以是毫秒的可能性很高 —— 但**没有直接验证过**。

> 顺带说明：这个字段对现有分析不影响（算 SOH 用不到它），
> 但如果将来要做"工步耗时统计"就会用到，所以值得补一次确认。

## 九、本次对账已经修正的内容

| 文件 | 修改 |
|---|---|
| `probe.py` | 容量显示单位 Ah → **mAh**；内部键名 `_cap_ah` → `_cap_mah` |
| `export_review.py` | 字段字典里 `cap`/`eng`/`curr` 单位改为 mAh/mWh/mA，并标注"已对账确认"；图表纵轴单位改 mAh |
| `reconcile.py` | 对账项单位改 mAh |
| `selftest.py` | 同步键名 |
| 本报告 | 记录完整证据链 |

**注意**：协议文档 v1.19 在这一点上是错的（`cap:容量，单位Ah`）。
现场客户端是 2025.11.24 版，比文档新两年，**以实测为准**。

---

## 十、结论与后续

### 已验证 ✅

- 通讯链路正常，数据能完整取出
- **数据的数值与 BTSDA 完全一致**
- 比率类指标（效率、保持率）算法正确
- 单位已确定：容量 mAh、能量 mWh、电流 mA、内阻 mΩ
- 电流符号：充电为正、放电为负
- `CPU` 辅助通道是设备温度

### 已修正 ✅

- 代码里所有单位标注

### 需要专家判断 ⚠️

1. **首圈放电只有理论容量的 2%** —— 材料利用率为何这么低？
2. **185 圈衰减到 17%** —— 这个衰减速度是否符合预期？
3. ~~DCIR 2400 Ω 是否合理~~ —— **已由反推验证解决：数值正确**。剩下的只是"这个内阻对差电池而言是否正常"，属材料判断
4. 首圈库仑效率 13.61%（不可逆容量 86%）是否符合该材料特性？

### 交给专家时

把 `CCCC.xlsx`（BTSDA 原始导出）+ 本报告 一起交过去。
本报告已经把"数据对不对"这一层验证完了，专家只需判断"数据说明了什么"。

---

## 十一、实测发现的另一个坑：明细和工步层的 cycleid 差 1

在盒子上实采后发现的（通道 `27-188-10-1`，测试 247）：

| 来源 | 第 1 圈的 cycleid | 说明 |
|---|---|---|
| **工步层**（`downloadStepLayer`） | **1** | 与 BTSDA 的"循环号"一致 ✅ |
| **明细**（`download`） | **0** | 比工步层小 1 |

**其余字段完全对得上**（已逐段核对）：

```
工步1: seqid 1–781    明细里 stepid=['1']  工步层 stepid=1  ✅
工步2: seqid 782–798  明细里 stepid=['2']  工步层 stepid=2  ✅
工步3: seqid 799–1241 明细里 stepid=['3']  工步层 stepid=3  ✅
```

**只有 cycleid 这一个字段的起点不同。**

> 协议文档 v1.19 的 `download` 示例里写的是 `cycleid="1"`，
> 但**现场客户端实际给的是 0** —— 文档又错了一次。
> （API 文档的实测截图里也是 `cycleid="0"`，与现场一致。）

### 影响

| 场景 | 有没有影响 |
|---|---|
| 算 SOH（用**工步层**） | ❌ 无影响 —— `soh_preview` 用工步层，与 BTSDA 一致 |
| 按明细的 cycleid 分组做逐圈分析 | ⚠️ **会整体偏移一圈** —— 用之前先 `+1` |

**所以：要做逐圈分析就用工步层的数据；非要明细就记得 cycleid 要加 1。**
