# 天数彤央 TY1200：规格与使用

信息来自**天数智芯官方产品规格文档**（`7_Specification`）与官网产品页（iluvatar.com），
**具体以你手上那台的实物和随附文档为准。**

> ⚠️ 那份官方文档是**文档集的第 7 章（7_Specification）**，只讲产品介绍和硬件规格。
> **第 1～6 章**（大概是开箱、安装、首次开机、系统配置）才是我们真正需要的 ——
> 那里面应该有登录方式、默认账号、系统初始配置。**如果你手上有，请一并提供。**

---

## 一、官方规格

来源：**天数智芯官方产品规格文档**（`7_Specification`，Sphinx 格式）
＋ 官网产品页（iluvatar.com）。**以你手上那台的实物为准。**

| 项 | 规格 | 对你的意义 |
|---|---|---|
| **CPU** | **Intel Core Ultra 7 255H**（16 核 22 线程 = 6 P核 + 8 E核 + 2 LPE核）<br>另集成 **Intel AI Boost NPU** 和 **Intel Arc iGPU** | ✅ x86，Python 生态无障碍。★ 这台机器实际有 **CPU + NPU + iGPU + 天数 GPGPU 四种算力** —— 但都跟你无关 |
| **GPU** | 天数智芯数据中心级 GPGPU，**300 TOPS @ INT8**，兼容 CUDA 生态 | 大模型推理用，你不需要碰 |
| **显存** | **32GB HBM2e** | 决定能跑多大的模型 |
| **内存** | 2× 双通道 DDR5 5600MHz，默认 2 根 8GB，**共 16GB** | ⚠️ 你和模型共享，程序要克制 |
| **存储** | 1× M.2 2280 PCIe x4 插槽，默认 **512GB PCIe 4.0 NVMe** | ⚠️ 要和模型抢空间；可换更大 SSD |
| **操作系统** | **默认 Debian 12**，支持其他主流 OS | ★★ **重要，见 1.1** |
| **显示** | **1× HDMI 2.1** | ★★ **有显示输出 —— SSH 没开也能救** |
| **USB** | 4× USB 3.2 Type-A；2× USB4.0 / 雷电4 Type-C | ★ 可接键鼠 |
| **网络** | **2× RJ45 2.5GbE** | ★ 两个有线口，网络配置灵活 |
| **无线** | WiFi 6 / 蓝牙，内置天线 | 另一种接入方式 |
| **多功能拓展** | 2×I2C、1×SPI、1×RS232、1×RS485、2×CAN-FD、8×GPIO | 用不到 |
| **音频** | 1× 3.5mm 二合一 | — |
| 视频编解码 | 16 路编码 / 204 路解码 1080p@30fps | 用不到 |
| 供电 / 功耗 | 19V 16A；典型 ≤80W，峰值 ≤150W | 功耗低，适合长期开机 |
| 工作温度 | 0℃ ~ 50℃ | — |
| 存储温度 | −20℃ ~ 60℃ | — |
| 噪音 | 常温满载 **< 47 dB** | 放实验室不吵 |
| 认证 | ESD、安规、EMC、3C | — |
| 尺寸 / 重量 | 150×150×56 mm / 1506.84 g | 桌面级 |

**软件栈**：IXUCA（天数自研，兼容 CUDA 生态）
- 深度学习框架要用天数适配版，或直接用官方 Docker 镜像
- 推理引擎：IxRT / IGIE；大模型推理用 IxFormer（基于 vLLM）
- `ixsmi` 相当于 `nvidia-smi`

### 1.1 ★ 三条影响你开发的信息

**① 默认系统是 Debian 12，不是 Ubuntu**

Debian 12（bookworm）自带的 Python 是 **3.11**。

而你的笔记本 WSL 是 **Python 3.14** —— **两者的 wheel 不通用！**
所以如果盒子不能上外网、需要离线装包，**必须先确认盒子的 Python 版本**，
再用同版本去下载 wheel：

```bash
# 在盒子上先查
python3 --version

# 在笔记本上用 uv 装一个同版本的 Python 再下载
uv python install 3.11
uv pip download pandas pyarrow duckdb matplotlib --python 3.11 -d ./wheels
```

（Debian 12 的包管理是 `apt`，`systemd` 正常可用，这些和 Ubuntu 一样。）

**② 有 HDMI 2.1 + 4 个 USB-A —— SSH 没开也有救**

这是好消息。如果盒子的 SSH 服务没启动（很多工业设备出厂不装 `openssh-server`），
**接个显示器键盘直接操作就行** —— 不用特意跑一趟找别人。

上去之后执行：

```bash
sudo apt update && sudo apt install -y openssh-server
sudo systemctl enable --now ssh
```

**③ 有两个 2.5G 网口 —— 网络配置灵活**

如果实验室的网络是分开的（比如 ② 在一个网段、实验室办公网在另一个），
可以用两个网口分别接。**采集程序要连 ② 的 502**，
所以至少有一个网口要能到 `10.201.47.169`。

用哪个口、怎么配，问一下网络管理员或负责部署的人 ——
别自己乱改网络配置，因为大模型那边可能依赖现有配置。

### 1.2 ★ 怎么进系统（官方文档原文）

来源：官方文档《硬件说明》与《运行环境说明》。

**默认账号密码：**

| 项 | 值 |
|---|---|
| **用户名** | **`admin`** |
| **密码** | **`admin`** |
| 操作系统 | **Debian 12** |
| 内核版本 | **6.12.90** |
| 预装情况 | 出厂已预装**基础软件栈 + 算力软件栈**，一般无需更新 |

**开机流程（官方原文）：**

1. **连线**：取出电源适配器和主机，连电源适配器到主机电源接口；
   接上需要的外设（**HDMI 显示器、键鼠、网线**）；适配器 220V 端接电
2. **开机**：按下电源按键，等待设备启动完成；设备进入系统后，**输入密码进入系统**
3. **关机**：在操作系统主界面软关机，按提示选择

**说明有图形界面** —— 出厂就是带桌面的 Debian，可以直接接显示器键鼠操作。

> **SSH 是否默认开启，官方文档没有提到。** 如果连不上 22 端口，
> 接显示器上去执行：
> ```bash
> sudo apt update && sudo apt install -y openssh-server
> sudo systemctl enable --now ssh
> ```

### 1.3 ⚠️ 两条"别乱动"的提醒

**① 不要升级内核，不要 apt upgrade 系统包**

官方明确警告：

> 如果出现**内核升级导致的 GPGPU 驱动丢失**问题，需要重新安装天数软件栈驱动。

所以官方建议关掉内核自动升级：

```bash
sudo systemctl disable unattended-upgrades
sudo systemctl stop unattended-upgrades
```

**对我们的实际含义**：我们的采集程序不用 GPGPU，驱动丢了本身不影响我们 ——
**但会给别人的大模型搞出问题。** 所以：

- ❌ 不要 `apt upgrade`（尤其别升内核）
- ❌ 不要动 `/etc/apt` 的源配置
- ✅ 装 Python 包一律用 venv，不碰系统 Python

**② 官方推荐用 Docker 容器开发，但我们可以不用**

官方文档《工作环境》原文：

> 天数提供容器化的开发环境，**强烈建议使用容器进行开发**，减少手动部署环境带来的问题。

容器创建方式（官方给的命令）：

```bash
docker run -itd --name my_docker --privileged -v /code:/code \
  --shm-size="16g" --pid=host --net=host <镜像名称或ID> bash
```

| 参数 | 作用 | 对我们重要吗 |
|---|---|---|
| `--privileged` | 特权模式，否则容器**无法访问物理机 GPGPU 驱动** | ❌ 我们不需要 GPGPU |
| `--net=host` | 容器与宿主机**共用网络空间** | ✅ 意味着容器里能直接访问 ② 的 502 |
| `-v /code:/code` | 物理机与容器映射，方便传数据 | ✅ 代码和数据通过挂载目录进出 |
| `--shm-size="16g"` | 共享内存大小 | ❌ 用不到 |

镜像来自天数的私有仓库（`harbor.iluvatar.com.cn:10443` 或
`zibo.harbor.iluvatar.com.cn:30000`），需要先配 DNS 和 `daemon.json` 的
`insecure-registries`（细节见官方《工作环境》页）。

**我们怎么办**：容器化是**可选**的。采集程序是纯 Python、不碰 GPGPU、不需要特权模式，
**直接在宿主机上用 venv 更简单**。

**但如果将来要和 Agent 集成**，有个很干净的组合：

```
宿主机  admin 用户，venv
   └── 采集程序（连 ② 的 502 拿数据）→ 暴露一个本地 HTTP 接口

容器（--net=host）
   └── 大模型 / Agent → 通过 127.0.0.1 调用宿主机上的采集接口
```

因为容器用 `--net=host`，**容器内的 `127.0.0.1` 就是宿主机的 `127.0.0.1`**，
所以两边互通不需要配任何网络。

### 1.4 一个需要注意的点：Agent 框架可能是 OpenClaw

官方提供的镜像列表里有这几类（见官方《镜像信息》页）：

```
大模型镜像（vllm）
小模型镜像（deepsparkinference）
多媒体开发镜像（ixstream）
Agent 镜像（openclaw）        ← 注意这个
多模态镜像（VLA）
其他镜像 / 自研镜像
```

**厂商官方推荐的 Agent 框架是 OpenClaw**，而需求分析书里写的是
"Qwen Agent 应用框架 qwenpaw"（我在早期分析里就标注过这个名字疑似笔误）。

**建议跟负责部署大模型的人确认一次**：到底用哪个 Agent 框架？
这决定了将来采集程序要以什么形式暴露给它（HTTP 接口？MCP？还是直接读文件）。

---

## 二、最重要的结论：你的程序不需要碰 GPU

这是最该先知道的一件事。

**你的采集程序做的事是**：用 socket 发 XML 给 ② 的客户端、收数据、用 pandas 存 Parquet。

**这些和 GPU 完全无关。** 所以：

| 你不需要 | 为什么 |
|---|---|
| 不需要装天数适配版的 PyTorch | 你不用 GPU |
| 不需要懂 IXUCA 软件栈 | 同上 |
| 不需要用官方 Docker 镜像跑你的程序 | 那是给模型推理用的 |
| 不需要 `ixsmi` | 你监控的是采集，不是显存 |

**你需要的只是**：一个能跑的 Python 3.9+（标准库 + `pandas` + `pyarrow`），
以及一个能联网访问 ② 的网络环境。

**这大大降低了你的工作量** —— 盒子那套 GPU 软件栈是**部署大模型的人的活**，
不是你的。你俩在同一台机器上，但用的是完全不同的两套东西。

> ⚠️ 唯一要注意的：**不要动别人的环境**。
> 别去 pip 升级系统里的包，也别改系统的 Python ——
> 因为模型的运行环境可能依赖特定版本。**用虚拟环境**（venv）隔离你自己的依赖。

---

## 三、三个数字怎么影响你的方案

### 3.1 内存 16GB —— 你的程序必须克制

模型加载（官方说最高支持 35B）+ 操作系统 + 你的采集程序，
**共享这 16GB**。

所以采集程序要：
- 别一次把几百万行明细全读进内存（用**分批处理**或流式写入）
- pandas 读大 CSV/Parquet 时注意 `chunksize` 或直接用 DuckDB 做流式查询
- 不要开很多并发

**具体建议**：拉明细数据时**边拉边写盘**，不要在内存里累积整个列表。

采集库已经为这件事准备好了两个方法：

- `download_all()` —— 返回完整列表，**方便但会把数据堆在内存里**
- `iter_download()` —— **生成器版本，内存里同时只留一批**（默认 1000 条）

```python
# 内存受限时用这个：边拉边写，内存占用恒定
for batch in client.iter_download(channel):
    append_to_parquet(batch)          # 立刻落盘，然后丢弃
```

**在 16GB 内存的盒子上，明细数据一律用 `iter_download()`。**
工步层数据只有几千行，用 `download_all()` 无所谓。

### 3.2 存储 512GB —— 要和模型抢空间

粗略估算：

| 占空间的 | 估计 |
|---|---|
| 操作系统 + 驱动 | 20～50 GB |
| 大模型权重（27B，INT8 量化） | 约 27 GB |
| 模型运行时的临时空间、KV cache 等 | 几十 GB |
| **留给数据的** | **可能只剩 300～400 GB** |

你的数据大概多大：

| 数据类型 | 单个测试的体积 |
|---|---|
| 工步层汇总 | 几千行 → **几 MB** |
| 完整明细 | 几百万行 → **几十 MB 到几百 MB**（Parquet 压缩后） |
| 原始 NDA 文件 | 几 MB 到几百 MB |

**结论**：300～400GB 能放不少测试（几百到上千个），
**但这是个会慢慢耗尽的东西，必须从第一天就规划**：

1. **原始 NDA 文件的归宿要想清楚**：留在 ②（推荐）还是搬到 ③
2. **给数据单独划一个目录**，别和模型文件混在一起
3. **留出扩容余地**：官方支持扩到 2TB NVMe，早点提需求比满了再提好
4. **考虑给数据设个保留策略**：比如保留所有 derived（小）、
   normalized 按需保留、raw 永久保留在 ②

### 3.3 显存 32GB —— 这回答了需求书里的一个风险

需求分析书里的"风险 1：算力与模型匹配风险"现在**有数字可以算了**。

粗略的模型显存估算（不含 KV cache 和运行时开销）：

| 模型规模 | FP16 | INT8 | INT4 |
|---|---|---|---|
| 7～8B | 约 16 GB | 约 8 GB | 约 5 GB |
| 14B | 约 28 GB | 约 14 GB | 约 8 GB |
| **27B** | 约 54 GB ❌ **装不下** | 约 27 GB ⚠️ 勉强 | 约 14 GB ✅ |
| 32～35B | 约 64 GB ❌ | 约 32 GB ⚠️ 极限 | 约 17 GB ✅ |

**结论**：

- 需求书里说的 **27B 模型，用 FP16 是绝对装不进 32GB 的**，
  必须量化（INT8 勉强、INT4 宽松）
- 官方宣传"最高支持 35B 模型"，说明他们**有对应的量化方案**，
  但那是上限、是理想情况
- **加上 16GB 系统内存这个限制**，不能像服务器那样靠 CPU 卸载兜底

**给模型选型的建议**（这条要转告负责部署的人）：
不要看"参数越大越好"，要看**在 32GB 显存 + 16GB 内存下的实际可用性**。
14B 级别的模型在这个盒子上会跑得舒服得多，而 SOH 报告这类任务
未必需要 27B。**建议先在盒子上实测几个尺寸的模型，再定选型。**

> 顺带说：需求书里那个"qwen3.8-27B"的写法本身就有歧义（我在分析报告里标过）。
> 现在有了硬件规格，正好可以一起定下来。

---

## 四、怎么上手（分步）

### 第 0 步：先搞清楚这台盒子现在是什么状态

**这台盒子不是裸机** —— 出厂已预装基础软件栈和算力软件栈（见 1.2）。
所以**别急着重装系统或改环境**。

**已经查清的（来自官方文档，见 1.2）**：账号 `admin` / 密码 `admin`、
系统 Debian 12、有 HDMI 和 USB 可以接显示器键鼠。

**还需要问人的**：

| 问什么 | 为什么 |
|---|---|
| **大模型部署到哪一步了？谁在管？** | 决定你什么时候能用资源，以及**绝对不能动什么** |
| 盒子的 IP 是多少？接了哪个网口？ | 采集程序要连 ② 的 `10.201.47.169:502` |
| SSH 开了吗？（官方文档没写） | 决定你是远程开发还是接显示器开发 |
| 用哪个 Agent 框架？（OpenClaw 还是别的） | 决定采集程序要以什么形式暴露给它，见 1.4 |
| 有没有别人也在用这台机器？ | 协调使用时间，避免互相干扰 |

### 第 1 步：登录进去，摸清环境

```bash
# 系统和架构（预期是 Linux x86_64）
uname -a
cat /etc/os-release

# Python 版本（需要 3.9+）
python3 --version

# 磁盘现状 —— 这个最重要，看还剩多少
df -h

# 内存
free -h

# GPU 状态（看一眼就行，你不用管它）
ixsmi 2>/dev/null || echo "ixsmi 不可用（可能没装驱动工具，不影响你）"
```

**把 `df -h` 的输出记下来** —— 这是判断"数据能放多少"的依据。

### 第 2 步：建自己的隔离环境（不要污染系统环境）

```bash
# 用 venv 隔离，不碰系统的 Python
python3 -m venv ~/batterylab-venv
source ~/batterylab-venv/bin/activate

# 只装你需要的，不要装 GPU 相关的任何东西
pip install pandas pyarrow
```

**为什么必须隔离**：这台机器上跑着大模型，它的运行环境可能依赖特定版本的包。
你在系统 Python 里 `pip install` 有可能把模型环境搞坏。**这是会出人命的事。**

### 第 3 步：把采集代码放上去，先手动跑通

```bash
cd ~/neware_comms
python3 selftest.py                        # 74 项，不需要设备
python3 neware_client.py --transport tcp --host <②的IP> info
```

自测过了、`info` 能列出通道，就说明环境和网络都没问题。

### 第 4 步：做成常驻服务

用 systemd（Linux 的标准做法，比 Windows 任务计划程序清爽）：

```ini
# /etc/systemd/system/batterylab-collector.service
[Unit]
Description=BatteryLab 数据采集服务
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=<你的用户名>
WorkingDirectory=/home/<你的用户名>/neware_comms
ExecStart=/home/<你的用户名>/batterylab-venv/bin/python collector.py
Restart=always                 # 崩了自动重启
RestartSec=30
# 资源限制 —— 考虑到 16GB 内存要和模型共享，这里要克制
MemoryMax=2G                   # 最多用 2GB 内存
CPUQuota=100%                  # 最多用 1 个核

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now batterylab-collector
systemctl status batterylab-collector
journalctl -u batterylab-collector -f     # 看日志
```

**`MemoryMax` 和 `CPUQuota` 这两行很重要** —— 它保证你的程序永远不会
因为内存泄漏之类的问题把盒子搞崩，**尤其这台机器上还跑着大模型**。

---

## 五、要跟谁确认什么

| 事项 | 找谁 | 为什么重要 |
|---|---|---|
| 盒子的登录方式和账号 | 设备/IT 负责人 | 没这个进不去 |
| 系统版本、是否已部署模型 | 大模型部署的人 | 决定你的环境怎么搭、**不能动什么** |
| 磁盘规划：数据放哪、能占多少 | 大模型部署的人 | 512GB 要和模型共享，必须协调 |
| **能不能扩到 2TB NVMe** | 采购/负责人 | 如果数据量大，早点提需求 |
| 盒子是否长期开机 | 负责人 | 采集服务要求 24 小时在线 |
| 盒子和 ② 是否在同一网络 | 网络管理员 | TCP 要能通 |

---

## 六、一句话总结

**这台盒子对你的意义是"一台普通的 Linux x86 服务器"，不是"一台 GPU 机器"。**

- ✅ 你的采集程序是纯 Python，**不碰 GPU、不碰 IXUCA**
- ⚠️ 只有三件事要放在心上：**16GB 内存**（程序要克制）、
  **512GB 磁盘**（和模型抢空间）、**别污染别人的环境**（用 venv）
- ✅ CPU 是 x86，Python 生态无障碍，这是好事

**所以"怎么用"的答案是**：用 venv 搭个隔离的 Python 环境，
把你的代码放上去，用 systemd 做成带资源限制的常驻服务。
GPU 那一套完全不用你操心。
