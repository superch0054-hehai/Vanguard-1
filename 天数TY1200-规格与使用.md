# 天数彤央 TY1200：规格与使用

信息来自天数智芯官网产品页（iluvatar.com）及公开发布资料，
**具体以你手上那台的实物和随附文档为准。**

---

## 一、官方规格

| 项 | 规格 | 对你的意义 |
|---|---|---|
| **CPU** | **16 核 x86** | ✅ x86 架构，Python 生态无障碍（如果是 ARM，很多包要自己编译） |
| **GPU** | 天数自研 GPGPU 模块，**300 TOPS** 实测稠密算力 | 大模型推理用，你不需要碰 |
| **显存** | **32GB HBM2e** | 决定能跑多大的模型 |
| **内存** | **16GB LPDDR5x** | ⚠️ **这是硬约束，你的程序和模型共享** |
| **存储** | **512GB SSD，最高支持 2TB NVMe** | ⚠️ **要和模型抢空间，需要规划** |
| 尺寸 | 150 × 150 × 56 mm | 桌面级，很小 |
| 定位 | AI Agent 终端 | 官方说"最高支持 35B 模型""可完全离线运行" |

**软件栈**：IXUCA（天数自研，兼容 CUDA 生态）
- 深度学习框架要用天数适配版（PyTorch 等），或直接用官方 Docker 镜像
- 推理引擎：IxRT / IGIE；大模型推理用 IxFormer（基于 vLLM）
- `ixsmi` 相当于 `nvidia-smi`
- 支持主流 Linux 发行版

---

## 二、最重要的结论：你的程序不需要碰 GPU

这是最该先知道的一件事。

**你的采集程序做的事是**：用 socket 发 XML 给 ② 的客户端、收数据、用 pandas 存 Parquet。

**这些和 GPU 完全无关。** 所以：

| 你不需要 | 为什么 |
|---|---|
| 不需要装天数适配版的 PyTorch | 你不用 GPU |
| 不需要懂 IXUCA 软件栈 | 同上 |
| 不需要用官方 Docker 镜像跑你的程序 | 那是给模型推理用的 |
| 不需要 `ixsmi` | 你监控的是采集，不是显存 |

**你需要的只是**：一个能跑的 Python 3.9+（标准库 + `pandas` + `pyarrow`），
以及一个能联网访问 ② 的网络环境。

**这大大降低了你的工作量** —— 盒子那套 GPU 软件栈是**部署大模型的人的活**，
不是你的。你俩在同一台机器上，但用的是完全不同的两套东西。

> ⚠️ 唯一要注意的：**不要动别人的环境**。
> 别去 pip 升级系统里的包，也别改系统的 Python ——
> 因为模型的运行环境可能依赖特定版本。**用虚拟环境**（venv）隔离你自己的依赖。

---

## 三、三个数字怎么影响你的方案

### 3.1 内存 16GB —— 你的程序必须克制

模型加载（官方说最高支持 35B）+ 操作系统 + 你的采集程序，
**共享这 16GB**。

所以采集程序要：
- 别一次把几百万行明细全读进内存（用**分批处理**或流式写入）
- pandas 读大 CSV/Parquet 时注意 `chunksize` 或直接用 DuckDB 做流式查询
- 不要开很多并发

**具体建议**：拉明细数据时**边拉边写盘**，不要在内存里累积整个列表。

采集库已经为这件事准备好了两个方法：

- `download_all()` —— 返回完整列表，**方便但会把数据堆在内存里**
- `iter_download()` —— **生成器版本，内存里同时只留一批**（默认 1000 条）

```python
# 内存受限时用这个：边拉边写，内存占用恒定
for batch in client.iter_download(channel):
    append_to_parquet(batch)          # 立刻落盘，然后丢弃
```

**在 16GB 内存的盒子上，明细数据一律用 `iter_download()`。**
工步层数据只有几千行，用 `download_all()` 无所谓。

### 3.2 存储 512GB —— 要和模型抢空间

粗略估算：

| 占空间的 | 估计 |
|---|---|
| 操作系统 + 驱动 | 20～50 GB |
| 大模型权重（27B，INT8 量化） | 约 27 GB |
| 模型运行时的临时空间、KV cache 等 | 几十 GB |
| **留给数据的** | **可能只剩 300～400 GB** |

你的数据大概多大：

| 数据类型 | 单个测试的体积 |
|---|---|
| 工步层汇总 | 几千行 → **几 MB** |
| 完整明细 | 几百万行 → **几十 MB 到几百 MB**（Parquet 压缩后） |
| 原始 NDA 文件 | 几 MB 到几百 MB |

**结论**：300～400GB 能放不少测试（几百到上千个），
**但这是个会慢慢耗尽的东西，必须从第一天就规划**：

1. **原始 NDA 文件的归宿要想清楚**：留在 ②（推荐）还是搬到 ③
2. **给数据单独划一个目录**，别和模型文件混在一起
3. **留出扩容余地**：官方支持扩到 2TB NVMe，早点提需求比满了再提好
4. **考虑给数据设个保留策略**：比如保留所有 derived（小）、
   normalized 按需保留、raw 永久保留在 ②

### 3.3 显存 32GB —— 这回答了需求书里的一个风险

需求分析书里的"风险 1：算力与模型匹配风险"现在**有数字可以算了**。

粗略的模型显存估算（不含 KV cache 和运行时开销）：

| 模型规模 | FP16 | INT8 | INT4 |
|---|---|---|---|
| 7～8B | 约 16 GB | 约 8 GB | 约 5 GB |
| 14B | 约 28 GB | 约 14 GB | 约 8 GB |
| **27B** | 约 54 GB ❌ **装不下** | 约 27 GB ⚠️ 勉强 | 约 14 GB ✅ |
| 32～35B | 约 64 GB ❌ | 约 32 GB ⚠️ 极限 | 约 17 GB ✅ |

**结论**：

- 需求书里说的 **27B 模型，用 FP16 是绝对装不进 32GB 的**，
  必须量化（INT8 勉强、INT4 宽松）
- 官方宣传"最高支持 35B 模型"，说明他们**有对应的量化方案**，
  但那是上限、是理想情况
- **加上 16GB 系统内存这个限制**，不能像服务器那样靠 CPU 卸载兜底

**给模型选型的建议**（这条要转告负责部署的人）：
不要看"参数越大越好"，要看**在 32GB 显存 + 16GB 内存下的实际可用性**。
14B 级别的模型在这个盒子上会跑得舒服得多，而 SOH 报告这类任务
未必需要 27B。**建议先在盒子上实测几个尺寸的模型，再定选型。**

> 顺带说：需求书里那个"qwen3.8-27B"的写法本身就有歧义（我在分析报告里标过）。
> 现在有了硬件规格，正好可以一起定下来。

---

## 四、怎么上手（分步）

### 第 0 步：先搞清楚这台盒子现在是什么状态

**这台盒子可能不是"裸机"** —— 官方宣传"预装优化框架与主流大模型，开箱即用"。
所以要先弄清现状，**别急着重装系统或改环境**。

要确认的：

| 问什么 | 为什么 |
|---|---|
| 现在系统装好了吗？什么发行版和版本？ | 决定你的 Python 怎么装 |
| 大模型已经部署了吗？谁在管？ | 决定你什么时候能用自己的资源，以及**不能动什么** |
| 怎么登录？（SSH？接显示器键盘？） | 决定你怎么操作 |
| 账号密码 / SSH 端口 | — |
| 有没有别人也在用这台机器？ | 协调使用时间 |

### 第 1 步：登录进去，摸清环境

```bash
# 系统和架构（预期是 Linux x86_64）
uname -a
cat /etc/os-release

# Python 版本（需要 3.9+）
python3 --version

# 磁盘现状 —— 这个最重要，看还剩多少
df -h

# 内存
free -h

# GPU 状态（看一眼就行，你不用管它）
ixsmi 2>/dev/null || echo "ixsmi 不可用（可能没装驱动工具，不影响你）"
```

**把 `df -h` 的输出记下来** —— 这是判断"数据能放多少"的依据。

### 第 2 步：建自己的隔离环境（不要污染系统环境）

```bash
# 用 venv 隔离，不碰系统的 Python
python3 -m venv ~/batterylab-venv
source ~/batterylab-venv/bin/activate

# 只装你需要的，不要装 GPU 相关的任何东西
pip install pandas pyarrow
```

**为什么必须隔离**：这台机器上跑着大模型，它的运行环境可能依赖特定版本的包。
你在系统 Python 里 `pip install` 有可能把模型环境搞坏。**这是会出人命的事。**

### 第 3 步：把采集代码放上去，先手动跑通

```bash
cd ~/neware_comms
python3 selftest.py                        # 74 项，不需要设备
python3 neware_client.py --transport tcp --host <②的IP> info
```

自测过了、`info` 能列出通道，就说明环境和网络都没问题。

### 第 4 步：做成常驻服务

用 systemd（Linux 的标准做法，比 Windows 任务计划程序清爽）：

```ini
# /etc/systemd/system/batterylab-collector.service
[Unit]
Description=BatteryLab 数据采集服务
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=<你的用户名>
WorkingDirectory=/home/<你的用户名>/neware_comms
ExecStart=/home/<你的用户名>/batterylab-venv/bin/python collector.py
Restart=always                 # 崩了自动重启
RestartSec=30
# 资源限制 —— 考虑到 16GB 内存要和模型共享，这里要克制
MemoryMax=2G                   # 最多用 2GB 内存
CPUQuota=100%                  # 最多用 1 个核

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now batterylab-collector
systemctl status batterylab-collector
journalctl -u batterylab-collector -f     # 看日志
```

**`MemoryMax` 和 `CPUQuota` 这两行很重要** —— 它保证你的程序永远不会
因为内存泄漏之类的问题把盒子搞崩，**尤其这台机器上还跑着大模型**。

---

## 五、要跟谁确认什么

| 事项 | 找谁 | 为什么重要 |
|---|---|---|
| 盒子的登录方式和账号 | 设备/IT 负责人 | 没这个进不去 |
| 系统版本、是否已部署模型 | 大模型部署的人 | 决定你的环境怎么搭、**不能动什么** |
| 磁盘规划：数据放哪、能占多少 | 大模型部署的人 | 512GB 要和模型共享，必须协调 |
| **能不能扩到 2TB NVMe** | 采购/负责人 | 如果数据量大，早点提需求 |
| 盒子是否长期开机 | 负责人 | 采集服务要求 24 小时在线 |
| 盒子和 ② 是否在同一网络 | 网络管理员 | TCP 要能通 |

---

## 六、一句话总结

**这台盒子对你的意义是"一台普通的 Linux x86 服务器"，不是"一台 GPU 机器"。**

- ✅ 你的采集程序是纯 Python，**不碰 GPU、不碰 IXUCA**
- ⚠️ 只有三件事要放在心上：**16GB 内存**（程序要克制）、
  **512GB 磁盘**（和模型抢空间）、**别污染别人的环境**（用 venv）
- ✅ CPU 是 x86，Python 生态无障碍，这是好事

**所以"怎么用"的答案是**：用 venv 搭个隔离的 Python 环境，
把你的代码放上去，用 systemd 做成带资源限制的常驻服务。
GPU 那一套完全不用你操心。
