# 管道与 TCP：从物理层面讲清楚

这份文档不满足于打比方，而是讲清**数据到底在哪里、经过什么、变成什么形态**。
理解到这一层，你就知道每一条限制背后的真实原因，而不是死记"必须这样"。

---

## 一、先建立一个基本认识

两个程序之间"通讯"，物理上只做一件事：**把一串字节从 A 的内存搬到 B 的内存。**

区别只在于**怎么搬**：

| | 搬的方式 | 数据经过的物理介质 |
|---|---|---|
| **命名管道** | 在同一台电脑内部搬 | **只有内存条**（和 CPU 缓存） |
| **TCP** | 搬的方式取决于目的地 | **内存条 + 网卡 + 网线/光纤/空气** |

**这是两者最根本的物理区别。** 后面所有差异都是它的推论。

还有一点要先说清楚：**两个程序不能直接读写对方的内存。** 这是现代操作系统最基础的隔离机制（否则一个程序崩溃会连带毁掉另一个，也无法做权限控制）。

所以必须有个"中间人"，而这个中间人就是**操作系统内核**。所有通讯，本质上都是：

```
程序A → 【内核】→ 程序B
```

内核里放一块缓冲区（就是一段内存），A 把数据写进这块缓冲区，B 从里面读走。
**管道和 TCP 的差别，就在于这块缓冲区放在哪、以及数据要不要出这台电脑。**

---

## 二、命名管道（Named Pipe）

### 2.1 它到底是个什么"东西"

**命名管道是一个内核对象（kernel object），本质上就是内核内存里的一块缓冲区加一个名字。**

注意几个"没有"：

- **没有对应的磁盘文件**。你去硬盘上找不到任何一个文件叫 `NewareBtsAPI`。
- **没有经过网络**。数据不碰网卡。
- **没有专门的硬件**。

它就是一段内存。CPU 通过内存总线访问它，和访问普通变量没有本质区别，只是这段内存属于内核、而且被两个进程共享。

Windows 内核里，这类对象统一由**对象管理器（Object Manager）**管理，每个对象有引用计数、安全描述符、名字。管道就是其中一种。

### 2.2 名字为什么长这样

```
\\.\pipe\NewareBtsAPI
```

拆开看：

| 部分 | 含义 |
|---|---|
| `\\.\` | Win32 的**设备命名空间**前缀。表示"我要访问的是设备/内核对象，不是普通文件路径" |
| `pipe` | 对象管理器里的一个"目录"，所有管道都注册在它下面 |
| `NewareBtsAPI` | 这个管道的名字，由创建者（BTS Client）自己起 |

内核内部的真实路径其实更长（形如 `\Device\NamedPipe\NewareBtsAPI`），
`\\.\pipe\` 是给应用程序用的简写入口。

**"命名"两个字的意义**：因为它在对象管理器里有个名字，别的进程才能"按名字找到它"。
匿名管道（父子进程间用的那种）没有名字，只有拿到句柄的进程能用。

### 2.3 数据跑过的完整物理路径

假设我们在进程 A，BTS Client 是进程 B。我们发一条 `getdevinfo`：

```
① 进程A 用户态内存
   字符串 "<?xml ...><cmd>getdevinfo</cmd></bts>" 在 A 的内存里

② A 调用 WriteFile()
   CPU 从"用户态"切换到"内核态"（一次模式切换，有开销）

③ 内核把这段字节从 A 的用户态内存，拷贝到管道的内核缓冲区
   ★ 这就是"拷贝"，物理上是内存条上的字节复制
   ★ 数据现在躺在内核内存里（内核池），既不属于 A 也不属于 B

④ 进程B 调用 ReadFile()
   内核把字节从管道缓冲区拷贝到 B 的用户态内存
   CPU 再切换一次模式

⑤ BTS Client 拿到字符串，解析，干活
```

**关键结论：全程只有"内存 → 内存"的拷贝，一个字节都没有到网卡。**

这也是管道快的原因：没有协议栈处理、没有网卡、没有物理介质转换。

### 2.4 两块缓冲区，不是一块

管道是**双向**的，所以内核里实际有两块缓冲区：

- A→B 方向一块（我们发命令走这条）
- B→A 方向一块（对方回答案走这条）

所以"管道"其实是一对缓冲区，共享一个名字。这也是为什么叫"管道"——像两根管子。

### 2.5 谁创建、谁连接（这个顺序很重要）

管道的建立有个**主从关系**：

```
BTS Client（服务端）                          我们的程序（客户端）
─────────────────────────                    ─────────────────────
CreateNamedPipe("\\.\pipe\NewareBtsAPI")
   ↓ 创建内核对象，设好名字和安全属性
ConnectNamedPipe()
   ↓ 阻塞等待，内核里这个管道处于"等待连接"状态
                                              CreateFile("\\.\pipe\NewareBtsAPI")
                                                 ↓ 内核按名字找到那个对象
                                                 ↓ 做一次"访问检查"（见 2.6）
                                              ← 连接建立，双方的 ReadFile / WriteFile 可以用了
```

**这解释了一个常见故障**：如果 BTS Client 没启动，那个内核对象根本不存在，
我们 `CreateFile` 会立刻失败，报 `ERROR_FILE_NOT_FOUND`（错误码 2）。
Python 里表现为 `FileNotFoundError`。

我们的工具就是抓这个错误，告诉你"管道不存在：BTS Client 未启动"。

### 2.6 为什么必须管理员权限：内核的强制完整性控制

这是最容易被误解的一条。**不是新威在软件里写了个判断"你不是管理员就不理你"**，
而是 Windows 内核强制执行的。

从 Windows Vista 引入 UAC 之后，每个进程都有一个**完整性级别（Integrity Level）**：

| 级别 | 什么进程 |
|---|---|
| 高（High） | 以管理员身份运行的进程 |
| 中（Medium） | 普通用户启动的进程（默认） |
| 低（Low） | 沙箱进程（如浏览器渲染进程） |

**内核机制**：一个高完整性进程创建内核对象时，内核会给这个对象挂上一个
**强制完整性标签（Mandatory Label）**，内容是"**不允许更低完整性的进程写入**"
（`SYSTEM_MANDATORY_LABEL_NO_WRITE_UP`）。

于是：

```
BTS Client（高完整性）创建管道
   ↓ 内核给管道打标签：禁止低完整性进程"写"
我们的程序（中完整性，普通启动）
   ↓ CreateFile 请求"读写"权限
内核检查标签 → 你要"写"，但你不配 → 拒绝
   ↓
ERROR_ACCESS_DENIED（错误码 5）→ Python 的 PermissionError
```

**为什么"写"是关键**：我们要发命令，就必须有写权限。只读也许能过，但发不出命令。

所以"用管理员权限运行"不是迷信，是**绕过这个内核检查的唯一办法**——
把自己也提到高完整性，标签就不再阻止你了。

> 顺带说：协议文档那句"因为新威电池测试系统运行在管理员权限之下"，
> 说的就是这个现象，只是没解释机制。

### 2.7 为什么用两个换行符分隔：字节流 vs 消息流

Windows 命名管道有两种工作模式：

| 模式 | 行为 | 后果 |
|---|---|---|
| **字节模式（byte mode）** | 内核只当它是一串连续的字节，**不保留"A 写了几次"这个信息** | 应用层必须自己定义"一包到哪结束" |
| **消息模式（message mode）** | 内核保留消息边界，一次 `WriteFile` 对应一次 `ReadFile` | 不需要分隔符 |

**协议规定"所有协议都是通过两个紧靠换行符来判断包结束"** ——
这个设计反过来证明它用的是**字节模式**。

为什么？如果是消息模式，内核已经帮你划好边界了，根本不需要约定分隔符。
需要约定分隔符，正是因为**内核不保留边界，字节会连成一片**：

```
A 写了两次：  "GET /a"      "GET /b"
接收方读到的： "GET /aGET /b"   ← 粘在一起了，怎么知道哪是哪？
```

所以协议作者定了规矩：**用两个换行 `\n\n` 当作"这句话说完了"的标记。**
接收方就一直读，读到看见 `\n\n` 就知道一包结束了。

这跟我们代码里的做法有关联：我用的是"读到出现 `</bts>`"来判断结束。
原理完全一样（都是找一个结束标记），只是我选的标记是 XML 的闭合标签，
比 `\n\n` 更不容易误判 —— 因为数据内容里也可能出现换行。

### 2.8 缓冲区满了会怎样：背压

管道缓冲区大小有限（Windows 通常以 4KB 内存页为单位，可以设置）。

```
如果 B（接收方）读得慢，A（发送方）写得快：
   缓冲区逐渐填满
   ↓
   写满之后，A 的 WriteFile 会【阻塞】—— 停在原地等
   ↓
   A 的线程就卡住了
```

这叫**背压（backpressure）**：接收方处理不过来，压力会传导回去，逼发送方慢下来。

**为什么这跟我们的项目有关**：
`download` 一次可能返回上千条数据、几十 KB 甚至上百 KB。
如果我们的程序发了命令却不去读回应，BTS Client 写数据时就会被堵住 ——
而 BTS Client 同时在管着整个实验室的测试柜，它被堵住是**有实际风险的**。

所以我们的实现里有一条：**发一条，必须把回应读完，才发下一条**（协议规定的"问答式"）。
这不只是协议要求，也是在保护对方程序。

### 2.9 对方崩了会怎样：ERROR_BROKEN_PIPE

命名管道对象靠**句柄引用计数**维持生命。BTS Client 退出时，它持有的句柄关闭，
引用计数归零，**内核就销毁这个对象，管道的两块缓冲区也被释放**。

这时候我们如果还在读，内核返回 `ERROR_BROKEN_PIPE`（错误码 109），
字面意思"管道断了"。

**关键点：这个对象已经不存在了，你没法"修复"它。** 只能：
1. 等 BTS Client 重新启动（它会重新 `CreateNamedPipe`）
2. 重新 `CreateFile` 建立新连接

这解释了我们实现里的一个行为：**管道一旦超时或断开，我直接把句柄关掉、要求重连**，
而不是尝试在原连接上继续。因为原连接对应的内核对象可能已经没了。

> 对比一下 TCP：TCP 连接在对方崩溃时，你的 socket 句柄还存在（只是收到了 FIN/RST），
> 行为不太一样。这也是我们代码里管道超时用"关句柄让阻塞的读操作抛错"这种手法的原因 ——
> Windows 的管道读没有超时参数，只能靠外力打断。

### 2.10 为什么不能跨机器

因为它是**本机内核里的一个对象**。对象管理器的命名空间是**每台电脑各有一份**。
你这台电脑的内核里没有对方那台电脑的对象，你怎么"按名字找到它"？

技术上 Windows 确实支持远程管道（写成 `\\机器名\pipe\名字`），
但那是**底层偷偷走了 SMB 协议 + TCP 445 端口**，物理上已经变成网络通讯了，
而且需要对方开放文件共享、权限也得允许。

协议文档写"管道通讯方式不支持操作远程电脑上的客户端"，就是明确堵掉了这条路：
**你要跨机器，请直接走 TCP，别绕这一圈。**

---

## 三、TCP

### 3.1 socket 是什么

TCP 通讯的端点叫 **socket（套接字）**。它也是**内核里的一个数据结构**，里面装着：

- 本地 IP + 本地端口
- 对端 IP + 对端端口
- 发送缓冲区（内核内存，准备发出去的字节）
- 接收缓冲区（内核内存，收到还没被程序取走的字节）
- 连接状态机（SYN_SENT / ESTABLISHED / FIN_WAIT…）
- 序号、窗口大小、重传计时器等等

同样是"内核里一块内存"，但这次它**绑定着一块网卡**。

### 3.2 跨机时，数据跑过的完整路径

假设我们的程序在 TY1200 盒子上，BTS Client 在另一台电脑上。发一条命令：

```
① 进程A 用户态内存：字符串 "<?xml ...></bts>#\r\n"
        ↓
② send() 系统调用 → 切到内核态
        ↓
③ 字节拷进 socket 的【发送缓冲区】（内核内存）
        ↓
④ TCP 层干活：
     - 把字节流切成一段段（分段），每段不超过 MTU（以太网通常 1500 字节）
     - 给每段加上 TCP 头（含源端口、目的端口、序号、校验和）
        ↓
⑤ IP 层干活：加上 IP 头（含源 IP、目的 IP）
        ↓
⑥ 数据链路层：加上以太网帧头（含 MAC 地址）
        ↓
⑦ ★ 网卡（NIC）★
     - 内核通过 DMA 把内存里的帧直接交给网卡（不占用 CPU 搬运）
     - 网卡把帧变成【物理信号】：
         双绞线（网线）  → 差分电压的高低变化（电信号）
         光纤            → 光的明暗闪烁（光信号）
         WiFi            → 天线辐射电磁波
        ↓
⑧ ─────── 物理介质传输 ───────
        ↓
⑨ 对端网卡：把物理信号变回字节，DMA 写入对端内核内存
        ↓
⑩ 对端内核逐层拆头（以太网 → IP → TCP），校验、按序号排序
        ↓
⑪ 放进对端 socket 的【接收缓冲区】（内核内存）
        ↓
⑫ BTS Client 调用 recv() → 拷贝到它的用户态内存
```

**对比 2.3 节**：多了步骤 ④~⑩。这就是"物理上要出这台电脑"的代价。

### 3.3 一个重要的特例：本机回环（loopback）

如果目的地是 **127.0.0.1**（就是"我自己"），路径变成：

```
① 进程A 用户态内存
        ↓
② send() → 内核
        ↓
③④⑤⑥ 走的还是同一套 TCP/IP 协议栈（照样分段、加头、排号、算校验和）
        ↓
⑦ ★ 网卡这一层被【短路】了 ★
     内核发现目的地址是本机 → 直接把数据从发送缓冲区转到接收缓冲区
     数据【一步都没有出内存条】
        ↓
⑫ 进程B recv() → 拷到它的用户态内存
```

**这是很重要的一点**：

> **TCP 连 127.0.0.1 时，物理上不经过网卡、不经过任何网线。
> 数据始终在这台电脑的内存里。**

所以：

- 用 `TcpTransport("127.0.0.1", 502)` 和用管道，**物理路径其实很接近**，都是纯内存拷贝。
- loopback 比走真实网卡快得多，但比管道略慢 —— 因为它仍然要走完整的协议栈
  （分段、加头、校验、状态机），而管道不用。
- **loopback 不会被防火墙拦**（大多数防火墙默认放行回环），也不怕网线断。
  所以如果 BTS Client 和我们的程序在同一台机器上，**用 TCP 连 127.0.0.1
  是个很省心的选择**：不用管理员权限，也不用改防火墙。

这条对我们的项目很有用。因为管道需要管理员权限，而边缘盒子上可能不方便一直用管理员跑服务，
**同机时优先用 `tcp 127.0.0.1` 往往比管道更省事**。

### 3.4 端口号在物理上是什么

**端口号不是硬件，是内核里一张表（分用表）的索引。**

内核的 TCP 栈维护一张连接表，每个条目包含四元组：

```
(本地IP, 本地端口, 对端IP, 对端端口)
```

收到一个包时，内核干这个事：

```
拆开 IP 头 → 看目的 IP
拆开 TCP 头 → 看目的端口
      ↓
查表：哪个 socket 绑定了这个 IP:端口？
      ↓
找到了 → 把这个包交给那个 socket 的接收缓冲区
没找到 → 回一个 RST（拒绝），发送方看到"连接被拒绝"
```

**"502 端口被占用"在物理上是**：表里已经有一个 socket 绑定了 `*:502`。
内核不允许两个 socket 绑同一个端口（除非显式设了 `SO_REUSEADDR`），
所以 BTS Client 调用 bind(502) 时会拿到 `EADDRINUSE`，它的 TCP 监听功能就起不来。

于是你连过去会收到 RST，程序报 **`WSAECONNREFUSED`（10061）**。
—— 这正是我们之前测试时看到的那个错误码：

```
连接 127.0.0.1:502 失败：[WinError 10061] 由于目标计算机积极拒绝，无法连接。
```

"积极拒绝（actively refused）"这个词很准确：**不是超时（没人理），
而是对方内核明确回了一个 RST（有人在，但那个端口没人监听）。**
这两个错误的物理含义完全不同，排查方向也不同：

| 错误 | 物理含义 | 该查什么 |
|---|---|---|
| `10061` 连接被拒绝（收到 RST） | 包到了对方，但对方内核说"这个端口没程序监听" | 客户端没启动 / API 没激活 / 502 被占用 / **版本太老没有 TCP 功能** |
| 连接超时（无响应） | 包石沉大海，可能根本没到对方 | IP 写错 / 不在同一网段 / **防火墙丢包** |

### 3.5 三次握手：连接建立时物理上发生了什么

TCP 建立连接要交换三个包（这一步是内核自动做的，应用程序不感知）：

```
客户A                                          服务B
  │  ──── SYN（序号=x）──────────────────────→  │   "我想连你，我的起始序号是x"
  │  ←─── SYN+ACK（序号=y，确认=x+1）─────────  │   "收到，我也想连你，我的起始序号是y"
  │  ──── ACK（确认=y+1）────────────────────→  │   "收到"
  │                                             │
  │  ←── 双方都进入 ESTABLISHED 状态，可以传数据 →  │
```

**为什么要三次而不是两次**：为了让**双方都确认"对方收到了我的意图"**。
两次的话，服务端无法确认客户端是否收到了自己的 SYN+ACK。
两次会导致"历史重复连接"问题（客户端早已放弃，服务端却以为连上了）。

对我们的意义：`transport.connect()` 返回成功，意味着**这三个包都走通了**。
所以"连不上"和"连上了但收不到回答"是两个不同层面的问题：

- 连不上 → 网络/端口/防火墙层面的问题
- 连上了但收不到回答 → 应用层面的问题（命令格式不对、客户端没执行、在等更长时间）

我们的体检脚本把这两件事分成两个检查项（连接 / 各条命令），就是为了区分这两个层面。

### 3.6 TCP 替你保证了什么（"可靠"的物理含义）

TCP 号称"可靠传输"，具体是内核帮你做了这些事：

| 机制 | 物理上做什么 |
|---|---|
| **序号 + 确认（ACK）** | 每个字节都有编号，收到就回确认 |
| **超时重传** | 发出去了但没收到确认，等一段时间重新发一遍 |
| **乱序重排** | 包可能走不同路径、到达顺序乱了，按序号重新排好 |
| **去重** | 重传可能导致收到两份，按序号丢掉重复的 |
| **校验和** | 每个包有校验和，物理传输中位翻转能被发现 |
| **流量控制** | 接收方缓冲区快满了，通过"窗口大小"告诉对方慢点发 |

**所以：应用程序看到的是一个"有序、无重复、无损坏的字节流"，
完全看不到底下的分包、重传、乱序。**

但注意：**TCP 只保证字节流不丢不乱，不保证"消息边界"。**
就像水管，它保证水按顺序到，但不告诉你"这一瓢是哪次倒的"。
所以应用层仍然需要自己定分隔符 —— 这就是下一节。

### 3.7 为什么 TCP 方式发送要加 `#\r\n`

协议规定：管道方式报文末尾加 `\n\n`，**TCP 方式报文末尾加 `#\r\n`**。

原因就是上面说的：**TCP 是纯字节流，没有消息边界。**
管道好歹有字节模式/消息模式之分（虽然这里用了字节模式），
TCP 则完全没有这个概念 —— 它就是个字节水管。

所以协议必须自己定一个"我说完了"的标记。为什么管道用 `\n\n`、TCP 用 `#\r\n`？
**这是协议作者的选择，没有物理必然性**，可能只是历史原因（两套代码分别写的）。

> **一个值得注意的空白**：协议文档说了 TCP 发送要加 `#\r\n`，
> **但没说接收方怎么判断回应结束**。这是文档的一个缺口。
> 我们代码里的做法是"读到出现 `</bts>` 就认为收全了" ——
> 因为每条回应都有这个闭合标签，用它当结束标记最稳妥，不依赖未文档化的约定。

### 3.8 防火墙在物理上做什么

防火墙（Windows 上是 **WFP，Windows Filtering Platform**）不是一个独立的"门"，
而是**内核协议栈里的一组检查点**。

数据包到达时的物理流程：

```
网卡收到物理信号 → 变回字节 → 进入内核协议栈
                                  ↓
                       ★ WFP 检查点 ★
                       逐条匹配规则表：
                         - 允许？→ 继续往上交给 TCP 层
                         - 阻止？→ 直接丢弃（包就消失了）
                                  ↓
                           TCP 层 → socket 接收缓冲区
```

**关键**：被防火墙丢掉的包是**静默丢弃**，不返回任何错误。
所以从发送方看，现象是**超时**（包石沉大海），而不是"被拒绝"。

这解释了为什么排查要区分"拒绝"和"超时"——
如果 TCP 连 502 是超时而不是拒绝，八成是防火墙把包丢了，
而不是客户端没启动。

### 3.9 为什么有"版本 ≥ 2023/3/17"这个要求

这一条跟物理无关，是**软件功能问题**：
新威在那个日期之后的客户端版本里，才加了"监听 502 端口"这个功能。
老版本的客户端根本没调用 `bind(502)`，所以内核的表里没有条目，
你连过去只会拿到 RST（10061）。

**所以确认客户端版本号是必须的** —— 版本太老就只能用管道。

---

## 四、逐维度物理对比

| 维度 | 命名管道 | TCP（跨机） | TCP（127.0.0.1 回环） |
|---|---|---|---|
| **实体是什么** | 内核内存里的一对缓冲区 + 名字 | 内核 socket + 网卡 | 内核 socket（网卡被短路） |
| **数据经过的介质** | 只有内存条 | 内存 → 网卡 → 网线/光纤/电磁波 → 网卡 → 内存 | 只有内存条 |
| **是否经过网卡** | 否 | **是** | 否 |
| **是否跨机器** | 否 | 是 | 否 |
| **需要什么标识** | 管道名字字符串 | IP + 端口 | 127.0.0.1 + 端口 |
| **要不要管理员权限** | **要**（内核强制完整性控制） | 不要 | 不要 |
| **典型延迟量级** | 微秒级 | 0.1～几毫秒（千兆局域网） | 微秒级（比管道略慢） |
| **典型吞吐上限** | 受内存带宽限制（GB/s 级） | 千兆网约 125 MB/s | GB/s 级 |
| **会被防火墙拦吗** | 不会（不出机器） | 可能 | 一般不会（回环通常放行） |
| **加密** | 不需要（不出机器） | 默认不加密（明文） | 不涉及 |
| **对端崩溃时** | 内核对象被销毁，报 `ERROR_BROKEN_PIPE` | 收到 FIN/RST，socket 句柄还在 | 同左 |
| **消息边界** | 用字节模式，需自己定分隔符 | 无边界，需自己定分隔符 | 同左 |
| **协议规定结束标记** | `\n\n` | `#\r\n` | `#\r\n` |

---

## 五、这些物理差异如何决定我们的架构

### 5.1 `download` 为什么只能拿 127.0.0.1 的数据

回顾协议那条限制：**`download` 只支持下载服务器 127.0.0.1 下的测试数据。**

用物理视角看就清楚了：

```
BTS Client 进程
   ├── 它自己所在的那台电脑（127.0.0.1）—— 数据库/数据文件就在本机磁盘上
   │      → 它能直接读，所以 download 能给你数据
   │
   └── 其他"服务器"（192.168.1.33 等中位机）—— 数据在别的机器上
          → 它的进程没法直接读别人的磁盘，也不做代理
          → 所以给你不了
```

严格说中位机（middle）是硬件设备，数据是要"上传"到客户端所在机器的。
`inquiredf` 这个命令就是用来问"中位机把数据传完了没有"的。

**这不影响我们的方案**，因为——见下一条。

### 5.2 同机还是异机，方案的差别在哪

| | 同机 | 异机 |
|---|---|---|
| 可用方式 | 管道，或 TCP 连 127.0.0.1 | **只能 TCP**（管道物理上出不了这台机器） |
| 推荐 | 优先 **TCP 127.0.0.1**（不用管理员、不怕防火墙） | TCP 连对方 IP |
| `download` 的 127.0.0.1 限制 | 正好就是本机，无障碍 | **也不是问题**：从 BTS Client 自己的视角看，数据就在它的 127.0.0.1 上 |
| 物理链路风险 | 无 | 网线松动、IP 变动、防火墙、交换机故障 |
| 最大吞吐 | 内存带宽，很快 | 千兆网 125 MB/s（拉几百 MB 数据要几秒到几十秒） |

**注意最后那个结论很重要**：不管同机还是异机，`download` 的限制都不构成障碍。
因为"127.0.0.1"是**相对于 BTS Client 而言**的，不是相对于我们而言的。

### 5.3 一个实际的性能考虑

`download` 一次 1000 条，假设一条 200 字节，一包约 200 KB。

- **管道/回环**：一次往返微秒到毫秒级。拉 100 万条 = 1000 次往返，
  加上客户端从磁盘读数据的时间，可能要几分钟（瓶颈在客户端的磁盘和数据库查询，不在通讯）。
- **跨机千兆网**：1000 包 × 200KB = 200 MB，光传输就是 1.6 秒以上，
  再加上往返延迟（每次往返至少 0.1ms，1000 次 = 0.1 秒），影响不大。
  **真正的瓶颈仍然是 BTS Client 处理每条命令的时间。**

结论：**通讯方式对性能的影响不是主要矛盾。** 主要矛盾是协议规定的
"一次最多 1000 条、一次只能一个通道、必须问答式"这三个限制，
它们让取数的次数很多，而每次都有固定开销。

### 5.4 顺带解释一个厂商提到的奇怪现象

API 文档里有一条故障排查：

> "获取通道数据时，不完整。答：检查电脑内存是否超过 90%，
> 超过则关闭不用的软件使内存降至 90% 以下。或检查中位机数据上传情况。"

用物理视角看，两个原因都很实在：

1. **内存不足**：管道/socket 的缓冲区、TCP 的分段缓冲，都是内核内存（内核池）。
   系统内存紧张时，内核可能无法分配足够的缓冲，或者频繁换页，
   导致数据被截断或丢弃。另外 BTS Client 组装上千条数据的回应也需要大块内存。
2. **中位机上传**：数据先从中位机（硬件）上传到客户端所在电脑，
   上传没完成时，你 `download` 自然拿不到全部数据 —— 这时应该先用
   `inquiredf` 问一下"传完了没有"。

**第 2 条直接给了我们一个实现要点**：取数前应该先 `inquiredf`，
确认 `count` 稳定、`true`（全部上传完），再去 `download`。
我们的工具里有 `dfcheck` 子命令就是干这个的。

---

## 六、失败时你会看到什么：错误码 → 物理原因

| 你看到的错误 | 错误码 | 物理原因 | 对策 |
|---|---|---|---|
| `FileNotFoundError`（管道） | 2 `ERROR_FILE_NOT_FOUND` | 内核对象管理器里没有这个名字的管道 —— **对象不存在** | BTS Client 没启动；或对接的是 BTSAPIServer，要改管道名 |
| `PermissionError`（管道） | 5 `ERROR_ACCESS_DENIED` | 内核检查了管道的强制完整性标签，拒绝你的"写"请求 | 以**管理员权限**运行我们的程序 |
| `ERROR_BROKEN_PIPE`（管道） | 109 | 对方进程退出，内核销毁了管道对象 | 必须**重新连接**，不能复用旧连接 |
| `10061 WSAECONNREFUSED` | 10061 | 包到达对方，对方内核回 RST —— **端口无人监听** | 客户端没启动 / API 未激活 / 502 被占用 / 客户端版本太老 |
| 连接超时（`WSAETIMEDOUT`） | 10060 | 包被**静默丢弃**，没有任何回应 | IP 写错 / 不在同网段 / **防火墙丢包** |
| 连上了但收不到回应 | — | 连接层是好的，问题在应用层 | 命令格式不对（大小写！）；或对方在等更长时间；或"联机消息"没发 |
| 数据不完整 | — | 缓冲区被截断，或中位机还没上传完 | 内存降到 90% 以下；先 `inquiredf` 确认上传完成 |

**这张表的价值**：错误码能告诉你问题出在**哪一层**。
物理上没有"名字"这个对象 → 对方没创建；
物理上收到了 RST → 对方在但没人监听；
物理上什么都没收到 → 路上丢了。
**三者的排查方向完全不同，靠错误码区分，不要凭感觉猜。**

---

## 七、该用哪个：两者各自满足什么需求

### 7.1 先纠正一个常见误解

**两者能做的事完全一样。**

不是"TCP 功能更多"、也不是"管道更高级"。走通之后，你面对的是**同一套 22 条命令、
同样的字段、同样的数据**。今天能拿到的数据，换个连接方式一样能拿到。

所以它们满足的**不是功能需求，而是部署场景需求** ——
也就是"在什么条件下你还能连得上、跑得起来"。

### 7.2 各自唯一不可替代的能力

| | 唯一不可替代的能力 |
|---|---|
| **管道** | 不需要网络、不需要端口、不需要任何网络配置 |
| **TCP** | **能跨机器** |

只有这两条是"非它不可"的。其余差异都是可以权衡的次要点。

### 7.3 逐条需求对照

| 需求 | 管道 | TCP（127.0.0.1） | TCP（跨机） |
|---|---|---|---|
| 读取数据、控制测试 | ✅ | ✅ | ✅ |
| 同一台机器上通讯 | ✅ | ✅ | — |
| **跨机器通讯** | **❌ 物理上不可能** | ❌ | **✅ 唯一选择** |
| **不需要管理员权限** | **❌ 必须** | ✅ | ✅ |
| 不用管端口、不用配防火墙 | ✅ | ✅（回环一般不被拦） | ❌ 要确认 502 与防火墙 |
| 数据物理上不出这台机器 | ✅ 天然 | ✅ | ❌ 明文走网线 |
| 延迟 / 吞吐 | 最优（纯内存） | 接近最优（多走一遍协议栈） | 受网络限制（千兆网约 125 MB/s） |
| 客户端版本要求 | 无 | ≥ 2023/3/17 | ≥ 2023/3/17 |
| 适合长期常驻服务 / 容器化 | ❌ 很别扭 | ✅ | ✅ |

> 注意"不需要管理员权限"那一行：**这不是我的推测，是协议文档自己写的。**
> API 使用文档的排查指南里明确分成两句 ——
> "如果是**管道**通讯方式，检查 API 工具是否**管理员启动**；
> **TCP** 通讯方式，检查**端口**是否被占用"。
> 厂商自己就把这条差异区分开了。

### 7.4 选型决策树

```
运行 BTS Client 的电脑，和我们的程序，是同一台吗？
│
├─ 不是（异机）
│    └─→ 只能用 TCP，连对方 IP
│        （管道物理上出不了这台机器，没有选择余地）
│
└─ 是（同机）
     │
     ├─ 客户端版本 ≥ 2023/3/17，且 502 端口可用？
     │    └─→ 推荐 TCP 连 127.0.0.1
     │        理由：不用管理员权限、不用配网络、物理路径和管道几乎一样快
     │
     └─ 否则（版本太老，或 502 被无法释放的程序占用）
          └─→ 用管道（但必须管理员权限运行我们的程序）
```

**为什么同机也推荐 TCP 而不是管道**：两者物理路径很接近（第 3.3 节说了
127.0.0.1 不走网卡），但 TCP 省掉了"必须管理员权限"这个麻烦。
而管理员权限在后面对做**常驻服务**是实打实的障碍 —— 见 7.6。

### 7.5 四个典型场景

| 场景 | 选择 | 原因 |
|---|---|---|
| **A. 边缘盒子就是那台服务器** | TCP `127.0.0.1` | 同机，省掉管理员权限；也不怕防火墙 |
| **B. 边缘盒子是独立设备，客户端在实验室服务器上**（本项目最可能的情况） | TCP 连服务器 IP | 异机，管道做不到 |
| **C. 实验室规定不许开网络端口，或客户端版本太老** | 管道（前提同机） | 唯一能绕开端口和版本限制的路 |
| **D. 要做成长期运行的后台采集服务** | TCP（回环或跨机） | 管道需要管理员权限 + 独占本机命名空间，做服务很不合适 |

### 7.6 一个容易被忽略但很实际的点：常驻服务

后面这个项目要做的是**边缘端自动采集服务** —— 要一直跑着、自动发现新数据、自动入库。
这时候两种方式的差别会被放大：

| | 管道 | TCP |
|---|---|---|
| 服务要不要管理员权限 | **要**（否则连不上） | 不要 |
| 装成 Windows 服务 | 服务以 LocalSystem 跑，权限勉强够，但配置麻烦且权限过大 | 普通账号即可，最小权限 |
| 放进容器 | 几乎不可行（要访问宿主机命名空间 + 管理员权限） | 回环或跨机都自然 |
| 客户端重启后 | 内核对象销毁，服务必须检测 `ERROR_BROKEN_PIPE` 并重连 | 同样要重连，但机制更常规 |

**结论**：管道适合"人工在场、临时调试"；
**要做成无人值守的常驻采集服务，TCP 是更合适的形态。**

当然，如果现场条件只允许管道（比如客户端版本太老），也不是不能做 ——
只是要把"以管理员权限运行""断线重连"这两件事在部署文档里写清楚。

### 7.7 对本项目的具体结论

1. **先确认客户端和边缘盒子是不是同一台** —— 这一个问题就决定了有没有选择余地。
2. **异机** → 没得选，TCP。
3. **同机** → 优先 TCP `127.0.0.1`，管道作为退路（当 502 不可用或版本太老时）。
4. **无论选哪个，都要处理"客户端重启后重连"** —— 因为客户端是实验室里会被人关掉重开的。
5. 如果最终用管道，**部署文档里必须写明"需要管理员权限"**，否则后来接手的人会踩坑。

---

## 八、小结：记住这五句话

1. **两者都是"内核里一块内存"。** 程序不能直接读写对方内存，必须经过内核。
2. **管道的数据永远不出这台电脑**，物理上只是内存条上的字节拷贝 ——
   所以它快、但不跨机器、且受内核完整性级别限制（需要管理员）。
3. **TCP 跨机时数据要变成电信号/光信号出网卡**；
   但 **TCP 连 127.0.0.1 时网卡被短路，物理上仍然不出内存条** ——
   所以同机时用 TCP 回环是个又省事又安全的选择。
4. **端口号不是硬件，是内核分用表的索引**；"502 被占用"就是表里已经有条目。
5. **TCP 是字节流，没有消息边界** —— 所以协议必须自己定结束标记。
   管道这里也用字节模式，同样要定标记（`\n\n` / `#\r\n` / 我们的 `</bts>`）。

理解了这五句，前面那些限制（为什么管道要管理员、为什么不能跨机、
为什么要检查 502、为什么错误码要分"拒绝"和"超时"）就都是自然推论，不用背。
