# 开发工作流：程序写在 Linux 盒子上，怎么写、怎么调

现状：
- BTS Client 在 **② Windows 主机**（跑不了，也不用改）
- 你的程序要写在 **③ Linux 算力盒子**上
- 盒子大概没有图形界面（也不应该有）

**核心结论：你不需要盒子的图形界面。**

把盒子当成"远程执行环境"，**界面在你自己电脑上渲染**。
下面是怎么做。

---

## 零、用哪台电脑开发：Ubuntu 还是 Windows

**推荐用 Ubuntu。** 理由按重要性排：

### 0.1 和盒子环境同构，能消掉一整类问题

盒子是 Linux。本地也是 Linux 的话，就少了这些"本地能跑、盒子跑不了"的坑：

| 坑 | Windows 本地 | Ubuntu 本地 |
|---|---|---|
| 路径分隔符 | `\` 和 `/` 混用容易出错 | 只有 `/` |
| 换行符 | 可能 CRLF，拷到 Linux 后 bash 脚本报 `bad interpreter: /bin/bash^M` | LF |
| venv 结构 | `.venv\Scripts\` | `.venv/bin/` |
| 文件权限 | 概念不同，chmod 之类没法验证 | 和盒子一致 |
| 大小写敏感 | 不敏感（**可能盖住 bug**，到盒子上才炸） | 敏感，和盒子一致 |
| rsync | 要额外装 | 自带 |

**"大小写不敏感"这条特别阴险**：Windows 上 `import DataSource` 和文件 `datasource.py`
能混过去，搬到 Linux 上直接 ImportError。这类问题在开发期发现最省事。

### 0.2 离线装的包可以直接复用

如果盒子不能上外网（实验室很常见），你要在一台能上网的机器上下好 wheel 再拷过去。

**在 Ubuntu 上下载，wheel 和盒子需要的完全一致**，一条命令搞定：

```bash
pip download pandas pyarrow duckdb -d ./wheels
```

**在 Windows 上下载就得额外指定平台**，容易搞错：

```bash
pip download pandas pyarrow duckdb -d ./wheels ^
  --platform manylinux2014_x86_64 --only-binary=:all: --python-version 3.11
```

下了半天发现装不上，是新手很容易踩的坑。

### 0.3 systemd 配置文件能在本地先验证

你要给盒子写 systemd service 文件。在 Ubuntu 上可以先写好、
用 `systemd-analyze verify xxx.service` 检查语法，确认无误再拷到盒子上。
在 Windows 上只能盲写，然后到盒子上试错。

### 0.4 但有个决定性前提：先看网络能不能通到 ②

不管选哪个，**先确认哪套环境能访问到实验室的 ②**。

因为我建议的"先在本地跑通再搬上去"（第四节）依赖这个。**能访问到 ② 的那个优先。**

```bash
# 在候选环境里测
ping <②的IP>
python3 neware_client.py --transport tcp --host <②的IP> info
```

### 0.5 注意你的 Ubuntu 是哪种形态

| 形态 | 要注意什么 |
|---|---|
| **双系统** | 环境最干净、和盒子最像。缺点是切换系统麻烦 |
| **WSL**（Windows 里的 Linux） | ⚠️ **代码一定放在 WSL 的 Linux 文件系统里**（如 `~/batterylab`），**不要放 `/mnt/c/...`** —— 放那儿会有文件权限、行尾、性能三重问题。另外 WSL 默认能上外网，但访问局域网里的 ② 有时要额外配置 |
| **虚拟机** | ⚠️ 网络模式必须选**桥接（Bridged）**。NAT 模式下虚拟机在另一个网段，**可能访问不到实验室的 ②** |
| **另一台机器** | 最省事。同样注意和盒子、② 的网络连通性 |

**WSL 那条最常见的坑**：很多人在 Windows 里用 WSL，但把代码放在 `/mnt/c/Users/...` 下，
结果遇到一堆文件权限和行尾的怪问题。**把代码放 `~/` 下面就好了。**

### 0.6 那什么时候该用 Windows

- 你更熟 Windows，而且只是赶一个短期 Demo
- 你的 Ubuntu 网络访问不到 ②，而 Windows 能
- WSL 配不好，折腾时间比收益大

那就用 Windows —— VS Code Remote-SSH 在两端体验一样。
但要守住下面这三条跨平台要点。

### 0.7 不管选哪个，代码都要写成跨平台的

这样你不被绑死，两边都能跑。

**① 路径用 `pathlib`，不要手写分隔符**

```python
from pathlib import Path

out = Path("data") / "derived" / "steps.parquet"    # ✅ 两边都对
out = "data\\derived\\steps.parquet"                # ❌ 到 Linux 上就废了
```

**② 加一个 `.gitattributes`，强制换行符为 LF**

```
* text=auto eol=lf
```

不加的话，Windows 上写的 `.sh` 脚本传到 Linux 会报
`bad interpreter: /bin/bash^M`。这个错看起来吓人，其实就这一行配置的事。

**③ 平台差异显式判断，不要靠"碰巧能跑"**

比如"管道只能在 Windows 上用"这件事，就该显式判断并给出清楚的提示
（我们的代码已经这么做了：在非 Windows 上会明确告诉你"请改用 TCP"）。

**④ 一个必须保留的例外**

`start` 命令里的工步文件路径（如 `D:\stepManager\test\1.xml`）**本来就该是
② Windows 主机上的路径**，不是本机的路径。

**这不是 bug，是协议要求** —— 文件是客户端在它自己那台机器上读的。
在代码里要注释说明这件事，免得半年后有人"顺手把斜杠改成 `/`"。

### 0.8 顺带说：我们这套代码已经是跨平台的

- `neware_client.py` / `probe.py` / `selftest.py` 都只用了标准库，Windows 和 Linux 都能跑
- 管道在非 Windows 上会给出明确提示，而不是抛一个看不懂的异常
- `selftest.py` 的 81 项断言不依赖操作系统和设备，两边都能跑

所以你可以放心地在两种环境之间切换。

## 一、最关键的一招：VS Code Remote-SSH

装 [VS Code](https://code.visualstudio.com/)，然后装一个扩展：**Remote - SSH**。

装完之后你的体验是这样的：

| 你以为的 | 实际上的 |
|---|---|
| 要 SSH 进去用 `vim` 改代码 | 在 VS Code 里点开文件直接编辑，**和改本地文件一样** |
| 要看日志得敲一堆命令 | VS Code 里打开终端，`journalctl -f` 直接看 |
| 写错了要重传文件 | 保存即生效（编辑的就是盒子上的文件） |
| 没法调试 | 能打断点、单步、看变量 —— 调试器在盒子上跑，界面在你电脑上 |

**换句话说**：VS Code 在你电脑上画界面，实际的文件操作、命令执行、Python 进程全在盒子上。
**"盒子没有图形界面"这个问题就消失了** —— 你的图形界面就是你电脑上的 VS Code。

### 配置步骤

```bash
# ① 先确认能 SSH 上去（在你自己的电脑上执行）
ssh <用户名>@<盒子IP>

# ② 配免密登录，省得每次输密码
ssh-keygen -t ed25519              # 一路回车（已有 key 就跳过）
ssh-copy-id <用户名>@<盒子IP>
```

然后在本地 `~/.ssh/config` 里加个别名，以后 `ssh ty1200` 就能连：

```
Host ty1200
    HostName <盒子IP>
    User <用户名>
    # IdentityFile ~/.ssh/id_ed25519
```

VS Code 里：`Ctrl+Shift+P` → `Remote-SSH: Connect to Host` → 选 `ty1200`。

---

## 二、在盒子上准备环境（一次性）

连上之后，VS Code 的终端就是盒子的终端。执行：

```bash
# ① 看环境
uname -a
cat /etc/os-release
python3 --version            # 需要 3.9+

# ② 建项目目录
mkdir -p ~/batterylab && cd ~/batterylab

# ③ 建隔离的虚拟环境（★ 必须隔离，别污染模型的环境）
python3 -m venv .venv
source .venv/bin/activate

# ④ 只装需要的，不装任何 GPU 相关的东西
pip install pandas pyarrow duckdb
```

**为什么必须用 venv**：这台机器上跑着大模型，它的运行环境可能依赖特定版本的包。
你在系统 Python 里 `pip install` 有可能把模型环境搞坏。**这是会出人命的事。**

然后在 VS Code 里选解释器：`Ctrl+Shift+P` → `Python: Select Interpreter`
→ 选 `~/batterylab/.venv/bin/python`。

这样补全、检查、调试用的都是盒子上的环境。

---

## 三、代码怎么从本地到盒子（三种方式）

| 方式 | 命令 | 适合 |
|---|---|---|
| **Remote-SSH 直接编辑** | 无（编辑的就是盒子上的文件） | ✅ 开发期，最省事 |
| **scp** | `scp 文件 <用户>@<盒子>:/路径/` | 偶尔传个文件 |
| **rsync** | `rsync -av --exclude '__pycache__' --exclude '.venv' ./neware_comms/ <用户>@<盒子>:~/batterylab/` | 整个目录同步 |
| **git** | 本地 push → 盒子 pull | ✅ 正式部署，有版本记录 |

**建议**：开发期用 Remote-SSH 直接编辑（最快）；等稳定了把代码放进 git，
以后盒子上一句 `git pull && systemctl restart batterylab-collector` 就完成部署。

---

## 四、一个很实用的技巧：先在本地跑通，再搬到盒子

这一点很多人没想到，但能省掉大量麻烦：

> **因为你的代码是通过 TCP + IP 连 ② 的，所以"跑在哪台机器上"不影响代码本身。**
> 只要你自己的电脑能访问实验室网络，同一份代码在你电脑上和在盒子上都能跑。

所以推荐的节奏是：

```
第 1 步  在你自己的电脑上写、跑、调   ← 舒服，有完整 IDE，随便改
第 2 步  跑通了，再 rsync / git 到盒子  ← 只搬能跑的版本
第 3 步  在盒子上做成 systemd 服务     ← 只管"稳定运行"
```

**这样做的好处**：盒子只承担"稳定跑服务"这个职责，不承担"开发调试"。
你不在盒子上反复改代码、装包、试错，风险小得多。

**前提**：你的电脑要能连到 ②（同一个实验室网络，或者能访问到）。
如果连不上，那就只能在盒子上开发 —— 用 Remote-SSH 也还行。

---

## 五、调试和看结果怎么做

### 看运行情况

```bash
# VS Code 的终端里
systemctl status batterylab-collector
journalctl -u batterylab-collector -f          # 实时日志
cat ~/batterylab/status.json                   # 心跳文件
```

### 断点调试

在 VS Code 里建 `.vscode/launch.json`：

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "调试采集脚本",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/collector.py",
      "console": "integratedTerminal",
      "args": ["--host", "192.168.1.20"]
    }
  ]
}
```

按 F5 就能在盒子上的 Python 里断点调试，界面在你电脑上。

### 看数据文件

- **CSV / 小文件**：VS Code 左边文件树里点开就能看
- **Parquet**：装个扩展（如 *Parquet Viewer*），或者转成 CSV 再看
- **画图**：**不要在盒子上画**，把数据拉回本地画（见下）

```bash
# 从盒子拉数据回本地（在你自己电脑上执行）
scp ty1200:~/batterylab/data/derived/channel=25-41-1-1/testid=71/steps.parquet .
```

---

## 六、要不要给盒子装图形界面

**答案：不需要。** 上面那套流程，任何日常需求都不需要盒子有桌面。

| 你想做的事 | 怎么做 | 需要盒子有桌面吗 |
|---|---|---|
| 写代码、改文件 | VS Code Remote-SSH | ❌ |
| 跑脚本、看日志 | VS Code 终端 | ❌ |
| 断点调试 | VS Code 调试器 | ❌ |
| 看数据文件 | VS Code 打开 / 拉回本地 | ❌ |
| 画图表 | 本地画 | ❌ |
| 现场演示界面 | Web 页面，浏览器访问 | ❌ |
| 模型/Agent 的对话界面 | Web 页面，浏览器访问 | ❌ |

**注意最后两行**：如果盒子上要提供"界面给用户看"（比如 PPT 里的 AI 助手对话窗口、
数据看板），那都是**Web 应用** —— 盒子跑一个 HTTP 服务，用户用**任何一台电脑的浏览器**访问。
**这跟"桌面环境"是两件事**，完全不需要给盒子装图形界面。

### 为什么不建议装桌面环境

1. **吃内存**。GNOME / KDE 自己就要 1～2GB，这台盒子只有 16GB 还要装模型。
   真要装也只能装 XFCE 这类轻量的（约 300～500MB）。
2. **可能和加速卡驱动打架**。桌面环境要做图形渲染，而盒子的 GPU 是给推理用的，
   图形渲染路径未必被良好支持，容易出各种奇怪问题。
3. **多一个攻击面**。局域网里多开一个远程桌面端口，就多一个被扫的风险。
   需求书要求"数据不出实验室网络"，越少暴露越好。
4. **没必要**。见上表，一个日常需求都不需要它。

### 如果你还是想装（不推荐，但给方法）

```bash
# 装极轻量的 XFCE（不要装 GNOME/KDE）
sudo apt install xfce4 xfce4-goodies

# 装 xrdp 提供远程桌面（Windows 自带"远程桌面连接"就能连）
sudo apt install xrdp
sudo systemctl enable --now xrdp
```

然后 Windows 上用"远程桌面连接"连 `<盒子IP>:3389`。

**但请务必**：
- 用完就 `sudo systemctl stop xrdp` 停掉，不要长期开着
- 确认它不吃太多内存（装完用 `free -h` 对比一下）
- 先跟负责部署大模型的人**打招呼**，别影响他的环境

---

## 七、要提前确认的三件事

### 7.1 盒子能不能装 Python 包（★ 最容易卡住的一步）

实验室的机器**很可能不能上外网**。那 `pip install pandas` 就会失败。

```bash
# 先试一下
pip install --dry-run pandas
# 或者直接看能不能连外网
curl -I https://pypi.org
```

**如果不能上外网**，解决办法是**离线安装**：

```bash
# 在一台能上网的机器上（架构要和盒子一致，都是 x86_64）
pip download pandas pyarrow duckdb -d ./wheels

# 把这些 wheel 文件拷到盒子
scp -r ./wheels <用户>@<盒子>:~/

# 在盒子上离线安装
pip install --no-index --find-links ~/wheels pandas pyarrow duckdb
```

**注意 wheel 的平台标签**：要下 `manylinux` + `x86_64` 的（盒子是 Linux x86_64）。
如果在 Windows 上下载，要加 `--platform manylinux2014_x86_64 --only-binary=:all: --python-version 3.x`。

**建议早点试这一步**，因为离线装包可能要折腾一阵。

### 7.2 你的电脑能不能访问 ② 的网络

如果能，就可以"先在本地跑通再搬上去"（第四节），开发会舒服很多。
如果不能，就只能在盒子上开发（用 Remote-SSH 也还行）。

### 7.3 盒子上有没有别人在用

**先跟负责大模型部署的人确认**：
- 你怎么登录、用什么账号
- 数据放在哪个目录、能占多少空间
- 装 Python 包会不会影响他的环境（用 venv 就不会，但要告诉他）
- 你有没有 sudo 权限（装 systemd 服务需要）

---

## 八、具体场景：笔记本 Ubuntu（WSL）→ 天数 TY1200

现状：笔记本上是 WSL 的 Ubuntu，目标机器是 TY1200（**也是 Linux x86_64**）。
**两边都是 Linux，这是最干净的情况** —— 没有跨系统的路径、行尾、大小写问题。

### 8.1 先确认这六件事（前两件必须问人）

| # | 要确认的 | 谁来给 | 为什么重要 |
|---|---|---|---|
| 1 | **盒子的 IP、用户名、密码** | 负责部署大模型的人 | 没这个进不去 |
| 2 | **盒子上有没有开 SSH 服务** | 同上 | 很多边缘盒子出厂不装 `openssh-server`，得先装上或启用 |
| 3 | 笔记本能不能 ping 通盒子 | 自己测 | 网络层通不通 |
| 4 | **盒子能不能访问 ② 的 502** | 在盒子上测 | ★ 采集程序的核心依赖 |
| 5 | 盒子的 Python 版本 | 在盒子上测 | 决定装包方式（要和 wheel 匹配） |
| 6 | 盒子能不能上外网 | 在盒子上测 | 能上就直接 pip 装；不能上要离线准备 wheel |

**第 2 件最容易卡住**：如果 SSH 没开，你得接显示器键盘上去执行
`sudo apt install openssh-server && sudo systemctl enable --now ssh`。
先问清楚，省得白跑一趟。

### 8.2 四步走

**第一步：从笔记本测网络**

```bash
# 在笔记本的 WSL 里
ping -c 3 <盒子IP>
timeout 5 bash -c "echo > /dev/tcp/<盒子IP>/22" && echo "22 端口通（SSH 可用）" || echo "22 不通"
```

**第二步：配 SSH 免密**

```bash
ssh-keygen -t ed25519              # 已有可跳过，一路回车
ssh-copy-id <用户名>@<盒子IP>
```

然后在笔记本的 `~/.ssh/config` 里加别名（WSL 和 Windows 各有一份，改 WSL 那份就行）：

```
Host ty1200
    HostName <盒子IP>
    User <用户名>
```

以后 `ssh ty1200` 直接进。

**第三步：在盒子上建隔离环境**

```bash
ssh ty1200
python3 --version                  # 要 3.9+
mkdir -p ~/batterylab && cd ~/batterylab
python3 -m venv .venv
source .venv/bin/activate
pip install pandas pyarrow duckdb matplotlib
```

⚠️ **必须用 venv**，因为这台盒子跑着大模型，污染它的环境是要出事的事。

**如果盒子不能上外网**，需要离线准备 wheel。注意 **wheel 和 Python 版本绑定**：
先问出盒子的 Python 版本（比如 3.12），再在能上网的机器上下载匹配的：

```bash
# 在笔记本的 WSL 里（如果 WSL 的 Python 版本和盒子一致，直接下载即可）
pip download pandas pyarrow duckdb matplotlib -d ./wheels

# 版本不一致时，用 uv 装一个同版本的 Python 再下载
uv python install 3.12
uv pip download pandas pyarrow duckdb matplotlib --python 3.12 -d ./wheels
```

然后拷过去：

```bash
scp -r ./wheels ty1200:~/
# 在盒子上
pip install --no-index --find-links ~/wheels pandas pyarrow duckdb matplotlib
```

**第四步：VS Code 连上去开发**

笔记本装 VS Code，装 **Remote - SSH** 扩展，
`Ctrl+Shift+P` → `Remote-SSH: Connect to Host` → 选 `ty1200`。

连上之后：
- 左侧文件树是盒子上的文件
- 终端是盒子的终端
- `Python: Select Interpreter` 选 `~/batterylab/.venv/bin/python`
- 断点调试在盒子上跑，界面在笔记本上

**开发体验和你现在在 WSL 里几乎一样。**

### 8.3 代码放哪：三份拷贝怎么管

现在会有三处代码：

| 位置 | 建议角色 |
|---|---|
| **盒子** `~/batterylab/` | ★ **运行版本** —— 最终跑在这里 |
| 笔记本 WSL `~/batterylab/` | 开发/离线备份 |
| Windows `neware_comms/` | 存档（`.md` 在 Windows 上打开方便） |

**别三处同时改。** 两个可选做法：

**做法 A（推荐，省事）**：直接在盒子上开发（Remote-SSH 编辑的就是盒子上的文件）。
本地那份不动，当离线备份。

**做法 B**：本地开发，改完同步到盒子：

```bash
# 在笔记本的 WSL 里执行
rsync -av --delete --exclude '.venv' --exclude '__pycache__' \
  ~/batterylab/ ty1200:~/batterylab/
```

**Demo 阶段建议用做法 A** —— 少一步同步，而且调试的环境就是运行的环境，
不会出现"本地好的、盒子上不行"。

### 8.4 两个提醒

**① 盒子是共用的机器。** 用 venv 隔离依赖、别改系统 Python、
别装系统级的东西、别占用太多内存/磁盘。改之前跟负责大模型的人打个招呼。

**② 盒子上也要能访问 ② 的 502。** 这是整个采集链路的命门。
在盒子上先测：

```bash
timeout 5 bash -c "echo > /dev/tcp/10.201.47.169/502" && echo "502 可达" || echo "502 不可达"
```

**如果不可达**，先解决网络（可能和笔记本不在同一网段、或者需要走不同网卡），
别急着往上装东西。

---

## 九、完整流程速览

```
【你自己的电脑】                        【③ Linux 盒子】
  写代码（VS Code Remote-SSH）  ──────→  文件直接落在盒子上
  跑、调（VS Code 调试器）      ──────→  Python 进程在盒子上跑
  看日志（VS Code 终端）        ←──────  journalctl / status.json
  画图（本地 matplotlib）       ←──────  scp 拉回 Parquet

开发期：本地跑通 → rsync/git 到盒子
运行期：盒子跑 systemd 服务（带 MemoryMax/CPUQuota 限制）
运维期：心跳文件 + journalctl + 一个只读状态页
```

**一句话**：**盒子只负责"稳定跑"，界面和开发体验全在你自己的电脑上。**
所以"盒子没有图形界面"不仅不是问题，反而是对的做法 ——
省内存、少风险，而且不影响效率。
