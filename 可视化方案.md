# 可视化方案

"这个盒子的可视化"能指四件不同的事。这份文档分别讲，你对一下是哪种。

| 含义 | 一句话 |
|---|---|
| **A. 怎么看见盒子本身** | 你没有显示器，怎么操作它 |
| **B. 怎么看见你的服务在干什么** | 采集服务在后台跑，怎么知道它活着、在采什么 |
| **C. 数据可视化在哪做** | 曲线图、容量保持率图，在哪台机器上画 |
| **D. 最终 Demo 的可视化形态** | 交给需求方看的东西长什么样 |

---

## 一、先记住一条总原则

> **可视化要在"看的人"那边做，而不是在"数据所在"那边做。**

盒子只有 16GB 内存，还要跑大模型。而看数据的人**坐在自己的电脑前面**，
不在盒子前面。

所以正确的分工是：

| 盒子（③） | 你的电脑 |
|---|---|
| 采集数据、落盘 | 画图、看曲线 |
| 计算指标（可选） | 交互式分析 |
| 提供数据（文件或接口） | 渲染页面 |

**常见错误**：在盒子上装 Jupyter / 桌面环境 / 重量级 BI 工具。
这些都会吃掉模型的内存，而且你根本不需要在盒子上看 —— 你在自己电脑上看得更舒服。

---

## 二、A. 怎么看见盒子本身（远程访问）

### 四种方式对比

| 方式 | 用途 | 建议 |
|---|---|---|
| **HDMI + 键鼠** | 首次装机、救急 | ⚠️ 只在盒子确实有显示接口时用；**一次性**，不要日常挂着 |
| **SSH** | 日常全部操作 | ✅ **推荐，这是唯一真正需要的** |
| 远程桌面（xrdp / VNC） | 需要图形界面时 | ❌ **不建议**，见下面理由 |
| 厂商 Web 控制台 | 看硬件/驱动状态 | ❓ 问厂商有没有，有的话用它的 |

### 为什么不建议给盒子装桌面环境

1. **吃内存**。GNOME / KDE 这类桌面环境自己就要 1～2GB。
   这台盒子只有 16GB，还要装下大模型。
2. **可能和加速卡驱动打架**。桌面环境要做图形渲染，而盒子的 GPU 是给推理用的，
   图形渲染路径未必被良好支持，容易出各种奇怪问题。
3. **你没有需要图形界面的操作**。采集是后台服务、看日志是 `journalctl`、
   传文件是 `scp` —— 全部用命令行更简单可靠。
4. **多一个攻击面**。局域网里多开一个远程桌面端口，就多一个被扫的风险。
   需求书要求"数据不出实验室网络"，越少暴露越好。

**结论**：日常用 SSH。如果盒子有 HDMI 口，首次调试时接一下显示器键盘最省事
（能看到启动过程、网卡配置），配好网络和 SSH 之后就拔掉。

### SSH 上手

```bash
# 从你的电脑连过去
ssh <用户名>@<盒子IP>

# 看系统
uname -a
cat /etc/os-release

# 看资源（这三个最常用，记住）
df -h            # 磁盘
free -h          # 内存
htop             # 进程（没装就用 top）
```

**建议**：
- 配 **SSH 免密登录**（`ssh-keygen` + `ssh-copy-id`），省得每次输密码
- 配一个 `~/.ssh/config` 别名，比如 `ssh ty1200` 就能连上
- **不要改 sshd 配置**（比如换端口），除非你确定不会把自己关在外面

---

## 三、B. 怎么看见"你的服务"在干什么

采集服务是后台跑的，看不见摸不着。要让它"可见"，分三层做。

### 第 1 层：systemd 自带的（零成本，必做）

```bash
systemctl status batterylab-collector        # 活着吗？跑多久了？重启过几次？
journalctl -u batterylab-collector -f        # 实时看日志
journalctl -u batterylab-collector --since "1 hour ago"
journalctl -u batterylab-collector -p err    # 只看错误
```

**看什么**：`Active: active (running)` 说明活着；
**`restart` 次数很关键** —— 如果重启了 20 次，说明程序在反复崩，得查。

### 第 2 层：写一个"心跳文件"（简单，强烈建议）

让采集服务每次循环都更新一个小 JSON 文件。你 SSH 上去 `cat` 一下就全知道了：

```json
{
  "updated_at": "2026-10-01 09:15:33",
  "uptime_hours": 71.2,
  "channels_seen": 16,
  "tests_completed_today": 3,
  "last_collected": {
    "channel": "25-41-1-1",
    "barcode": "BAR001",
    "testid": 71,
    "rows": 1284371,
    "at": "2026-10-01 08:52:10"
  },
  "last_error": null,
  "pending": []
}
```

```bash
cat /var/lib/batterylab/status.json
```

**为什么这个比日志好用**：日志要翻、要筛，而心跳文件是"当前状态的快照"，
一眼就能看出"最后一次成功采集是什么时候"。

**关键作用**：如果 `updated_at` 停在几小时前，说明服务卡住了 ——
**这是你唯一能发现"服务静默卡死"的办法**。后台服务最怕的就是这个：
进程还在、没报错、但什么都没干。

### 第 3 层：一个极简只读状态页（可选，Demo 时很加分）

如果想让别人也能看到，可以在盒子上开个极小的 HTTP 服务，
读那个 `status.json` 渲染成网页。用 Python 标准库就能做，不用装框架：

```python
# 大约 20 行，只读，不做任何写操作
from http.server import HTTPServer, BaseHTTPRequestHandler
import json

class Status(BaseHTTPRequestHandler):
    def do_GET(self):
        data = json.load(open("/var/lib/batterylab/status.json"))
        html = f"<h2>采集服务状态</h2><pre>{json.dumps(data, ensure_ascii=False, indent=2)}</pre>"
        self.send_response(200)
        self.send_header("Content-Type", "text/html; charset=utf-8")
        self.end_headers()
        self.wfile.write(html.encode())
```

局域网内浏览器打开 `http://<盒子IP>:8080` 就能看到。

**注意事项**：
- **只读**，绝不提供任何能改数据的接口
- 绑到内网 IP，不要绑 `0.0.0.0` 暴露出去
- 加个 `systemd` 服务和资源限制（`MemoryMax=128M` 就够）
- 需求书要求"数据不出实验室网络"，这个页面只能在内网看

---

## 四、C. 数据可视化在哪做

### 4.1 三个层次的做法

| 做法 | 在哪台机器 | 适合 |
|---|---|---|
| **拷 Parquet 到本地画图** | 你自己的电脑 | ✅ **最简单，推荐** |
| **本地 DuckDB 直接查** | 你的电脑（或读共享目录） | ✅ 想用 SQL 做聚合时 |
| 盒子上跑 Web 服务 | ③ | 现场演示需要"活的"页面时 |
| 盒子上跑 Jupyter | ③ | ❌ 不要，吃内存+安全风险 |

### 4.2 为什么推荐"拷到本地画"

工步层数据（算 SOH 主要用它）只有几千行，**几 MB 而已**，
`scp` 一秒钟就拷完了。然后在你自己的电脑上，想用什么工具都行：

```bash
# 从盒子拷到本地
scp <用户名>@<盒子IP>:/data/derived/channel=25-41-1-1/testid=71/steps.parquet .

# 本地用 DuckDB 直接查 Parquet，不用建库
python -c "
import duckdb
print(duckdb.sql('''
    SELECT cycleid, sum(cap) AS discharge_cap
    FROM \"steps.parquet\"
    WHERE steptype IN ('dc','dv','cccd','dp','dr')
    GROUP BY cycleid ORDER BY cycleid
''').df())
"
```

**明细数据**（几百万行、几十到几百 MB）也不用都拷到本地 ——
用 DuckDB **直接读盒子上的文件**（共享目录），或者只把算好的汇总拷回来。

### 4.3 画图工具怎么选

| 工具 | 输出 | 占盒子内存 | 适合 |
|---|---|---|---|
| **matplotlib** | 静态 PNG/SVG | — （在本地跑） | ✅ **Demo 报告首选** |
| **plotly** | 交互式 HTML 单文件 | — | 想给交互效果时 |
| **ECharts** | HTML + JS | 极低（只是拼字符串） | ✅ 让模型生成图表时 |
| Streamlit / Gradio | 动态网页 | ⚠️ 100～200MB | 需要"活的"看板时 |
| Jupyter | 交互式笔记本 | ⚠️ 几百 MB | ❌ 不要放在盒子上 |

**一个很划算的发现**：**ECharts / Plotly 生成的是 HTML，渲染在浏览器里，
不占盒子任何资源。** 所以"生成图表"这件事在盒子上非常轻
（只是拼一段配置字符串），真正的渲染在你电脑的浏览器里完成。

这对 16GB 的盒子特别友好，而且和"AI 原生"的定位很搭 ——
**让大模型生成图表配置** 是很自然的做法（模型输出 ECharts 的 JSON 配置，
前端渲染）。PPT 里那些图表就属于这一类。

### 4.4 画图时必须标注口径

这是你的字段字典在可视化上的延续。**每张图的图注都要写清楚口径**：

- "容量"图 → 图注注明：**放电容量，取每圈放电工步的 cap 累计值**
- 时间轴 → 注明用的是 `atime`（绝对时间）还是 `testtime`（工步内毫秒）
- 辅助通道 → 注明 `T1` 具体是什么

**为什么**：看图的人不知道这些。如果他以为"容量"是整圈总容量、
或者以为横轴是秒，他会得出错误结论 —— **而且图看起来是对的**，
这种错误最难发现。

---

## 五、D. 最终 Demo 的可视化形态

需求书对报告的要求是"**格式规范、可直接用于汇报演示**"。所以 Demo 的可视化是：

### 建议的形态：静态报告为主，动态看板为辅

| 形态 | 工作量 | 建议 |
|---|---|---|
| **静态报告**（HTML / docx / PDF）+ 内嵌图表 | 低 | ✅ **必需**，这是交付物 |
| 数据看板（Web 页面，实时刷新） | 中 | 可选，Demo 时加分 |
| 对话式（问 AI，它答 + 出图） | 高 | PPT 里的目标形态，但 3 周内风险大 |

**为什么静态报告为主**：需求书明确要"可直接用于汇报演示"，
而静态报告最可控 —— 不用担心演示时网络断了、服务挂了、模型卡了。

### 报告里该有的图

从数据能直接画出来的（都是 SOH 的标准图）：

| 图 | 横轴 | 纵轴 | 说明什么 |
|---|---|---|---|
| **容量保持率曲线** | 循环号 | 容量保持率 % | SOH 的核心图，最能说明电池寿命 |
| 充放电容量曲线 | 循环号 | 容量 Ah | 充/放电容量随循环的变化 |
| 库仑效率曲线 | 循环号 | CE % | 副反应情况，通常贴近 100% |
| 能量效率曲线 | 循环号 | EE % | 比 CE 更严格 |
| DCIR 曲线 | 循环号 | 内阻 mΩ | 老化趋势 |
| 单圈充放电曲线 | 容量 Ah | 电压 V | 展示一个典型循环的形态 |
| 温度趋势 | 时间 | 温度 ℃ | 来自辅助通道，要看硬件有没有 |

**做 Demo 时，前三张 + DCIR 就够了**，能说明"数据完整可用 + 指标算得出来"。

### 一条实用建议

**报告生成可以有两级**：

1. **数据层**（在盒子上）：采集 + 算指标 → 落成 Parquet
2. **呈现层**（在本地或任意机器）：读 Parquet → 画图 → 生成报告文档

这样分工的好处：**报告样式改了、要重出一版，不用碰盒子上的采集服务**，
在本地跑一遍就行（因为数据都在，算指标是秒级的）。

---

## 六、一句话总结

| 问题 | 答案 |
|---|---|
| 怎么看见盒子？ | **SSH**。不装桌面环境（吃内存、可能和加速卡驱动打架） |
| 怎么看见服务？ | systemd + **心跳 JSON 文件**（防"静默卡死"）+ 可选只读状态页 |
| 图表在哪画？ | **在你自己电脑上画**。数据拷回来，或用 DuckDB 直接读 |
| Demo 交什么？ | **静态报告 + 内嵌图表**。优先保证"能演示"，动态看板是加分项 |

**核心一句**：盒子的职责是"把数据采集好、落盘、提供出去"，
**可视化的活交给看的人那边的机器**。这样既不给 16GB 的盒子添负担，
也符合"数据在哪儿算、界面在哪儿看"的自然分工。
