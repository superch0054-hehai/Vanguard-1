# 智能体（QwenPaw）接入现状

记录日期：2026-09-25
盒子上跑着的东西：vLLM（模型）+ QwenPaw（Agent），都是 root 手动启动的容器/进程。

---

## 一、盒子上实际跑着什么

| 服务 | 端口 | 进程 | 启动方式 |
|---|---|---|---|
| **vLLM**（大模型） | 12345 | `root python3 -m vllm.entrypoints.openai.api_server` | root 手动（pts/2），9月17日 |
| **QwenPaw**（Agent） | 8088 | `root /app/venv/bin/qwenpaw app --host 0.0.0.0 --port 8088` | root 手动（pts/0），9月17日 |

**模型**：`Qwen3.8-27B-AWQ-INT4`（AWQ INT4 量化，`max_model_len=120000`，vLLM 0.23.0）
**注意**：这是个**推理模型** —— 输出里带 `reasoning` 字段（先思考再回答）。

> 需求书里写的"qwen3.8-27B"和"qwenpaw"**都不是笔误** —— 现场就是这个。
> （我早期判断它们是笔误，错了。当时拿"业界常见命名"去套现场，方法不对。）

---

## 二、数据怎么给 Agent

QwenPaw 跑在 **Docker 容器**里，它的工作目录在 docker volume 上（我们没权限）。
但容器**挂载了宿主机目录 `/home/admin/jerry`**（bind mount，两边同一路径）。

所以数据通道是：

```
我们的采集服务
   ↓ 落盘
~/batterylab/data/
   ↓ python3 make_share.py
/home/admin/jerry/batterylab-data/       ← 宿主机
   ↓ （bind mount，容器内同一路径）
/home/admin/jerry/batterylab-data/       ← 容器内，Agent 直接读
```

**共享目录内容**：
```
batterylab-data/
├── README.md      ★ 口径规则（6 条）+ 字段字典 + 示例代码
├── datasets.md    数据集索引
└── channel=<通道>/testid=<测试号>/
    ├── summary.md ★ 算好的 SOH 指标
    ├── steps.csv  工步层（小，可直接读）
    ├── detail.csv 明细（大，要代码筛）
    └── meta.json
```

**关键设计决定**：
- **共享目录里只放数据，不放能操作设备的代码** —— Agent 有 shell 权限，
  如果给它 `neware_client.py`，理论上它能对测试机发 `stop`。不给这个机会。
- **口径规则写在 README 里**，不指望它自己悟 —— 它自己用 pandas 重算会算出不一样的数。

---

## 三、已验证通过的（截至 2026-09-25）

| # | 验证项 | 结果 |
|---|---|---|
| 1 | Agent 能读 `/home/admin/jerry/` | ✅ 它能列出内容，还认出那是自己底座模型所在目录 |
| 2 | Agent 有 shell + Python 能力 | ✅ 它自述可跑 `execute_shell_command` 和写 Python |
| 3 | Agent 理解口径规则 | ✅ 复述 README 第一节，**六条一条不漏**，还额外抓到 `testid` 不唯一、辅助通道、`upload_complete` |
| 4 | Agent 读索引正确 | ✅ `datasets.md` 表格复述无误 |
| 5 | Agent 读摘要正确 | ✅ SOH 指标一个数没串 |
| 6 | Agent 能自己算（口径对） | ✅ 它算出 57.06%，方法正确（discharge/charge） |
| 7 | 安全机制有效 | ✅ `/dev/tcp` 内网探测被 `detection_rules` 判为 CRITICAL 拦截，且它声明不会绕过 |

**结论：技术闭环成立 —— Agent 能读我们的数据、理解我们的口径、算出正确结果。**

---

## 四、已知问题：复杂任务会超时

**现象**：
```
MODEL_TIMEOUT
Model 'Qwen3.8-27B-AWQ-INT4' timed out.
Reason: LLM stream ... produced no content for 30s.
Set QWENPAW_LLM_STREAM_IDLE_TIMEOUT to adjust this timeout
```

**根因**（实测确认）：

这个模型是**推理模型**，输出流长这样：
```
[第 1 个 chunk]  delta.content   = ""            ← 空
[后续 N 个 chunk] delta.reasoning = "..."        ← 全是思考内容
[思考完之后…]     delta.content   = "..."        ← 才轮到正式回答
```

**QwenPaw 等的是 `delta.content`** —— 思考阶段不产生 content，超过 30 秒就判定超时。

**实测数据**：

| 任务复杂度 | 耗时 | 是否超时 |
|---|---|---|
| 读文件 + 整理成表格 | 短 | ✅ 成功 |
| 读 README + 复述要点 | 短 | ✅ 成功 |
| 读 summary + 跑代码重算对比 | **46 秒** | ❌ 超时 |

**验证过的解法**（在宿主机直接 curl vLLM 测的）：

| 写法 | 耗时 | 有没有思考 |
|---|---|---|
| 基线（带思考） | 34.8 s | 有 |
| **`chat_template_kwargs: {"enable_thinking": false}`** | **0.87 s** | **无** ✅ |
| 顶层 `enable_thinking: false` | 34.8 s | 有 ❌ vLLM 不认 |
| `extra_body: {"enable_thinking": false}` | 34.7 s | 有 ❌ 无效 |

**只有 `chat_template_kwargs` 这一种写法有效。**

> ⚠️ **更正（2026-09-25 晚深入实测）**：上面那句原本写的是"效果差 40 倍"。
> 那个倍数**只对短回答成立**。长回答的耗时由 token 总量决定，关不关思考几乎没差别。
> **关掉思考真正的作用不是"更快"，而是"有输出"** —— 详见 §七。

> ⚠️ **更正（2026-09-25 深夜）**：这里原本写「QwenPaw 的 Example 是误导的」—— **这个判断是错的，已作废。**
> 正确的理解见 §八：**`extra_body` 这个"外壳"是必须的**，QwenPaw 的示例在形式上是对的；
> 只是里面装什么，要按 vLLM 的要求写成 `{"chat_template_kwargs": {"enable_thinking": false}}`，
> 而不是裸的 `{"enable_thinking": false}`。
> 我当初拿"直接 curl 打 vLLM"的结论去下"QwenPaw 该怎么填"的结论，**把两个层次搞混了**。

---

## 五、已解决：2026-09-25 通过 QwenPaw 自己的接口落地（**不需要重启、不需要 jerry 配合**）

**突破口**：QwenPaw 的控制台本身是个 HTTP 服务（8088），它把所有配置都暴露成了 API。
浏览器 F12 能看到，接口在 `/api/` 前缀下。

**关键发现**：`GET /api/envs/catalog` 列出了它**支持的全部环境变量**，而且带一个 `mutability` 字段。
那两个超时变量的值是 **`hot_runtime`** —— **改完立刻生效，不用重启容器**。
（原来判断"要重启容器"是错的，因为当时不知道有这个接口。）

实际执行的两步：

| # | 做了什么 | 接口 | 结果 |
|---|---|---|---|
| 1 | 超时 30 → **300 秒** | `PATCH /api/envs`，body `{"QWENPAW_LLM_STREAM_FIRST_CONTENT_TIMEOUT":"300","QWENPAW_LLM_STREAM_IDLE_TIMEOUT":"300"}` | ✅ 回读 `source=user`，已生效 |
| 2 | 关掉思考 + 限输出长度 | `PUT /api/models/openai-custom/models/Qwen3.8-27B-AWQ-INT4/config`，body 见下 | ✅ 回读 `config_overrides=["generate_kwargs"]` |

第 2 步的完整请求体（★ 注意 `chat_template_kwargs` 必须包在 `extra_body` 里，理由见 §八）：

```json
{
  "generate_kwargs": {
    "extra_body": {"chat_template_kwargs": {"enable_thinking": false}},
    "max_tokens": 2500
  },
  "relay_reasoning": true,
  "thinking_enabled": null,
  "thinking_budget": null,
  "reasoning_effort": null
}
```

**两个必须知道的点**：

1. **实际在用的 provider 是 `openai-custom`，不是 `qwenpaw-local`。**
   从 `GET /api/agents` 的 `active_model` 才看出来：
   ```json
   "active_model": {"provider_id": "openai-custom", "model": "Qwen3.8-27B-AWQ-INT4"}
   ```
   改错 provider 会白改。

2. **`generate_kwargs` 里的键是作为「关键字参数」传给 OpenAI SDK 的，不是直接当请求体。**
   QwenPaw 界面提示原文是「会被展开传入到生成请求（`openai.chat.completions` 或 `anthropic.messages`）中」——
   **关键词是"展开传入到 SDK 调用"**，所以必须是 **SDK 认识的参数名**。
   想给请求体塞自定义字段，得用 SDK 的 `extra_body`（它会解开合并进请求体）。
   写错的后果不是"静默失效"，而是直接抛 `TypeError` 把 Agent 打挂 —— 详见 §八。

**回滚方法**（万一要退回去）：

```bash
# 超时恢复默认
curl -X POST http://127.0.0.1:8088/api/envs/QWENPAW_LLM_STREAM_FIRST_CONTENT_TIMEOUT/reset
curl -X POST http://127.0.0.1:8088/api/envs/QWENPAW_LLM_STREAM_IDLE_TIMEOUT/reset

# 生成参数恢复为空对象
curl -X PUT http://127.0.0.1:8088/api/models/openai-custom/models/Qwen3.8-27B-AWQ-INT4/config \
  -H 'Content-Type: application/json' \
  -d '{"generate_kwargs":{},"relay_reasoning":true,"thinking_enabled":null,"thinking_budget":null,"reasoning_effort":null}'
```

> ⚠️ 这两个改动在**共享的** QwenPaw 上，jerry 也用它。改动本身可逆、也不改变模型权重，
> 但**应该告诉 jerry 一声**。

---

## 六、这个问题的实际影响

| 场景 | 受影响吗 |
|---|---|
| Agent 读摘要、回答"这颗电池怎么样" | ❌ 不受影响（简单任务思考短） |
| Agent 整理指标、生成表格 | ❌ 不受影响 |
| **Agent 做跨数据集比较、归因分析** | ⚠️ **会受影响** |
| **Agent 现场跑代码算新指标** | ⚠️ **会受影响** |

**原文这里写的是"所以 Demo 的最小闭环不受影响"—— 这个判断过于乐观，已作废。**

实测（§7.2）表明：复杂任务不是"慢"，而是**返回空回答**。而 FR-03 要求的就是
"对核心实验数据进行分析处理、自动生成 SOH 分析报告" —— 正属于会受影响的那一类。
**所以这是会打到 Demo 核心的问题，不是边角问题。** 现已在 §五 修复。

**另一个缓解思路**：把摘要做"厚"一点 —— 我们预先算好更多指标写进 `summary.md`，
Agent 就只需要读结论，不需要现场算。**这也更可靠**（口径由我们控制）。

> **事后看，这条建议是对的，而且比当时想的更重要** —— 见 §7.3。

---

## 七、深入实测后的更正与真实性能数字（2026-09-25 晚）

### 7.1 更正：关掉思考**不是**为了更快

前面写"效果差 40 倍"。**那只对短回答成立。**

同一道复杂题（20 圈循环数据做衰减归因分析，`max_tokens=3000`）的对照：

| | 保留思考 | 关闭思考 |
|---|---|---|
| 耗时 | **2 分 57.9 秒** | **2 分 57.7 秒** |
| 思考段 | 6112 字符 | 0 |
| **回答段** | **0 字符** ❌ | **4865 字符** ✅ |
| `finish_reason` | `length` | `length` |

**耗时几乎一样。** 关掉思考的真正价值是：**把 token 预算从"看不见的思考"挪给"看得见的回答"。**

### 7.2 真正的根因：输出预算被思考吃掉了

复杂任务时，模型把**全部 `max_tokens` 都花在思考上**，`finish_reason` 变成 `length`（被截断），
`content` 字段**是空的**。

这意味着 **光调大超时没用** —— 就算不超时，Agent 收到的也是一个空回答。必须**同时**做两件事：

1. **调大超时** —— 否则调用在思考阶段就被打断
2. **关掉思考** —— 否则预算被思考吃掉、返回空

### 7.3 这台机器的真实吞吐：**17.0 tokens/s**

两次独立测量一致（3000 tokens ÷ 177.9 s = 16.9；800 tokens ÷ 47.2 s = 17.0）。

**所以报告长度可以直接估算耗时**：

| 模型输出长度 | 大约耗时 |
|---|---|
| 500 tokens | ~30 秒 |
| 1000 tokens | ~60 秒 |
| 2500 tokens（当前设的上限） | ~150 秒 |

**这条对 Demo 设计影响很大：报告不能长。**
这正好印证了上面"厚摘要、薄推理"的原则 —— 让模型只做简短表述，不做长篇生成。

### 7.4 关闭思考后的输出质量：没问题

同一道复杂题，关闭思考后模型产出的分析报告包含：
- 计算逻辑说明
- 一张 Markdown 表格（循环 / 放电容量 / 容量保持率 / 单圈容量损失 / 单圈衰减率）
- **数字全部正确**（98.2→100.0%、97.5→99.3%、损失 0.7、衰减 0.71%）

**结论：关掉思考不会让分析变水。** 该算的都算对了，只是不再"自言自语"。

### 7.5 还没做的：端到端验证

已完成：
- ✅ 配置写入并回读确认（两个超时 + `generate_kwargs`）
- ✅ 底层机制验证（直接调 vLLM，关闭思考后复杂任务能出完整回答，且数字正确）

**还没做**：从 QwenPaw 界面发一条复杂消息，确认整条链路通。
原因是它的对话接口 `POST /api/console/chat` 请求体是混淆的，试了 4 种格式都没提交成功 ——
继续逆向的性价比太低，停在这里。

**验证方法（1 分钟，需要人操作界面）**：
打开 http://10.201.9.180:8088 ，问一个需要推理的问题，
比如「读一下 channel=27-188-10-1/testid=247 的 summary.md，分析衰减主导机制」。

- **之前**：这种问题必报 `MODEL_TIMEOUT`
- **现在**：应该在 1~3 分钟内正常给出回答

如果仍然超时，说明改动没生效，回去查 §五 的两个接口是否还是那个值。

---

## 八、★ 层次陷阱：为什么 `extra_body` 是必须的（已实测证明）

### 8.1 我踩的坑

我先按 `{"chat_template_kwargs": {...}}` 写进 `generate_kwargs`，结果 Agent 直接报：

```
MODEL_EXECUTION_ERROR
Model 'Qwen3.8-27B-AWQ-INT4' execution failed.
Reason: AsyncCompletions.create() got an unexpected keyword argument 'chat_template_kwargs'
```

**原因**：QwenPaw 把 `generate_kwargs` 里的键**当作关键字参数**传给 OpenAI SDK 的
`AsyncCompletions.create()`。SDK 的参数表是固定的，`chat_template_kwargs` 不在里面 → Python 直接抛 `TypeError`。

### 8.2 关键：同一个词，在两个层次含义完全不同

| 层次 | `extra_body` 的行为 | `chat_template_kwargs` 的行为 |
|---|---|---|
| **直接 curl 打 vLLM** | 就是个普通字段，vLLM 不认识 ❌ | vLLM 要求放顶层 ✅ |
| **经 QwenPaw → OpenAI SDK** | **SDK 的正式参数，会把内容解开合并进请求体顶层** ✅ | SDK 不认识这个参数名 ❌ |

**我最初用 curl 测出「`extra_body` 无效」，然后拿这个结论去指导 QwenPaw 的配置 —— 层次错了。**
QwenPaw 自己给的 Example 用 `extra_body` 是**对的**，错的只是我当时否定了它。

### 8.3 正确写法

```json
{"extra_body": {"chat_template_kwargs": {"enable_thinking": false}}}
```

SDK 会把它变成请求体顶层的 `chat_template_kwargs` —— 正是 vLLM 要的。

### 8.4 证明过程

用本地装的 `openai` SDK（v2.15.0）直连盒子上的 vLLM，**用和 QwenPaw 同一个 `AsyncCompletions` 类**：

| 实验 | 结果 |
|---|---|
| **A.** `chat_template_kwargs` 直接当参数 | ❌ **一字不差复现了 Agent 报的错**：`AsyncCompletions.create() got an unexpected keyword argument 'chat_template_kwargs'` |
| **B.** 包在 `extra_body` 里 | ✅ 成功 |

B 与对照组的对比（同一个问题 `Reply with exactly: OK`）：

| | 思考关（`extra_body` 写法） | 思考开（不带任何参数） |
|---|---|---|
| `completion_tokens` | **2** | **28** |
| `reasoning` | **（无）** | **88 字符** |
| `content` | `'OK'` | `'\n\nOK'` |

**思考确实是关掉的。** A 复现错误 + B 与对照组对比，两头都闭合了。

### 8.5 一个没用的验证途径（记下来，免得以后又试）

`POST /api/models/{provider}/models/test` 看起来正好能验证配置，**但它不带 `generate_kwargs`**。

我做了受控实验：故意填一个非法参数 `definitely_not_a_real_kwarg_xyz`，它照样返回
`{"success": true, "message": "Model connection successful"}`。

**所以这个接口返回 success 不能用来证明配置有效** —— 我差点把它当成验证通过了。
