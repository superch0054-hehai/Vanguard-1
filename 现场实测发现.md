# 现场实测发现：文档说的 vs 实际是这样的

这些**全部是在真机上实测出来的**，和协议文档（v1.19，2023 年）不一致。
现场客户端是 **BTS Client 2025.11.24**，比文档新两年 —— **以实测为准。**

整理这份是因为：**每一条都是会让人做出错误分析的坑。**

---

## 一、通讯层

### 1.1 回应尾部多一个 `#\r\n`

**文档**：只说发送要加结束标识，没说回应里有。

**实测**：客户端在 `</bts>` 之后还追加 `#\r\n`：

```
  </middle>
</bts>
                        ← 空行
#
```

**坑**：如果不截掉，XML 解析报 `junk after document element`，
**每一条命令都会失败**。

**已处理**：`neware_client._one_frame()` 只截出恰好一帧。

### 1.2 回应结构比文档丰富

**文档**：`<middle><channel .../></middle>`

**实测**：多了 `<client>` 元素和 `<server>` 下的 `<dev>/<xwj>` 层级：

```xml
<client version="BTS Client 2025.11.24(R3) (64)" />
<serverip count="1">
  <server ip="127.0.0.1" port="3306" version="8.0.4.546" devCount="6">
    <dev ip="192.168.1.188" devid="188" zwjVersion="..." xwjCount="10">
      <xwj id="1" xwjVersion="..." xwjGuid="..." />
    </dev>
  </server>
</serverip>
<middle count="488"> ... </middle>
```

文档描述的 `<middle>/<channel>` **仍然存在**，所以两套都解析。

**顺带**：`<client version>` 这个字段要记进元数据（我们做到了）。

---

## 二、单位（最容易出错的一类）

| 字段 | 文档说 | **实测** | 证据 |
|---|---|---|---|
| 容量 `cap` | Ah | **mAh** | 原始值 ×1000 = BTSDA 显示值 |
| 能量 `eng` | Wh | **mWh** | 同上 |
| 电流 `curr` | A | **mA** | BTSDA `unit` 表 + 工步方案交叉验证 |
| 电压 `volt` | V | V ✅ | 逐位一致 |
| 内阻 `dcir` | mΩ | mΩ ✅ | 反推验证，误差 <0.31% |

**单位来源**：BTSDA 导出的 `unit` 工作表里有一行"单位方案清单"，
厂商自己写明了。**比协议文档可靠。**

**影响**：
- 绝对值类（容量、能量、电流）**标错就差 1000 倍**
- **比率类（效率、保持率）单位约掉了，不受影响** —— 所以我们算的
  库仑效率 13.61% 和 BTSDA 的 13.61% 完全一致

---

## 三、符号约定

**充电为正、放电为负。**

**文档**：只说了单位，没说符号。

**实测**（按工步类型分组统计明细）：

```
恒流充电    +0.014181 ~ +0.142093 mA     ← 正
恒流放电    -0.142105 ~ -0.014187 mA     ← 负
搁置         0
```

---

## 四、两个 `cycleid` 不一样（差 1）

同一个测试（通道 `27-188-10-1`、测试 247）：

| 来源 | 第 1 圈的 cycleid |
|---|---|
| **工步层**（`downloadStepLayer`） | **1**（与 BTSDA 的"循环号"一致 ✅） |
| **明细**（`download`） | **0** |

**其余字段完全对得上** —— 已逐段核对：

```
工步1: seqid 1–781     明细里 stepid=['1']   ✅
工步2: seqid 782–798   明细里 stepid=['2']   ✅
工步3: seqid 799–1241  明细里 stepid=['3']   ✅
```

**只有 cycleid 的起点不同。**

**文档**：v1.19 的 `download` 示例写的是 `cycleid="1"` —— 又错了。
（API 文档的实测截图里是 `cycleid="0"`，与现场一致。）

**影响**：
- 算 SOH 用**工步层** → 不受影响 ✅
- 按**明细**做逐圈分析 → 会整体偏移一圈，用之前要 `+1`

---

## 五、`testid` 会被多个通道共用 ★

**实测**（同一时刻查三个通道）：

| 通道 | 条码 | cycle | 状态 | **testid** | 已上传 |
|---|---|---|---|---|---|
| `27-188-10-1` | `D:测试数据黄坤1` | 1 | finish | **247** | 1241 |
| `27-188-10-2` | `D672035TAA112` | 294 | working | **244** | 59253 |
| `27-188-10-3` | `D672035TAA113` | 309 | working | **244** | 23039 |

**10-2 和 10-3 共享 testid 244** —— 因为它们是**同一次批量 `start`** 启起来的，
客户端给这一批分配了同一个测试号。

**这是个重要的数据模型结论**：

```
❌ 不能用 testid 唯一标识一份数据
✅ 必须用 (通道, testid)
```

**条码则是 per-channel 的**（112 / 113 不同）—— 所以**条码能区分电池，testid 不能区分通道**。

我们的采集服务用的键正是 `(通道, testid)`，这个设计是对的。

---

## 六、条码只在有限窗口内有效

**`inquire` 只能拿到通道"当前/最近一次"测试的条码。**

- 测试进行中：能拿到 ✓
- 测试刚结束、下一个测试还没开始：**仍然能拿到** ✓（实测：10-1 是 `finish`，
  `inquire` 依然返回它的条码）
- **通道被下一次测试复用后：旧条码永久丢失** ✗

**所以采集服务必须在"测试结束后、下次测试开始前"这个窗口内抓条码** ——
我们在测试**进行中**就把 `(通道, testid)` 记下来，结束时校验一致才采。

---

## 七、其他

| 发现 | 说明 |
|---|---|
| **`LogCode.csv` 缺失** | `log_code=102000` 这类码无法解读。厂商文档引用了它但没随附 |
| **辅助通道只有 `CPU`** | 是**测试设备的 CPU 温度**（℃），**没有电池温度通道** |
| **`inquiredf` 的"完整"会变** | 测试进行中是 `false`，跑完才是 `true`。没传完就拉会缺尾部 |
| **设备数量会变** | 通道数从 496 变成 488（有人动了设备，拔了某个单元）|

---

## 八、给后来人的三条忠告

1. **协议文档只能当参考，不能当依据。** 它比现场客户端旧两年，
   上面这些差异每一条都会让分析出错。
2. **任何字段的口径都要用真实数据验证一次** —— 尤其是单位。
   最有效的办法是拿 BTSDA 官方导出对账（见《对账报告-BTSDA.md》）。
3. **改完数据相关的代码，一定跑一次 `validate.py`** ——
   它专门查这类结构、单位、符号的自洽性（9 项检查）。
