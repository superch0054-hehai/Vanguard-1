# 容器化部署方案（模型 / 智能体 / 采集程序）

## 一、要求

来自需求方的部署要求：

1. **模型**：由对方提供**模型镜像**
2. **智能体**：**最好也以容器运行**

由此推出来的整体形态：TY1200 上的服务全部容器化。

---

## 二、三个容器的关系

```
TY1200（Debian 12，admin）
│
├── Docker
│   │
│   ├── 【模型容器】      对方提供的镜像
│   │
│   ├── 【智能体容器】    Agent，最好用官方 openclaw 镜像
│   │
│   └── 【采集容器】      ★ 我们的活 —— 连 ② 拿数据
│
└── 宿主机上的持久化目录（数据落这里）
```

**网络关系**：三者的连通方式是要先定下来的第一件事（见第四节）。

**关键的不对称**：**只有采集程序需要访问 ②**（`10.201.47.169:502`）。
模型和 Agent 都不需要碰测试机。所以权限可以收紧：

| 容器 | 需要访问 ② 吗 | 需要访问别的容器吗 |
|---|---|---|
| 模型容器 | ❌ 不需要 | 被 Agent 访问 |
| 智能体容器 | ❌ 不需要 | 访问采集程序、模型 |
| **采集容器** | ✅ **必须** | 被 Agent 访问 |

---

## 三、我们的采集程序要不要也容器化

**建议：要。** 理由：

1. **和整体部署方式一致** —— 别人都用容器，我们单独跑宿主机会显得突兀，
   而且部署步骤会变成"两套做法"，交接时容易出岔子
2. **环境完全可控** —— 镜像里固定了 Python 版本和依赖，
   **彻底绕开"宿主机 Python 是 3.11 还是 3.13"和"能不能 pip 装包"这两个坑**
3. **不污染宿主机** —— 这台盒子是共用的，跑着别人的大模型
4. **镜像本身就是交付物** —— 对方拿到镜像就能跑，比"给一堆代码 + 部署文档"更省事

**代价（要认）**：

| 代价 | 怎么处理 |
|---|---|
| **数据必须挂到宿主机**，否则容器重建就丢 | 用 `-v` 把数据目录挂出来，**这是最容易出事的地方** |
| 调试稍麻烦 | `docker exec -it <容器> bash` 进去，或者开发期在宿主机 venv 跑、稳定后再打镜像 |
| 常驻服务不能用 systemd | 用容器的重启策略（`--restart unless-stopped`）代替 |
| **时区默认是 UTC** | 容器要设 `TZ=Asia/Shanghai`，否则日志和文件命名的时间会差 8 小时 |
| 需要一个 Dockerfile | 这个我来写 |

**我们的采集程序容器化没有技术障碍** —— 它是纯 Python、**不碰 GPGPU、
不需要 `--privileged`**、不访问 USB/串口（数据走 TCP）。所以镜像可以做得又小又简单。

---

## 四、必须先定的三件事

### 4.1 容器之间怎么互访

智能体要拿到电池数据，就得能调到采集程序。三种做法：

| 做法 | 怎么做 | 优 | 劣 |
|---|---|---|---|
| **A. 同一个自定义网络** | 建一个 docker network，三个容器都接进去，用**容器名**互相访问 | ★ **推荐**，标准做法，不依赖宿主机网络配置 | 要多一步 `docker network create` |
| B. 都用 `--net=host` | 三个容器都跟宿主机共用网络，用 `127.0.0.1` 互通 | 配置最简单 | 端口容易冲突；隔离性差 |
| C. 只共享数据目录 | 采集程序把数据落到挂载卷，Agent 直接读文件 | 完全解耦，最稳 | Agent 无法"触发采集"或查实时状态 |

**倾向 A。** 但如果模型容器的镜像是对方提供的、启动参数也是他们定的，
就要看他们用的是 `--net=host` 还是 bridge —— **这个必须问清楚**，
否则我们的容器可能连不上他们。

### 4.2 智能体怎么调用采集能力

这决定了我们的采集程序长什么形态：

| 形态 | 说明 | 适合 |
|---|---|---|
| **HTTP 服务** | 采集程序常驻，暴露几个只读接口（查状态、取数据、触发采集） | ★ 通用，Agent 用 HTTP 工具就能调 |
| **MCP server** | 按 MCP 协议暴露成工具，Agent 直接当工具用 | 如果 Agent 框架支持 MCP，最"原生" |
| 纯文件 | 只落盘，Agent 读文件 | Demo 最省事，但没有实时性 |
| 库 | 采集程序做成 Python 包，被 Agent 直接 import | 只有在同一个容器里才可行 |

**⚠️ 这个必须问清楚**：Agent 是 OpenClaw 还是别的？它怎么调用外部工具？
（需求书里写的是"Qwen Agent 框架 qwenpaw"，但官方镜像列表里是 `openclaw`，
这个不一致我早期就标注过。）

### 4.3 数据落在宿主机哪个目录

**这是容器化最容易出事的地方。** 采集程序的产出就是数据，如果只写在容器里，
容器一重建数据就没了 —— 而电池实验数据**无法补录**。

建议约定：

```
宿主机：/data/batterylab/
  ├── raw/           原始 NDA 文件（永久保留）
  ├── normalized/    解析后的时序（Parquet）
  ├── derived/       汇总指标（算 SOH 用）
  ├── manifest.db    索引：哪份数据对应哪颗电池
  └── logs/          采集日志
```

容器启动时把这个目录挂进去：`-v /data/batterylab:/data`

---

## 五、需要问清楚的五个问题

| # | 问题 | 找谁 | 为什么必须先知道 |
|---|---|---|---|
| 1 | **模型镜像从哪来？镜像名是什么？** | 需求方 / 部署模型的人 | 决定整个部署形态，也影响我们镜像的基线 |
| 2 | **容器之间怎么互访？`--net=host` 还是 bridge？** | 同上 | 决定采集程序要不要暴露端口、以及怎么暴露 |
| 3 | **Agent 用什么框架？怎么调用外部工具？** | 同上 | 决定采集程序的接口形态（HTTP/MCP/文件） |
| 4 | **数据目录定在哪？谁保证不被清掉？** | 同上 | 关系到数据会不会丢 |
| 5 | **采集程序的镜像谁来构建、放到哪？** | 同上 | 天数私有仓库我们大概没有推送权限，可能只能本地构建 |

**第 5 个问题有个现实考虑**：天数的镜像仓库（`harbor.iluvatar.com.cn:10443`）
是他们的私有仓库，我们未必能推镜像进去。**所以很可能是在盒子上本地构建** ——
那采集程序的镜像应该基于什么？两个选择：

- 基于盒子上已有的天数基础镜像（如果它有 Python）
- 基于一个公开的小镜像（如 `python:3.11-slim`），但要确认盒子能不能拉到公网镜像

**如果盒子不能上外网、也不能拉天数镜像**，那"本地构建"也就不可行，
只能退回宿主机 venv。**这一条也要问。**

---

## 六、我现在能做的

**可以先做、且不依赖上面答案的**：

1. **写 Dockerfile 草稿** —— 基于 `python:3.11-slim`，装 pandas/pyarrow/duckdb，
   设定时区，把代码 COPY 进去，用非 root 用户跑。
   拿到答案后改基线镜像就行
2. **把采集程序整理成"适合容器化"的样子** —— 环境变量/配置文件驱动的参数
   （不要命令行硬编码）、日志走 stdout（容器标准做法）、数据路径可配置
3. **准备 `docker-compose.yml` 草稿** —— 把网络、卷、重启策略、时区都写进去，
   比一长串 `docker run` 好维护得多

**这三样等网络和接口形式定了，改起来很快。**

---

## 七、一句话总结

要求我理解了：**模型和 Agent 容器化，我们的采集程序也应该跟着容器化**。

**容器化对我们是好事**（环境可控、不污染宿主机、镜像即交付物），
**代价主要在数据持久化** —— 必须挂到宿主机，否则数据会丢，
而电池测试数据是无法补录的。

**现在最要紧的是问清四件事**：镜像从哪来、容器怎么互访、Agent 怎么调工具、
数据目录定在哪。这四个定了，剩下的就是写 Dockerfile 和 compose 文件的事。
