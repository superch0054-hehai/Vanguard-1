# 测试机控制与数据采集是什么意思

这是你的职责名称。这份文档把它拆成"每天具体在做什么"。

---

## 一句话版本

| | 一句话 | 类比 |
|---|---|---|
| **测试机控制** | 用程序代替人工去**操作**电池测试柜 | 用遥控器代替走到电视机前按按钮 |
| **数据采集** | 把测试产生的数据**自动收回来、存好** | 自动把收银机的小票收起来归档，而不是等人一张张去撕 |

**合起来是一个闭环的两半**：控制让数据产生，采集把数据拿走。

---

## 一、测试机控制：具体是哪些动作

现在研究人员是怎么做实验的？在 BTS Client 界面上点鼠标：
点"启动测试"选工步文件和通道 → 盯着看跑得怎么样 → 跑完了点停止 → 手抄结果。

**控制就是把这套点鼠标的动作交给程序做。** 具体包括：

| 动作 | 对应命令 | 什么时候用 |
|---|---|---|
| **启动测试** | `start` | 下发工步文件 + 电池条码 + 备注，可一次启动多个通道 |
| **停止测试** | `stop` | 正常停止 |
| **紧急全停** | `broadcaststop` | 一台设备下所有通道一起停 |
| **看现在的状态** | `getchlstatus` | 在跑吗？跑完没？出保护了吗？ |
| **看现在读数** | `inquire` | 当前电压电流、跑到第几圈 |
| **暂停后继续** | `continue` | 暂停过的测试接上 |
| **跳到指定工步** | `goto` | 跳过或重跑某一步 |
| **重新下发工步** | `reset` | 换一套工步重跑 |
| **预约暂停** | `setpause` / `cancelpause` / `getpause` | 定时暂停 |
| **通道通断** | `chl_ctrl` | IGBT 设备的通道开关（需硬件支持） |
| **点灯** | `light` | 点亮通道指示灯，在现场找到对应的物理通道 |
| **报警复位** | `resetalarm` | 声光报警复位 |
| **清标记** | `clearflag` | 清除通道标记 |
| **并联** | `parallel` | 多个通道并联使用 |

### 为什么需要"控制"

1. **无人值守**。一个循环测试可能跑几天几夜，人不可能一直盯着。程序可以自动等它结束、自动开始下一个。
2. **和其他设备联动**。协议文档 1.4 节自己举的例子：测试到某个阶段要通知恒温箱调温，等温度到了再继续下一步 —— 这种人机协作的时序，人工做很容易错。
3. **批量操作**。16 个通道跑同一套工步，人工要点 16 次，程序一行就行。
4. **和业务流程打通**。将来 Agent 说"给这批电池开始测试"，就能自动开始，不用人去点。

---

## 二、数据采集：具体是哪些动作

测试柜在不停地产生数据 —— 每隔一小段时间记一条："第几圈、第几步、电压多少、电流多少、已经充进去多少电"。一次测试下来几万到几百万条。

**采集就是把它们自动收回来、整理好、存起来。** 具体包括：

| 动作 | 对应命令 | 说明 |
|---|---|---|
| **拉历史明细** | `download` | 一条条的时间序列，最主要的数据源 |
| **拉工步层汇总** | `downloadStepLayer` | 每个工步一行的汇总，**自带内阻 DCIR**，算 SOH 最省事 |
| **拉实时读数** | `inquire` | 当前值，用来做进度监控 |
| **拉日志** | `downloadlog` | 异常事件记录 |
| **确认数据传完了** | `inquiredf` | ★ 没传完就拉会缺数据，这是厂商点名的坑 |
| **看有哪些通道** | `getdevinfo` | 建立通道清单 |

采集完了还要做这几件事（这些才是"采集"真正的工作量）：

| 要做的事 | 为什么 |
|---|---|
| **落成文件**（Parquet / CSV） | 留在内存里没意义，要能长期保存和检索 |
| **记录元数据** | 这份数据从哪来、什么时候采的、用什么版本解析的 —— 将来能追溯、能重建 |
| **关联到电池** | 用条码把数据绑定到"哪颗电池、哪次实验"。**没有这一步，数据就是一堆没有归属的数字** |
| **校验完整性** | 时间戳是否单调、行数对不对、首末电压是否合理 |

### 为什么需要"采集"

1. **数据量太大**，人工不可能抄。
2. **人工导出会出错、会漏、会命名混乱**。今天叫 `1.xlsx`，明天叫 `测试数据-最终版.xlsx`，一个月后没人知道哪个是哪个。
3. **报告需要自动生成**，数据必须先变成结构化的形式，程序才能算。
4. **原始数据无法追溯补录** —— 当时没采到，就永远没有了。

---

## 三、一个关键区分：读操作 vs 写操作

这是这两块最重要的差别，也解释了为什么"控制"要更小心。

| | 数据采集 | 测试机控制 |
|---|---|---|
| 性质 | **读**（只查，不改设备状态） | **写**（会改变设备状态） |
| 出错了会怎样 | 最多这次数据没采到，重采就行 | **可能把正在跑的测试搞坏** |
| 能不能反复试 | 能，随便试 | **不能**，必须确认空闲通道 |
| 权限要求 | 低 | 高 |
| 节奏 | 持续的、规律的（定时轮询） | 事件驱动的、偶尔的 |
| 代码要怎么写 | 出错重试即可 | **必须先查状态再动作，要有确认步骤** |

**这条区别很实际**：我上一轮强调的那条安全红线 —— "绝对不要在正在跑测试的通道上做实验" ——
就是因为控制是写操作。一个 `stop` 发错通道，别人几天几夜的实验数据就没了，而且无法补录。

**所以日常写代码时**：
- 采集脚本可以大胆测试、反复跑
- 控制相关的代码，每一次动作前都应该先 `getchlstatus` 确认状态，
  而且第一次验证必须在空闲通道上做

---

## 四、在你这个项目里对应什么

需求分析书里的功能需求：

| 需求 | 内容 | 对应你的哪一半 |
|---|---|---|
| FR-01 | 边缘计算环境部署 | 不是你的（部署大模型） |
| **FR-02** | **实验数据读取** | **数据采集** |
| FR-03 | SOH 分析报告生成 | 交接点（见下） |

**FR-02 就是数据采集。** 控制没有单独列为一条需求，
但它是让"读取"能自动发生的手段 —— 而且顾问明确说了控制是你的职责。

### 整个系统的分层

```
④ 报告生成 / Agent / 大模型            ← 别人负责
        ↑
③ 算指标（容量保持率、库仑效率、DCIR…）  ← 交接点，要确认归属
        ↑
② 数据采集（把数据拿到手、存好、关联好）  ← 你负责
        ↑
① 测试机控制（让测试跑起来）            ← 你负责
        ↑
   电池柜 / BTS Client（② Windows 主机）
```

**你是最底层的地基。** 上面算指标、出报告、大模型分析，全部依赖你这一步的数据。
你这个环节的数据不对，上面全部白做 —— 这也是为什么我把"和 BTSDA 对账建金标准数据集"
放在了第一阶段。

### ⚠️ 有个边界要跟王涛老师确认

**"算指标"算不算你的活？** 两种可能，范围差很多：

| 划分方式 | 你的范围 | 后面谁做 |
|---|---|---|
| **窄** | 只负责把原始数据完整、正确地拿到并落盘 | 别人算 SOH 指标、出报告 |
| **宽** | 拿到数据 + 算出 SOH 指标 + 存成报告能用的形式 | 别人只管排版和 Agent 编排 |

建议早点问清楚。如果按"宽"的做，你要多看 SOH 指标的算法口径；
如果按"窄"的做，你的重点就是**数据的完整性和口径正确**。

---

## 五、一天的画面（有画面感一点）

**早上到实验室：**

1. **控制侧**：查一下昨晚的测试跑完没有 —— `status` 看所有通道状态
2. **采集侧**：发现 3、5、7 号通道跑完了 —— 先 `inquiredf` 确认数据传完
3. **采集侧**：拉工步层汇总（算指标用）+ 完整明细（归档用），落成文件
4. **采集侧**：用条码把数据关联到具体电池，记下元数据
5. **控制侧**：看看今天要跑什么 —— 挑空闲通道，`start` 启动新测试
   （带上条码、备注、自动备份）
6. **控制侧**：`light` 点灯确认通道对不对，`rt` 看实时读数确认真的跑起来了

**之后就是循环**：定时轮询状态 → 有测试结束就采集 → 有新任务就启动。

---

## 六、现在最该记住的三件事

1. **控制是"写"、采集是"读"。** 写操作有风险，永远不在正在跑测试的通道上做实验。
2. **采集的核心不是"把数据拉下来"，而是"拉全、拉对、能关联到电池"。**
   数据没拉全（没等上传完）、口径搞错（容量是每步还是整圈）、
   关联不上电池（没有条码对应表）—— 这三种情况的数据是废的。
3. **你这一层是地基。** 上面所有分析都建立在你这里的数据上。
   所以第一阶段最该投入的是：**把数据拉全、和官方软件对账确认口径正确。**
