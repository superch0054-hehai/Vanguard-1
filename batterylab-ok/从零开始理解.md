# 从零开始：这个项目、这份协议、这些代码，到底是什么

这份文档假设你**完全没有相关背景**。所有名词第一次出现时都会用大白话解释。
读完之后你应该能回答三个问题：为什么要做通讯、协议是什么、代码在干什么。

---

## 第 0 章 一句话说清整个项目

实验室里有一台**电池测试柜**，它给电池充电放电，同时不停地记录电池的电压、电流、容量。

这些数据现在**被困在一个软件里**（叫 BTS Client），只能人点鼠标看，或者手动导出一个文件。
我们的目标是：**写一个程序，自动把数据取出来，算出电池健康度，自动生成报告。**

整条流水线就五步：

```
 ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐
 │ 1.设备   │   │ 2.数据   │   │ 3.把数据 │   │ 4.算电池 │   │ 5.生成   │
 │ 产生数据 │ → │ 存在电脑 │ → │ 取出来   │ → │ 健康度   │ → │ SOH 报告 │
 └──────────┘   └──────────┘   └──────────┘   └──────────┘   └──────────┘
   电池测试柜    BTS Client      ← 我们在这里 →     算指标         交作业
   自动完成      自动完成         要做的部分
```

**我们负责的是第 3、4、5 步。** 第 1、2 步是设备和新威的软件自己完成的。

而"通讯调通"指的就是**第 3 步** —— 让我们的程序能跟 BTS Client 说上话、拿到数据。

---

## 第 1 章 先搞懂电池测试柜在干什么

### 1.1 它在做的事

一颗电池好不好，要看它能不能"反复充电放电很多次还能存住电"。
所以测试柜就做一件很朴素的事：

> 给电池充电 → 停一会 → 放电 → 停一会 → 再充电 → …… 反复几百上千次，
> 同时每隔一小段时间记一条数据："现在是第几圈、第几步、电压多少、电流多少、已经充进去多少电"

### 1.2 三个必须懂的行业词

**工步（step）**：测试程序里的一小步。
比如一个测试程序可能是：
1. 恒流充电到 4.2V（工步1）
2. 搁置 10 分钟（工步2）
3. 恒流放电到 3.0V（工步3）

协议里给这些步骤起了代号：`cc`=恒流充电、`dc`=恒流放电、`rest`=搁置、`cccv`=恒流恒压充电……
一共 17 种。**我们算容量的时候，必须靠这个代号分辨"这一步是在充电还是放电"。**

**循环（cycle）**：一次完整的"充满 + 放完"叫一个循环。
"循环寿命 1000 次"意思是充放 1000 次之后电池就明显不行了。

**容量（capacity）**：这一步/这一圈充进去或放出来多少电，单位安时（Ah）。

### 1.3 数据长什么样

测试柜每记录一次，就是一条数据。用协议文档里的真实例子：

```
序号 1，工步 1，循环 0，类型=搁置，本工步已运行 0 毫秒，
绝对时间 2023-04-21 16:07:14，电压 1.226438 V，电流 1.471726 A，
容量 0 Ah，能量 0 Wh，辅助通道1 = 0.601604，温度 T1 = 320.6，...
```

一次测试下来，这种数据会有**几万到几百万条**。所以：
- 不能靠人一条条抄，必须程序自动取 → 这就是第 3 步
- 数据量太大，所以协议规定"一次最多给你 1000 条"，要**翻页**慢慢取

### 1.4 为什么"取数据"是个难题

BTS Client 是个 Windows 图形软件，设计给人点鼠标用的。
它能显示数据、能导出 Excel，但这些都**没法自动做**。

新威（Neware，设备厂商）知道客户需要自动化，于是额外开了一个功能：
**允许别的程序通过一条"通道"来跟 BTS Client 对话**，让程序也能问到数据。
这个功能叫 **API**（Application Programming Interface，应用程序接口）。

这个功能**默认是关着的**，要输入厂商给的**注册码**才能激活。
—— 这就是我上一轮说"注册码是一票否决项"的原因。没有它，门根本打不开。

---

## 第 2 章 协议是什么：两个程序之间的"话术手册"

### 2.1 一个类比

你去饭店点菜。你不能随便说一句"来点吃的"，后厨听不懂。
你们之间有一套默契：你报**菜名**，后厨回**"好的"或者"没有"**。

**协议就是这份默契的书面版本。** 它规定四件事：

| 规定什么 | 在这个项目里 |
|---|---|
| ① 怎么建立联系 | 用"管道"或者"TCP"（第 5 章讲） |
| ② 能说什么话（有哪些命令） | 一共 22 条命令，比如"给我设备信息""给我历史数据" |
| ③ 话要怎么说（格式） | 用一种叫 XML 的文本格式（第 3 章讲） |
| ④ 对方会怎么回答 | 每条命令都有固定的回答格式，比如问 `getdevinfo` 就答 `getdevinfo_resp` |

所以"新威尔电池测试系统 BTSAPI 协议 v1.19"这份文档，就是**新威写给客户的说明书**，
告诉你：这 22 句话怎么说，对方会怎么答。

### 2.2 为什么会有大小写、格式这些奇怪要求

因为程序是**死的**，它只会逐字比对。
你发 `GetDevInfo`，它就不认识 —— 它只认 `getdevinfo`。
所以文档里反复强调"通讯内容要注意大小写，要保持与文档里一致"。
**这不是文档啰嗦，这是程序的工作方式。**

---

## 第 3 章 逐字读懂一次完整对话

协议用的是 **XML**。别被这个词吓到，它就是一种**用尖括号给内容贴标签**的文本格式。

### 3.1 我们的提问

想拿设备信息，程序会发出这样一段文字：

```xml
<?xml version="1.0" encoding="UTF-8" ?>
<bts version="1.0">
<cmd>getdevinfo</cmd>
</bts>
```

逐行翻译成人话：

| 这一行 | 意思 |
|---|---|
| `<?xml version="1.0" encoding="UTF-8" ?>` | "下面是一段 XML，请用 UTF-8 编码来读"（UTF-8 是一种中文英文都能表示的编码） |
| `<bts version="1.0">` | "一条 BTS 消息开始了，协议版本 1.0" |
| `<cmd>getdevinfo</cmd>` | "命令（cmd=command）是 getdevinfo" —— 就是"给我设备信息" |
| `</bts>` | "消息结束" |

> 标签成对出现：`<bts>` 开始、`</bts>` 结束。
> 带斜杠的表示"结束"，不带斜杠的表示"开始"。这是 XML 唯一的规则。

另外协议规定，这段话发出去之后要**再加两个换行**，表示"我说完了"。
（如果用 TCP 方式，改成加 `#` 和换行。这就是第 5 章的内容。）

### 3.2 对方的回答

BTS Client 回过来的是这样一段：

```xml
<bts version="1.0">
<cmd>getdevinfo_resp</cmd>
<serverip count="2">
  <server ip="192.168.1.33" port="3306"/>
  <server ip="192.168.1.34" port="3306"/>
</serverip>
<middle count="4">
  <channel ip="192.168.1.35" devtype="22" devid="1" subdevid="1" Channelid="1">true</channel>
  <channel ip="192.168.1.35" devtype="22" devid="1" subdevid="2" Channelid="2">true</channel>
  <channel ip="192.168.1.36" devtype="22" devid="1" subdevid="3" Channelid="3">true</channel>
  <channel ip="192.168.1.36" devtype="22" devid="1" subdevid="3" Channelid="1">false</channel>
</middle>
</bts>
```

逐行翻译：

| 这一行 | 意思 |
|---|---|
| `<cmd>getdevinfo_resp</cmd>` | `resp` = response = 回复。所以这是"设备信息的回复" |
| `<serverip count="2">` | 有 2 台服务器（可以理解为"数据中转机"） |
| `<server ip="..." port="3306"/>` | 第 1 台的地址是 192.168.1.33，端口 3306 |
| `<middle count="4">` | "中位机"下面挂着 4 个通道 |
| `<channel ...>true</channel>` | 一个通道。`true` 表示这个通道可以被点亮指示灯 |
| 结尾的 `/>` | 表示"这个标签自己就结束了"，没有内容 |

### 3.3 通道地址：四级门牌号

注意这一行：

```xml
<channel ip="192.168.1.36" devtype="22" devid="1" subdevid="3" Channelid="1">true</channel>
```

要操作**某一颗具体的电池**，得说清楚是哪一颗。地址分四级，像快递地址一样逐级缩小范围：

| 字段 | 像什么 | 含义 |
|---|---|---|
| `devtype` | 哪个小区 | 设备型号。22=BTS80、23=BTS81、24=BTS82、25=BTS83…… |
| `devid` | 哪一栋 | 设备编号 |
| `subdevid` | 哪一层 | 单元/机箱编号 |
| `chlid` | 哪一个房间 | 通道号。**一个通道接一颗电池** |

上面那行的地址就是 `22-1-3-1`：BTS80 型号的 1 号设备、3 号机箱、1 号通道。

**以后所有命令都要带上这个地址**，否则对方不知道你要操作哪颗电池。
我们代码里把它打包成一个 `Channel` 对象，就是为了每次自动带上这四段数字。

> ⚠️ 协议里有个小坑：`getdevinfo` 的回复用的是 `Channelid`（大写 C），
> 但其他命令用的是 `chlid`（小写）。这种不一致在厂商文档里很常见，
> 写代码时得两个都认。

### 3.4 为什么要"解析"

对方回的是**一大段文字**。程序要拿里面的数字去算，就得先把文字切成结构化的数据。
这个过程叫**解析（parse）**。

用大白话说：解析就是"从一封信里把关键信息抄到表格上"。
文字里 `devtype="22"`，解析完就变成 `{"devtype": 22}`，程序就能直接用了。

**原始文字要保留**（我们代码里 `response.text` 就是原文），
因为一旦解析出错，你得能回头看原文，才知道是你解析错了还是对方发的东西不对。

---

## 第 4 章 三条取数命令（这是全场重点）

22 条命令里，跟我们拿数据最相关的只有三条。协议里的名字和它们真正的作用：

### 4.1 `inquire` —— "现在这一刻的读数"

**类比**：看汽车的仪表盘。问"现在时速多少、油还剩多少"。

回答里是**当前这一瞬间**的状态：

| 字段 | 含义 |
|---|---|
| `voltage` / `current` | 现在电压 / 电流 |
| `capacity` / `energy` | 这一步目前累计的容量 / 能量 |
| `cycle_id` / `step_id` | 现在跑到第几循环、第几步 |
| `workstatus` | 工作状态：`working`测试中、`stop`停止、`protect`保护、`finish`完成…… |
| `barcode` | **电池的条码**（相当于电池的身份证号） |
| `totaltime` / `relativetime` | 总时间 / 相对时间，**单位是秒** |

**用途**：做进度监控、判断测试跑完没有、在界面上画实时曲线。
**注意**：这个命令拿到的是"当前值"，不是历史记录，不能用来算 SOH。

### 4.2 `download` —— "把历史明细一条条拉出来"

**类比**：导出银行流水，每一笔都列出来。

这是**最主要的数据来源**。返回的每一条包含：
`seqid`序号、`stepid`工步号、`cycleid`循环号、`steptype`工步类型、
`testtime`本工步已运行时间、`atime`绝对时间、
`volt`电压、`curr`电流、`cap`容量、`eng`能量，
还有 `V1/T1/Thk1` 这类**辅助通道**（温度、辅助电压、厚度……具体是什么要看硬件配置）。

**三个必须知道的限制**：

1. **一次最多只能要 1000 条**。数据有几万条怎么办？翻页：
   第一次要第 1~1000 条，第二次要第 1001~2000 条……直到对方给的不足 1000 条，说明到底了。
   我们代码里的 `download_all()` 就是自动做这件事的函数。
2. **一次只能要一个通道**。想要 16 个通道的数据，就得问 16 次。
3. **只能拿本机（127.0.0.1）上的数据**。这是个重要限制，第 5 章会用到。

> ⚠️ **时间单位的坑**：`download` 里的 `testtime` 单位是**毫秒**，
> 而 `inquire` 里的 `totaltime` 单位是**秒**。
> 混着用会差 1000 倍，画出来的曲线会离谱。这就是我在体检脚本里专门加一项
> "用真实数据反推时间单位"的原因 —— 不能只信文档，要用实测数据核对。

### 4.3 `downloadStepLayer` —— "每一步的汇总"，算 SOH 最省事

**类比**：不是看每一笔流水，而是看"每月账单小结"。

它不返回每条明细，而是**每个工步返回一行**，内容是这个工步的汇总：

| 字段 | 含义 |
|---|---|
| `stepindex` / `stepid` / `cycleid` | 工步序号 / 原始工步号 / 循环号 |
| `steptype` | 工步类型（cc/dc/rest…） |
| `cap` / `eng` | 这一步的容量 / 能量 |
| `startvolt` / `endvolt` | 这一步开始和结束时的电压 |
| **`dcir`** | **直流内阻**（单位毫欧）。电池老化的关键指标，**这个命令直接给你算好了** |

**为什么算 SOH 用这个最省事**：容量保持率是"每一圈的放电容量"之比。
明细数据有几百万条，而工步层数据通常只有几千行 ——
**数据量小两个数量级，但算 SOH 需要的数字全都有。**

我们 `probe.py` 里的"SOH 预览"就是完全基于这个命令算的。

---

## 第 5 章 管道和 TCP：两种"打电话"的方式

要跟 BTS Client 说上话，有两种连接方式。**这两种的区别会直接决定你的架构**，所以必须搞懂。

### 5.1 打个比方

| | 管道（Pipe） | TCP |
|---|---|---|
| 像什么 | 同一栋楼里两个房间之间的**传纸条窗口** | **打电话**到另一栋楼 |
| 能不能跨电脑 | **不能**，必须在同一台电脑上 | 能，只要有 IP 和端口 |
| 要知道什么 | 只要知道"窗口的名字" | 要知道对方的 **IP 地址**和**门牌号（端口）** |
| 难点 | 需要管理员权限 | 需要网络通、端口没被占用 |

### 5.2 管道

管道有个固定的名字：

```
\\.\pipe\NewareBtsAPI
```

`\\.\pipe\` 是 Windows 表示"管道"的固定前缀，后面 `NewareBtsAPI` 是名字。
可以理解成"本机上的一个传纸条窗口，窗口名叫 NewareBtsAPI"。

**两个硬性限制**（协议文档白纸黑字写的）：

1. **必须在同一台电脑上。** 文档原话："管道通讯方式不支持操作远程电脑上的客户端"。
2. **必须以管理员权限运行。** 因为 BTS Client 自己是管理员权限运行的，
   权限低的程序没有资格跟它对话 —— 就像普通员工进不了机房。

### 5.3 TCP

TCP 是真正的网络连接。需要两样东西：

- **IP 地址**：对方电脑在网络上的地址，比如 `192.168.1.20`
- **端口号**：对方电脑上"哪个程序在接电话"。BTS Client 固定用 **502**

> **端口**是什么？一台电脑上同时跑着很多程序（浏览器、微信、BTS Client……），
> IP 地址只能定位到"哪台电脑"，端口号才能定位到"哪个程序"。
> 就像 IP 是大楼地址，端口是房间号。

**为什么 502 会成为一个坑**：一个端口同时只能被一个程序占用。
如果别的程序占了 502，BTS Client 就开不了这个"接听窗口"，你就连不上。
所以排查时要检查"502 是否被占用"。

**版本要求**：协议文档说，TCP 方式需要 **2023/3/17 之后**的客户端版本。
所以必须问现场"客户端版本是多少"，老版本只能用管道。

### 5.4 这个选择为什么关键

回顾第 4.2 节的限制：`download` **只能拿 127.0.0.1 上的数据**。
（127.0.0.1 是"本机"的意思，也叫 localhost。）

现在把这个限制和第 5 章结合起来：

| 情况 | 结论 |
|---|---|
| BTS Client 和我们的程序**在同一台电脑** | 管道可用，TCP 也能用（连 127.0.0.1）。**最省事** |
| BTS Client 在**另一台电脑** | **只能用 TCP**。而且 `download` 那个"只能 127.0.0.1"的限制不是问题 —— 因为从 BTS Client 自己的角度看，数据确实在它本机上 |

所以那次我问你的"BTS Client 和 TY1200 盒子是不是同一台机器"，
不是随口一问 —— **它决定了连接方式，进而决定了后面所有代码怎么配。**

---

## 第 6 章 代码其实只做五件事

`neware_client.py` 有 900 多行，但核心逻辑就五步。
用 `example_minimal.py` 这个文件对照看（那是我专门写的带逐行注释的版本）。

### 6.1 五步

| 步骤 | 干什么 | 代码里对应 |
|---|---|---|
| ① 建立连接 | "拨通电话" | `transport.connect()` |
| ② 拼一句话 | 把命令拼成 XML 文本 | 库内部自动做，比如 `"<cmd>getdevinfo</cmd>"` |
| ③ 发出去，等回答 | 发送 + 等待 + 收回来 | `client.request(...)` |
| ④ 解析回答 | 从文字里挑出数据 | `NewareClient.parse_devinfo(...)` |
| ⑤ 存起来 | 落成表格文件 | `write_rows(...)` 或自己写 CSV |

### 6.2 一个真实的最小例子

```python
from neware_client import Channel, NewareClient, TcpTransport

# ① 建立连接：拨通 192.168.1.20 这台电脑的 502 端口
transport = TcpTransport("192.168.1.20", 502)
transport.connect()

# ② 对话器：以后用它一问一答
client = NewareClient(transport)

# ③ + ④ 问"有哪些通道"，并把回答解析成结构化数据
info = NewareClient.parse_devinfo(client.getdevinfo())
print(info["channels"][0])       # 看看第一个通道

# 选一个通道，给它一个"地址对象"
c = info["channels"][0]
channel = Channel(c["devtype"], c["devid"], c["subdevid"], c["chlid"], c["ip"])

# ⑤ 把这个通道的历史数据全部拉下来（内部自动翻页）
rows = client.download_all(channel)
print(f"共 {len(rows)} 条")

transport.close()                # 挂断
```

就这么多。**剩下的 900 行，是在处理 22 条命令各自的细节和协议里的各种坑。**

### 6.3 "翻页"在代码里长什么样

协议限制一次最多 1000 条，所以 `download_all` 内部是个循环。
用伪代码说清楚：

```
从第 1 条开始
重复 {
    要 1000 条
    如果 一条都没给 → 结束
    把这批数据追加到总结果里
    如果 这次的条数 < 1000 → 说明到底了，结束
    否则 下次从"这批最后一条的序号 + 1"开始
}
```

**关键点**：`startpos` 参数是**数据序号**（第几条），不是"跳过多少条"。
这是很容易搞错的地方，写错了会重复取数据或者漏数据。

### 6.4 一个具体例子：`getdevinfo()` 内部发生了什么

以 `client.getdevinfo()` 这一行为例，展开看它内部干的事：

```
1. 拼出这段文字：
       <?xml version="1.0" encoding="UTF-8" ?>
       <bts version="1.0">
       <cmd>getdevinfo</cmd>
       </bts>

2. 因为用的是 TCP，再在末尾加上 "#" 和换行，表示"我说完了"

3. 把这串文字转成字节，通过连接发出去

4. 循环收字节，直到收到的内容里出现了 "</bts>"，说明对方说完了
   （为什么要等 "</bts>"？因为对方可能分几次把数据发过来，
     必须等到明确的结束标记才能确定收全了）

5. 把收到的字节转回文字，交给解析函数

6. 返回
```

**第 4 步的等待是必须的**。网络传输不是一次到位的，
如果对方的话还没说完你就开始解析，就会解析失败。
这也是协议为什么规定"问答式"：**发一条，必须等到回答，才能发下一条。**
（好处是不用处理"回答串台"；代价是效率低，但对 SOH 分析够用了。）

---

## 第 7 章 为什么要按那个顺序做（8 步体检的道理）

这一章解释我上一轮给你的那份"准备清单"里，为什么坚持**先用新威自己的工具**。

### 7.1 核心思想：一次只怀疑一个东西

通讯链路上有四个环节，任何一环断了都不通：

```
我们的代码  ──→  连接方式  ──→  BTS Client  ──→  电池测试柜
（可能有bug）   （管道/TCP）    （要激活API）     （要开机、要接着电池）
```

如果一上来就用自己的代码测，失败了，你**根本不知道是哪个环节的问题**。
你会陷入"到底是我代码写错了还是设备没开"的泥潭，可能耗掉好几天。

### 7.2 解决办法：拿一个"确定是好的"零件做对照

新威提供了一个官方调试小工具 **TestAPI**（带界面，点按钮就能发命令）。
它自己写的，肯定是对的。

于是顺序变成：

| 第 1 步 | 用 TestAPI 点【设备信息】→【发送】 |
|---|---|
| 结果 A：有正确返回 | 说明**设备、客户端、连接方式、激活状态全都是好的**。<br>问题范围缩小到"只剩我们的代码"。这时候再看自己的代码，心里有底 |
| 结果 B：没有返回 | 说明**问题不在代码**，在设备或客户端。按故障速查表排查：客户端开没开？激活没有？管理员权限？502 被占用？ |

**这就是"控制变量法"**：先换上一个你认为没问题的零件，看还坏不坏。
和第 6 章的"先等 `</bts>` 再解析"一样，都是为了让每一步都有一个确定的判断依据。

### 7.3 同样的道理，也用在 `selftest.py` 上

我写的 `selftest.py`（70 项断言）用的是**协议文档里的样例数据**，
不需要真机就能跑。它的作用就是：

> 先在"没有设备"的情况下，证明**我的代码本身是对的**。

这样到了现场，如果 `probe.py` 失败，你就可以理直气壮地说：
"代码我离线测过了，70 项全过，所以问题在设备侧。"

**把"可能出错的东西"一个一个排除掉，这就是调试的全部方法论。**

---

## 第 8 章 SOH 是什么，报告里为什么要有它

### 8.1 SOH = State of Health = 电池健康度

一颗新电池能存 3.0 Ah 的电。用了一年之后只能存 2.4 Ah 了。
它还有多健康？`2.4 / 3.0 = 80%`。**这个 80% 就是 SOH。**

### 8.2 报告里通常有这四个指标

| 指标 | 怎么算 | 说明什么 |
|---|---|---|
| **容量保持率** | 第 N 圈的放电容量 ÷ 第 1 圈的放电容量 | SOH 的核心。掉到 80% 通常就算寿命终止 |
| **库仑效率** | 放电容量 ÷ 充电容量 | 充进去的和放出来的差多少。差得多说明有副反应在消耗电 |
| **能量效率** | 放电能量 ÷ 充电能量 | 比库仑效率更严格，因为它把电压差也算进去了 |
| **DCIR**（直流内阻） | 协议直接给（`downloadStepLayer` 的 `dcir` 字段） | 内阻变大 = 电池老化。单位毫欧 |

### 8.3 为什么这些能算出来（关键）

拿容量保持率举例，它需要"每一圈的放电容量"。这个数字怎么来？

1. 用工步层数据（第 4.3 节那个命令）
2. 按 `cycleid`（循环号）分组
3. 在每一组里，只看 `steptype` 是**放电**类的工步（`dc`/`dv`/`cccd`/`dp`/`dr`）
4. 把这些工步的 `cap` 加起来，就是这一圈的放电容量

> **注意一个容易错的点**：每一行的 `cap` 是"**这一步内**累计的容量"，不是整圈的。
> 所以要先按循环分组、再按工步类型筛选、最后求和。
> 直接对全表取最大值是错的 —— 这就是我在 `probe.py` 里把口径写在注释里的原因。

能做到这一步，说明**"数据读取 → 指标计算"这条链路已经完全验证了**，
剩下的只是"按模板排版成报告"，那是纯体力活。

### 8.4 为什么必须先拿到报告模板

因为"报告里有哪些指标、怎么分组、画什么图"决定了**我们到底需要取哪些数据、怎么算**。

举例：如果模板要求画"容量保持率随循环数的曲线"，那我们需要**每一圈**的容量。
但如果模板只要"首圈和末圈的对比 + 一个最终 SOH 值"，那只需要两个数字。

**模板不定，取数方案就定不下来，开发就会反复返工。** 所以它是关键路径。

---

## 第 9 章 名词小词典

| 名词 | 一句话解释 |
|---|---|
| **API** | 让程序（而不是人）去调用另一个程序的功能。这里是"让程序问 BTS Client 要数据"的功能 |
| **协议** | 两个程序对话的规则手册：能说什么、怎么说、对方怎么答 |
| **XML** | 一种用 `<标签>内容</标签>` 表示结构的文本格式。像填表格，每格都有标题 |
| **解析（parse）** | 把一大段文字里的有用信息挑出来，变成程序能直接用的数据结构 |
| **管道（Pipe）** | 同一台电脑上两个程序之间的通讯通道。名字形如 `\\.\pipe\xxx`。不能跨电脑 |
| **TCP** | 通过网络跟另一台电脑上的程序通讯。需要 IP 地址 + 端口号 |
| **IP 地址** | 电脑在网络上的地址，例如 `192.168.1.20` |
| **端口** | 一台电脑上"哪个程序在接电话"的编号。BTS Client 用 502 |
| **127.0.0.1 / localhost** | "本机"的意思。指自己这台电脑 |
| **管理员权限** | Windows 里的最高权限。管道方式必须用它，因为 BTS Client 是管理员运行的 |
| **devtype / devid / subdevid / chlid** | 通道的四级地址：设备型号 / 设备号 / 单元号 / 通道号 |
| **工步（step）** | 测试程序里的一步，如"恒流充电到 4.2V"。类型有 17 种 |
| **循环（cycle）** | 一次"充满 + 放完" |
| **工步层数据** | 每个工步一行的汇总数据（不是每条明细）。含 DCIR，算 SOH 最省事 |
| **辅助通道** | 主通道之外的额外测量，如温度 `T1`、辅助电压 `V1`。字段名随硬件变化 |
| **SOH** | 电池健康度 = 当前容量 ÷ 初始容量 |
| **DCIR** | 直流内阻，单位毫欧。电池老化的关键指标 |
| **BTSDA** | 新威官方的电池数据分析软件。用来做"官方导出"，作为我们解析结果的对照基准 |
| **NDA / NDAX** | 新威测试数据文件的格式。是"离线文件路线"的数据来源 |
| **CSV / Parquet** | 两种表格文件格式。CSV 能用 Excel 打开看，Parquet 体积小、读取快，适合大数据 |
| **TestAPI** | 新威官方的通讯调试小工具，用来在写代码之前先验证通讯是通的 |

---

## 第 10 章 你现在该做什么

### 第一步：先看，不用动手（今天就能做）

1. 打开 `example_minimal.py`，**从第一行读到最后一行的注释**。
   这个文件是专门为你写的，每行都解释了"为什么"。
2. 打开 `CHECKLIST.md` 的第二章，那是发给设备负责人的**信息采集表**。
   你会发现上面要填的东西，现在你都能看懂为什么要填了。

### 第二步：跑离线自测，感受一下

```bash
cd neware_comms
python selftest.py
```

你会看到 70 行 `PASS`。这些测试**不需要任何设备**，
用的全是协议文档里的样例数据。跑通它，你就知道代码这一半是好的。

### 第三步：等文件到手，做真的验证

拿到注册码、确认了现场信息之后：

```bash
# 先用新威官方 TestAPI 点【设备信息】→【发送】，看有没有正确返回
# 再用我们的工具做全面体检
python probe.py --transport tcp --host <现场那台电脑的IP>
```

它会一步一步告诉你哪个环节通了、哪个没通。

---

## 附：还有哪些没搞懂，可以直接问我

这份文档覆盖了"是什么"和"为什么"。如果你在具体某处卡住，可以直接说，比如：

- "第 3 章的 XML 我还是看不懂" → 我把每条命令的 XML 都逐字翻译一遍
- "为什么要用 Polars / Parquet / DuckDB 这些词" → 那些属于**数据处理阶段**（流水线第 4 步），还没讲到
- "SOH 的计算我还是不明白" → 我拿一份真实的测试数据，手把手算一遍给你看
- "Open 那些代码文件看不懂 Python" → 我推荐一条最短的 Python 学习路径，只学这个项目用得到的那部分

**不用怕问得基础，问清楚比猜着做强。**
