# 能不能直接和测试机通讯？—— 方案分析

问题：能不能绕过 BTS Client 软件，直接和测试机通讯？

**结论先说**：分几种含义，可行性差别很大。其中"直连数据库"技术上很可能可行，
但**我不建议在 Demo 阶段走这条路**。理由不是"做不到"，而是风险不可控。
下面把依据和理由摆清楚。

---

## 一、先看清厂商的架构（从协议文档里挖出来的线索）

这几条线索决定了"直接通讯"有没有可能：

### 线索 1：数据确实保存在数据库里

协议文档 1.3 节"系统目标"的原话：

> "电池测试系统的目标，就是能实时地接收大量采集到的数据，**并保存数据到数据库**，
> 可以查询历史数据，可以分析电池测试数据。"

### 线索 2：那个"服务器"的端口是 3306

`getdevinfo_resp` 返回的服务器列表：

```xml
<serverip count="4">
  <server ip="192.168.1.33" port="3306"/>
  <server ip="192.168.1.34" port="3306"/>
  <server ip="192.168.1.35" port="3306"/>
  <server ip="192.168.1.36" port="3306"/>
</serverip>
```

**3306 是 MySQL / MariaDB 的默认端口。** 这几乎可以肯定：
协议里说的"服务器"就是**跑着 MySQL 的机器**，测试数据存在这个库里。

### 线索 3："服务器"IP 和"通道"IP 是重合的

同一次 `getdevinfo` 返回的通道里：

```xml
<channel ip="192.168.1.35" devtype="22" devid="1" subdevid="1" Channelid="1">true</channel>
<channel ip="192.168.1.36" devtype="22" devid="1" subdevid="3" Channelid="3">true</channel>
```

`.35` 和 `.36` 既出现在服务器列表里，又出现在通道的 IP 里。
说明**一台"服务器"机器同时承担两个角色**：跑 MySQL 存数据 + 挂着通道硬件
（协议里叫"中位机"）。

### 线索 4：API 只能读"本机"那个库

协议里 `download`、`downloadStepLayer`、`downloadlog` 都强调：

> "只下载**服务器 127.0.0.1** 下的测试数据"

`inquiredf` 也写"只支持查询 **127.0.0.1** 服务器"。

**这条限制现在能理解了**：客户端能直接读到的是它**自己本机**那个库，
别的服务器机器上的库它读不到（或者说 API 不做代理）。

### 综合起来，实验室的架构可能是这样

```
电池柜（通道板 + 中位机）
      │  新威私有链路
      ↓
"服务器"机器 —— 跑 MySQL（3306），数据存这里
      │  MySQL 客户端连接
      ↓
② Windows 主机 —— 跑 BTS Client，连到上面那个库
      │
      ↓ 我们（通过 TCP 502 的 API）
   ③ 边缘算力盒子
```

**⚠️ 但这里有一处必须现场确认**：② 和"服务器"**是不是同一台机器**？

- 如果**是**（小实验室常见的"客户端+服务器装一台"）：数据就在 ② 上，
  MySQL 也在 ② 上
- 如果**不是**：实验室有一台我们还没注意到的独立数据库服务器。
  需求分析书里那句"数据统一输出并存储于该服务器"指的可能就是它

**怎么确认**：跑 `getdevinfo`，把返回的 `serverip` 列表和 ② 自己的 IP 比一比。
含有 ② 的 IP 就是同机；全是别的 IP 就说明有独立服务器。

这件事很重要，因为它决定了"数据到底在哪台机器上"——
这既影响"能不能直连"，也影响我们整个部署方案的假设。

---

## 二、"直接通讯"有四种含义，可行性完全不同

| 含义 | 技术上可行吗 | 建议 |
|---|---|---|
| **A. 直连数据库**（MySQL 3306） | **很可能可行** | 风险高，**不建议**做核心依赖（详见第三节） |
| **B. 直连中位机 / 通道板硬件** | **不可行** | 新威的私有硬件协议没有公开文档，除非厂商提供 |
| **C. 绕过客户端，模拟界面操作** | 不可行 | 极脆弱，界面一改就废，不该做 |
| **D. 用官方 API**（现在的方案） | ✅ 已经跑通 | **这本身就是"直接访问"的正当形态** |

### 关于 D：其实 API 就是"直接通讯"

这一点值得说清楚：**API 就是厂商提供的、受支持的、程序化访问数据的官方接口。**
它不是"间接" —— 它是官方指定的那个"直接"方式。

我们通过它拿到的数据，和界面上看到的是同一份，而且比手动导出规范得多。

### 关于 B：为什么不现实

中位机和通道板之间的通讯协议是**新威私有的、没有公开文档的**。
要直连就得逆向，而逆向的结果会随固件版本失效，且存在法律风险。
除非新威主动提供，否则这条路不该考虑。

---

## 三、关于"直连数据库"：为什么有吸引力，为什么我不建议

### 3.1 吸引力确实存在

| 好处 | 对比现在的 API 方案 |
|---|---|
| SQL 想查多少查多少 | API 每次最多 1000 条，要翻页 |
| 一次查多个通道 | API 一次只能要一个通道 |
| 可以直接做聚合查询 | 用 SQL 就能算出容量保持率，不用把数据全拉到内存 |
| 不依赖客户端的 API 模式 | 不用管激活码、不用管客户端开着 |
| 可能能拿到更细的原始数据 | API 给的字段是整理过的 |

这些好处都是真实的。如果只从"取数效率"看，直连库确实更好。

### 3.2 但不建议做核心依赖，五个理由

**① schema 是私有的，要逆向**

厂商没有公开表结构。你得自己摸索哪张表存什么、字段什么含义、
容量是"每步累计"还是"整圈累计"、时间单位是秒还是毫秒。

**② 厂商明确声明可能随时改，且不通知**

协议文档的 NOTICE 原文：

> "Neware reserves the right to make changes to specifications at any time and
> without notice."

软件版本一升级，表结构可能就变了，你的代码直接失效。
而 API 是厂商承诺的接口，改之前至少会在协议文档里体现。

**③ 有实际运行风险 —— 这是最严重的一条**

客户端正在往这个库里**写**数据。你直接查库，
可能和它争表锁、争磁盘 IO，**影响正在进行的测试记录**。

这个风险的严重性远超"代码失效"：**测试数据一旦损坏或缺失，无法追溯补录。**
实验室里可能有人正跑着几天几夜的实验，你的一次慢查询就可能把数据搞坏。

**④ 授权问题**

协议文档开头写着"没有任何授权的情况下不允许使用本文档"。
数据库 schema 更没有对外授权。要走这条路，**必须先跟新威确认授权**，
否则出了问题责任说不清。

**⑤ 依赖并没有减少，口径风险反而增加**

如果数据库在 ② 上，你还是需要 ② 一直开着 —— **去掉不了 ②**。
而且数据库里的存储格式和 API 返回的口径**可能不一样**
（容量口径、时间单位、辅助通道的表示方式）。
API 是厂商整理过的，直连库你得自己搞清所有口径 ——
**而这恰恰是最容易出错、最容易做出错误 SOH 报告的地方。**

### 3.3 一句话总结

**直连库解决的是"取数效率"问题，但引入了"数据被污染"和"口径搞错"两个更难收拾的问题。**
对于要出 SOH 报告的场景，**口径正确比取数快重要得多。**

---

## 四、如果目标是"减少对 ② 的依赖"，有更好的办法

先问一句：为什么要绕开 ②？如果是担心"② 关机了采集就断了"，
那有比直连数据库更正当的做法：

| 目标 | 更好的做法 | 为什么 |
|---|---|---|
| ② 关机也不断采集 | **把采集服务放在 ② 上常驻**（同机连 127.0.0.1） | 采集服务跟着客户端一起开机自启，数据不出机器，最稳 |
| 拿到完整原始数据 | **用 NDA 文件路线**（`start` 时开自动备份） | 这是官方支持的，BTSDA 自己也这么读 |
| 减少取数次数 | 优先用 `downloadStepLayer` | 数据量小两个数量级，算 SOH 需要的字段全都有 |
| 想用 SQL 做分析 | **把数据拉下来后在本地建库**（DuckDB / Parquet） | 既得到 SQL 的便利，又不动厂商的库，口径还是 API 的 |

**最后一条特别值得推荐**：你不需要去查厂商的库，
你只需要**把 API 拿到的数据落到本地**（Parquet 或 DuckDB），
然后在本地随便用 SQL 查。这样既有 SQL 的灵活性，又完全没有上面的五个风险。

**数据量其实不大**：工步层数据一个测试才几千行，明细数据几百万行
但要落成 Parquet 后也就几十 MB 级别。本地建库完全放得下。

---

## 五、现在就能做的一件事（顺便验证架构）

跑一下 `getdevinfo`，看清两件事：

```bash
python neware_client.py --transport tcp --host <②的IP> info
```

**看第一部分的服务器列表**：

1. **`serverip` 里有 ② 自己的 IP 吗？**
   - 有 → 数据库和客户端同机，数据就在 ② 上
   - 没有 → 实验室有独立的数据库服务器，需要弄清楚它在哪、谁管

2. **`server` 的端口是不是 3306？**
   - 是 → 确认是 MySQL/MariaDB
   - 不是 → 那可能不是数据库端口，需要重新判断

这两条信息会直接影响我们对"数据存储位置"的理解，也影响后面部署方案的假设。
**建议第 0 阶段就跑一次，把结果记下来。**

---

## 六、如果要探索这条路，现在该问厂商什么

不要一上来就动手连库。先问清楚这四件事：

| 要问的 | 为什么 |
|---|---|
| 测试数据是不是保存在 MySQL / MariaDB？什么版本？ | 确认技术前提 |
| **有没有官方授权的只读数据访问方式？**（只读账号 / 视图 / 官方数据接口） | 有官方途径最好，就不用逆向了 |
| 表结构文档能不能提供？ | 没有文档就得逆向，成本和风险都高 |
| 并发访问对正在进行的测试有没有影响？ | 这是最关键的安全问题 |

**三条底线**（如果最终决定要连）：

1. **要授权** —— 书面确认，不要默认"自己的设备随便连"
2. **只读** —— 绝对不做任何写操作
3. **避开测试时段** —— 不要在有人跑实验的时候做大查询

---

## 七、最终建议

### 短期（Demo 阶段，到 10/15）

**不要走数据库直连。** 用现在的方案：**API 取数 + NDA 文件归档**。

理由：3 周工期，这条路的风险（schema 逆向、口径搞错、影响测试）
都是**不可控且会致命**的 —— 尤其"影响正在跑的测试"这一条，
一旦出事不是延期的问题，是赔人家实验的问题。

### 中期

把数据落到本地（Parquet / DuckDB）之后，你在本地就能用 SQL 随便查。
这样就得到了"直连数据库"的全部便利，而没有任何风险。

### 同时

把第六节的四个问题发给新威。如果厂商确实提供官方只读接口，
那这条路的性质就从"逆向"变成了"官方支持"，可以重新评估。

---

## 附：一句话回答

**能，但没必要，而且现在不该做。**

- 「能」：数据存在 MySQL 里（端口 3306 是线索），技术上很可能连得上
- 「没必要」：把 API 数据落到本地用 DuckDB 查，SQL 的便利一样有，风险为零
- 「现在不该做」：要逆向私有 schema，还可能影响正在跑的测试，而测试数据无法补录
