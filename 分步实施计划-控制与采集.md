# 分步实施计划：测试机控制与数据采集

已确认的设备分工：

| 设备 | 承担什么 | 谁负责 |
|---|---|---|
| ① 电池柜 | 硬件，接电池，产生数据 | — |
| ② Windows 主机 | 跑 BTS Client + TestAPI，数据存在这里 | 可能要装我们的采集程序 |
| ③ 边缘算力盒子 | **部署大模型和 Agent** | 别人负责 |

你的职责：**测试机控制 + 数据采集**。这份文档是分步做法。

---

## 第一步：先定一件事 —— 代码跑在哪台机器上

这是动手前必须定的，因为后面所有命令的写法都取决于它。

### 两个选项

| | 方案 A：跑在 ③ 边缘盒子（推荐先用这个） | 方案 B：跑在 ② Windows 主机 |
|---|---|---|
| 怎么连客户端 | 走 TCP，连 ② 的 IP | 同机，可用 127.0.0.1 或管道 |
| 要配什么 | ② 要放行防火墙 502 | ② 上要装 Python 环境 |
| 数据落在哪 | ③ | ② |
| 调试难度 | 一套代码、一个地方，简单 | 多一层"③ 怎么拿到 ② 的数据" |
| 网络风险 | 怕网络抖动、IP 变动 | 不怕（不出机器） |
| 对 ③ 的资源占用 | 可忽略（见下） | — |

### 推荐：先按方案 A 做

**理由**：

1. **采集程序是"轻量级"的。** 它做的事情是每隔几十秒发几条小命令、偶尔拉几百 KB 数据。
   这跟 27B 模型推理完全不在一个量级 —— 一个大模型光加载权重就要占掉几十 GB 显存和内存。
   **采集程序对模型的影响可以忽略。** 你不用担心"抢资源"。

2. **Demo 阶段图的是能跑通、好调试。** 一套代码放在一个地方，
   出错时不用两边猜。等跑通了再考虑要不要下沉，也来得及。

3. **我们现有的工具全部是按这个方案写的**（`probe.py`、`neware_client.py` 都假设从远程机器走 TCP）。

**什么时候该改成方案 B**：

- ③ 的磁盘放不下原始数据（**这才是真正的资源瓶颈** —— 一次循环测试的明细可能有几百万行）
- 网络经常抖，TCP 天天断
- ③ 上还要跑别的东西，磁盘/内存实在紧张

**关于磁盘，一个实用的建议**：原始数据可以留在 ②，
让 ② 的客户端自动导出 NDA 文件（第 3 阶段会讲到怎么开），
③ 上只放"算好的指标和摘要"。这样 ③ 的磁盘压力就很小。
需求书要求"数据不出实验室网络"，② 和 ③ 都在实验室里，两种放法都满足。

> ⚠️ 这个决定涉及整体架构，**建议跟王涛老师确认一下再动手**。
> 我给的推荐是"Demo 阶段先用方案 A，跑通后再评估要不要下沉"。

### 先给自己做个心理预期

你负责的这两块，**难点不在写代码，而在几件"要别人给你"的事**：

- ② 的 IP 和客户端版本（问设备负责人）
- 通道 ↔ 电池条码的对应表（问设备负责人）
- 工步文件放在哪、谁维护（问研究人员）
- 注册码（已到手了）

代码这边其实已经写好了。**所以下面每个阶段，我都会标出"卡点在哪、要找谁"。**

---

## 第 0 阶段：把"能连上"做完（约半天）

目标：能从 ③ 上看到通道清单。

### 在 ② 上做

| # | 做什么 | 说明 |
|---|---|---|
| 1 | 【帮助 → 模式设置】填入注册码 → 点【激活】 | 注册码已到手 |
| 2 | **重启客户端** | 最容易忘的一步，不重启不生效 |
| 3 | 悬停【已激活】，确认能看到包含 `API` 的字样 | 这是激活成功的证据 |
| 4 | `ipconfig` 记下 IP，并**设为固定 IP** | 程序要写死这个地址，DHCP 换地址会导致静默断线 |
| 5 | 放行防火墙 502 入站 | 异机必做。不做就是"连接超时" |
| 6 | `netstat -ano \| findstr :502` 确认端口没被占用 | 有输出说明被占了，客户端监听不起来 |
| 7 | **用官方 TestAPI 点【设备信息】→【发送】** | ★ 本机验证，先排掉"客户端问题" |

**第 7 步为什么必须先做**：它不涉及网络，纯粹验证"客户端 + API 激活 + 设备"是好的。
如果直接从 ③ 测，失败了你会分不清是客户端问题还是网络问题。

**这一关的卡点**：如果客户端版本早于 2023/3/17，TCP 不可用。

### 在 ③ 上做

```bash
# 1. 确认操作系统（很可能 Linux）
uname -a

# 2. 确认网络可达
ping <②的IP>

# 3. 装 Python 3.9+（Linux 一般自带 python3）
python3 --version

# 4. 把 neware_comms 目录拷过去，然后跑
cd neware_comms
python3 neware_client.py --transport tcp --host <②的IP> info
```

### ✅ 本阶段验收标准

`info` 命令能列出通道清单，类似：

```
服务器（中位机）2 台，通道 16 个
  server 192.168.1.33:3306
  22-1-1-1@192.168.1.35  BTS80  可点灯=True
  ...
```

**能看到这个清单，说明第一层（连得上）通了。**

如果报错，看错误信息就能定位：

| 报错 | 问题在哪 | 找谁 |
|---|---|---|
| 连接被拒绝 | 网络通，问题在客户端 | 你自己查（客户端/激活/端口/版本） |
| 连接超时 | 网络层问题 | 网络管理员（防火墙/IP/网段） |
| ping 不通 | 网络根本不通 | 网络管理员 |

---

## 第 1 阶段：把"能拿数"做完（约 1～2 天）

目标：能拉到数据，并确认这些数据能算出 SOH。

### 做什么

```bash
# 全面体检：8 个环节 + 单位校验 + SOH 数据可行性预览
python3 probe.py --transport tcp --host <②的IP>
```

它会挨个检查：连接、设备信息、通道状态、实时数据、历史明细、工步层数据、
上传完整度、日志 —— 然后**直接算一遍 SOH 打印出来**。

### 重点看这四个数字

| 检查项 | 期望 | 不对说明什么 |
|---|---|---|
| `testtime 单位判定` | **ms** | 判定成 s 说明现场行为和协议文档不一致，后面所有时间换算都要改 |
| `辅助通道列名` | 自动列出现场实际的列名 | 列名随硬件变化，解析不能硬编码 |
| `工步类型` | 都在协议 17 种之内 | 出现未定义类型，要现场核对它代表什么 |
| **容量保持率等 SOH 指标** | **能算出数字** | 算不出说明容量口径和现场不一致，**必须在做报告前解决** |

### 同时去要这张表（卡点）

**通道 ↔ 电池条码的对应表。**

这是把数据和"哪颗电池、哪次实验"关联起来的唯一钥匙。
没有它，你采到的就是一堆没有归属的数字，后面做报告时无法回答"这是哪颗电池"。

要问清楚：条码是怎么产生的（扫码？人工录入？）、
一个通道是长期固定一颗电池还是每次都换。

### ✅ 本阶段验收标准

1. `probe.py` 8 项基本都 PASS
2. 最后出现"SOH 数据可行性预览"，且数字和 BTSDA 官方导出一致
3. 手里有通道 ↔ 条码的对应表

**这一步过了，意味着"数据读取 → 指标计算"这条链路完全验证完了。**
后面只剩"排版成报告"和"做成服务"，没有技术风险了。

---

## 第 2 阶段：把"控制"做完（约 1～2 天）

目标：能用程序启动一次测试、观察它、停止它，并自动拿到数据文件。

### ⚠️ 先说一条安全红线

**绝对不要在正在跑测试的通道上做实验！**

实验室的通道可能正跑着别人的实验，你一个 `stop` 就把几天几夜的实验数据毁掉了 ——
**而且原始数据无法追溯补录。** 这不是技术问题，是会赔人的事故。

**正确做法**：
1. 先跟设备负责人确认**哪些通道是空闲的**
2. 在空闲通道上做验证
3. 或者约一个时间段，专门给你测试用
4. 第一次试 `start` 时，先用**很短的工步文件**（比如充放电 10 分钟），
   不要用几天的正式工步

### 要问清的事（卡点）

| 要问的 | 为什么 |
|---|---|
| 工步文件（.xml）放在哪个目录？ | `start` 命令要这个路径 |
| 命名规则？ | 要程序自动选文件就得知道规则 |
| 谁维护？新增测试种类时谁做？ | **如果必须人工做，自动化控制就有个人工环节，要提前跟需求方说明** |
| 有没有一个"测试用"的短工步？ | 验证时用，别拿正式工步试 |

**记住一个坑**：`start` 里的工步文件路径是**② Windows 主机上的路径**，
不是 ③ 上的路径。因为文件是客户端在自己那台机器上读的。写成 ③ 的路径会直接失败。

### 做什么

```bash
# 1. 确认通道状态（挑一个 stop 的空闲通道）
python3 neware_client.py --transport tcp --host <②IP> status

# 2. 点灯，确认你操作的就是现场那个通道（很实用的一步）
python3 neware_client.py --transport tcp --host <②IP> light 25-41-1-1

# 3. 启动测试：通道:工步路径:条码:备注，并开启自动备份
python3 neware_client.py --transport tcp --host <②IP> start \
    "25-41-1-1:D:\\stepManager\\test\\short.xml:BAR001:我的第一次测试" \
    --backup-dir "D:\\NewareBackup" --backup-type nda

# 4. 看实时读数，确认真的跑起来了
python3 neware_client.py --transport tcp --host <②IP> rt 25-41-1-1

# 5. 等它结束（轮询状态）
python3 neware_client.py --transport tcp --host <②IP> wait 25-41-1-1 --timeout 1200

# 6. 停止（如果还在跑）
python3 neware_client.py --transport tcp --host <②IP> stop 25-41-1-1
```

### 一定要做的一件事：现在就开自动备份

上面第 3 步的 `--backup-dir` 会让客户端在**测试结束后自动把 NDA 文件导出**到
`D:\NewareBackup`，文件名用电池条码命名。

**强烈建议从现在开始，每次启动测试都带上这两个参数。** 理由：

> **历史数据无法追溯补录。** 某次测试当时没开自动备份，那次的原始文件就永远丢了，
> 你只能拿到 API 拉下来的部分数据。而开启成本就是在命令上加两个参数。

### ✅ 本阶段验收标准

1. 能在空闲通道上完整走一遍"启动 → 看实时读数 → 等结束（或停止）"
2. 测试结束后，`D:\NewareBackup` 里真的出现了以条码命名的 NDA 文件
3. 手里有工步文件目录和命名规则

---

## 第 3 阶段：把"采集"做成能重复跑的脚本（约 2～3 天）

目标：能自动发现"哪个测试跑完了"，并把数据落下来。

### 核心思路：靠状态变化触发，不要每次都拉全量

```python
# 伪代码，思路而已
上一轮状态 = {}

while True:
    当前状态 = getchlstatus(所有通道)

    for 通道 in 当前状态:
        # 从"测试中"变成了"结束" → 这次测试刚跑完
        if 上一轮状态[通道] == "working" and 当前状态[通道] in ("finish", "stop"):
            采集这个通道的数据(通道)

    上一轮状态 = 当前状态
    sleep(30)


def 采集这个通道的数据(通道):
    # ① 先确认数据上传完整 —— 没传完就拉会缺数据，这是厂商点名的坑
    while not inquiredf(通道).完整:
        sleep(10)

    # ② 工步层汇总：数据量小，算 SOH 就靠它（自带 DCIR）
    汇总 = download_steplayer(通道)
    存成 parquet(汇总)

    # ③ 完整明细：数据量大，按需拉
    明细 = download_all(通道)
    存成 parquet(明细)

    # ④ 记录元数据：文件哈希、解析器版本、采集时间、通道、条码
    #    这是"解析结果可重建"的前提
    记录元数据(...)

    # ⑤ 用条码关联到"哪颗电池、哪次实验"
    关联到电芯(条码)
```

### 几个设计要点

| 要点 | 为什么 |
|---|---|
| **靠状态变化触发** | 每次拉全量太慢太重，而且大部分时候没有新数据 |
| **先 `inquiredf` 确认完整** | 数据还在从中位机上传时就拉，会缺数据 |
| **工步层优先** | 数据量小两个数量级，算 SOH 需要的数字全都有 |
| **记录文件哈希和采集时间** | 将来解析器升级后能重新生成，不破坏原始数据 |
| **错误要分层次处理** | "网络断了"要等待重试，"客户端重启了"要重新连接 —— 两种处理方式不同 |

### 关于"两条数据路径"的配合

```
测试进行中  →  getchlstatus 看进度，inquire 看实时读数
测试刚结束  →  inquiredf 确认完整 → download_steplayer 拿汇总算 SOH
测试结束后  →  读 ② 上的 NDA 文件做完整归档（第 2 阶段已开了自动备份）
历史数据    →  批量读 NDA 文件回灌
```

### ⚠️ 一条边界

**不要频繁调 `inquire`，也不要频繁下发控制命令。**
协议实时性不优于 5 秒；而且这台客户端同时在管整个实验室正在跑的测试，
命令发太密可能影响它正常记录数据。轮询间隔建议 10 秒以上。

### ✅ 本阶段验收标准

让它跑一晚上，第二天能确认：
1. 每个跑完的测试都有对应的数据文件落下来
2. 数据能关联到具体的通道和条码
3. 中途网络抖了一下，程序能自己恢复

---

## 第 4 阶段：把数据变成 Agent 能用的接口（约 2～3 天）

目标：让 ③ 上的大模型能通过一个接口问到指标。

### 做什么

把采集和计算封装成一个**受控接口**，而不是让 Agent 直接去操作客户端或文件系统。

```
输入：通道号 或 时间范围 或 电池条码
输出：SOH 指标（容量保持率、库仑效率、能量效率、DCIR）+ 数据概览
```

为什么要有这一层：**Agent 只调接口，不碰底层的解析器、文件路径和数据库权限。**
这样既安全（权限收在一处），又可维护（解析器升级不影响 Agent）。

这条建议来自《Neware NDA 解析与 BatteryLab 接入方案》，里面已经给了接口草案：
```
POST /ingest/neware      -> dataset_id, parser_version, row_count, validation_status
GET  /datasets/{id}/metadata
GET  /datasets/{id}/timeseries
GET  /datasets/{id}/cycles
GET  /datasets/{id}/quality-report
```

### SOH 指标怎么算

| 指标 | 算法 |
|---|---|
| 容量保持率 | 按 `cycleid` 分组，取每组里**放电类工步**（`dc`/`dv`/`cccd`/`dp`/`dr`）的 `cap` 之和，然后"第 N 圈 ÷ 第 1 圈" |
| 库仑效率 | 同一圈的（放电容量 ÷ 充电容量） |
| 能量效率 | 同一圈的（放电能量 ÷ 充电能量） |
| DCIR | 工步层数据里的 `dcir` 字段，直接用 |

**注意一个容易错的点**：每行的 `cap` 是"**这一步内**累计的容量"，不是整圈的。
所以必须先按循环分组、再按工步类型筛选、最后求和。直接对全表取最大值是错的。

### ✅ 本阶段验收标准

Agent 能回答类似这样的问题："3 号通道那颗电池的容量保持率是多少？"

---

## 时间安排建议

距 10/15 还有约 3 周：

| 周 | 做什么 | 备注 |
|---|---|---|
| **W1** | 第 0 阶段 + 第 1 阶段 | **这是阻塞项，先做完。** 同时催 SOH 报告模板 |
| **W2** | 第 2 阶段 + 第 3 阶段 | 控制要跟设备负责人约时间用空闲通道 |
| **W3** | 第 4 阶段 + 联调 | 留几天给意外情况 |

**关键路径不在你这里，在 SOH 报告模板。** 模板决定要取哪些字段、算哪些指标。
模板不定，第 4 阶段的接口就定不下来，会反复返工。
建议 W1 就并行去催，别等通讯全通了才想起来。

---

## 现在就能做的三件事

1. **确认 ② 的 IP 和客户端版本号** —— 版本号是风险点，老版本 TCP 不可用
2. **在 ② 上激活 API 并重启客户端**，用 TestAPI 点一下【设备信息】
3. **问清三件事**：通道 ↔ 条码对应表、工步文件目录和维护方式、哪些通道是空闲的

第 1、3 项要等别人回答，所以要今天就发出去。第 2 项你自己就能做完。

---

## 附：这条链路上还会用到的东西（后面阶段才涉及）

| 名词 | 一句话 |
|---|---|
| `fastnda` | 开源的 NDA 文件解析库，用来读 ② 上自动备份出来的 NDA 文件 |
| `BDF` | 电池数据标准化格式，把各家设备的字段统一成一套名字 |
| Parquet | 一种列式存储文件，体积小、读取快，适合放时序数据 |
| Parquet vs CSV | CSV 能用 Excel 看但体积大；Parquet 快但要用代码读 |
| Parquet 与原始 NDA | NDA 是**原始证据**，永久保留；Parquet 是**解析结果**，可以重新生成 |
| 元数据 | 记录"这份数据从哪来、什么时候采的、用什么版本解析的"，用于追溯 |

这些属于**数据处理阶段**（不是通讯阶段），到了第 3、4 阶段再深入就行。
